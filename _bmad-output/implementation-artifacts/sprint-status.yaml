# generated: 2026-07-22
# last_updated: 2026-07-24
# project: wc-stats
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# SEQUENCING PLAN (order below = planned work order):
# ===================================================
# - Story 1-4 (template-consistency verification) is deliberately first: it de-risks
#   the top extraction threat (silent mid-tournament template revision) and gates
#   confidence in everything downstream in Epic 1. Its first run is anchor-level only
#   (no extractors yet) and pulls forward a minimal text-anchored discovery slice from 1-2;
#   it is re-run as a gate by every subsequent extraction story.
# - Story 1-1 (contract v1 + fixtures) is second: fixtures/sample bundles are Epic 2's
#   unblocking artifact (AD-14). No Epic 2 story starts before 1-1 is done.
# - Story 1-5 (marker → event-row linking) gates ALL spatial-join stories: it precedes
#   1-11/1-12/1-13/1-14 in Epic 1 and is a data prerequisite for Epic 2's shot-map
#   surfaces with time/player/xG (2-7 onward).
# - Epic 2 interleaves with Epic 1 once 1-1 lands: schema-only stories (2-1, 2-2, 2-3,
#   and fixture-driven UI stories) may proceed against data/fixtures/; stories needing
#   real data at scale (2-19) wait on 1-19.
# - Story 2-1 (i18n scaffolding + tokens + build gates) is first within Epic 2 — never
#   retrofitted. Story 2-3 (contract sign-off) gates Epic 1 proceeding past the sample set.

generated: 2026-07-22
last_updated: 2026-07-27
# 2026-07-27: story 1-9 (Domains E & F extraction) started — status ready-for-dev -> in-progress.
# 2026-07-27: story 1-9 context created and moved to ready-for-dev — and the story-creation
#             probe OVERTURNED the epic's premise for Domain E, corpus-wide over all 104
#             reports / 936 E+F pages. Domain E is printed PER TEAM, not per goalkeeper: all
#             four goalkeeping page families are titled `{team}`, NO goalkeeper name appears
#             anywhere on any of them, and 7 of 208 team-innings used TWO keepers (M21, M41,
#             M53, M62, M66, M88, M98 — verified on M53, where Mexico's RANGEL came off at 78'
#             and the page still prints one chart, one total, no name). AC 1's "every
#             goalkeeper with minutes has a record" is therefore unfulfillable, and so is the
#             contract's per-keeper GoalkeeperRecord. Scope ruled by Juan: re-scope inside the
#             story (the 1.13 precedent), staging per team with Domain A's goalkeeper list
#             carried BESIDE the block so the attribution is recorded, never inferred. Domain E
#             is also four different extraction problems under one name — one table page (goal
#             prevention), one half-table (aerial control), one MAP page (distribution) and one
#             CHART page (involvement) — so the "tabular extractor" framing fits Domain F only.
#             Juan ruled BOTH hard parts IN: the involvement per-minute series is extracted now
#             (95-111 slots regulation / 129-145 extra-time, per-report exactly like 1.8's
#             momentum; scale from the printed y-axis labels, never gridline spacing), and the
#             distribution complete/incomplete split is recovered by counting markers through
#             the SHARED CHAIN read-only — detect_pitch_frames returns exactly 4 equal-area
#             panels (59,516.0 pt2 each) on 208/208, so 1.12's plural accessor + title-anchored
#             panel typing is mandatory and detect_pitch_frame's max() is unusable. The exact
#             MarkerSpec is corpus-verified and pitch_margin_pt=0.0 is LOAD-BEARING: every
#             panel frame ends at y1=406.5 while the Complete/Incomplete legend sits at y~411,
#             so strict containment excludes the legend by geometry (any positive margin
#             inflates every count by 2); 0 off-palette markers admitted on 208/208, and no
#             legend-row exclusion is needed at all. Eleven relations measured EXACT corpus-wide
#             to key Self-Validation on, incl. AC 1's (feet+hands+throw == total panel, 208/208),
#             AC 2's (corner sides sum to total corners, 208/208), the marker-count-vs-printed-
#             donut-centre cross-check, and sum(5 intervention types) == attempts faced
#             (208/208). SIX tempting relations are corpus-FALSE and are named as do-not-ship,
#             headline: the contract's FreeKickCounts $comment asserts direct == directOnTarget
#             + directOffTarget "holds across all six fixture team-innings" — it fails on
#             208/208 REAL team-innings (160 print on+off == 0 while direct > 0). Also false:
#             corner delivery STYLE is not a partition of corners (112/208), and sum(series) ==
#             Total Involvements (149/208) — so the involvement check ships as the BOUND
#             sum(series) <= total, true 208/208 (delta 0..5, never negative, mean 1.26); the
#             residual gap is the one open question left to dev, explicitly NOT to be closed by
#             making the numbers agree (the 1.8/1.12 rule). Three documented absences take the
#             absence branch, all raster-only like 1.13's donut slices: the distribution
#             technique breakdowns, goal prevention's byBodyType, and aerial
#             crossesFacedCompleted. Two page traps pre-filed: the goal-prevention 7-column
#             table must be found by header-anchor + x>=460 (naive "row of 7 digits" finds ZERO
#             on M38 home, where a stray pitch-marker ordinal shares the row, and TWO on
#             M34/M38 away), and the set-plays KPI values print ABOVE their labels with one
#             value row shared by two KPIs. No new AnchorSpec — 1.2 registered all five specs.
#             Gate ids ruled goalkeeping-{completeness,counts} / set-plays-{completeness,counts};
#             deliberately NOT offers-count-match, which is test_checks_registry.py's unclaimed
#             placeholder. Touches pipeline/extract/ + the record/gate seams + conftest only —
#             /contract stays READ-ONLY (1-8's schemaVersion 2 bump has already landed and is in
#             review; the v2 diff touches the momentum defs alone, so every E/F shape 1-9 cites
#             is unchanged), and pipeline/markers/ is read-only for in-dev 1-13 (filter_chain is
#             imported, never edited). Baseline commit 7279637.
# 2026-07-27: story 2-8 context created and moved to ready-for-dev (pass-network visualization —
#             the second PitchPanel consumer, app/ only). Builds on 2.7's seam rather than
#             re-implementing pitch rendering: nodes are PitchMarkers (so the shared extent,
#             Voronoi hit cells, >=44px floor, clustering, popover, roving tabindex and
#             role="figure" all come free) and edges ride the `underlay` prop 2.7 cut for exactly
#             this story. Sixteen ruled decisions; the load-bearing ones: node positions are
#             EXTRACTED not derived (AD-3, not AD-4 — no force layout, d3-force stays
#             uninstalled), the pass network is a FULL pitch by construction (fixture node x
#             spans 20.21-79.71, so pitchExtentFor returns xMin:0 on its own — the halfway-line/
#             centre-circle code path 2.7 unit-tested but never rendered); the edge ramp bins by
#             panel-pooled nearest-rank quintiles so that UX-DR17's "lowest weight quintile" and
#             DESIGN's 5-stop ramp become the SAME partition (resolving the unruled `<` vs `<=`);
#             and isolation is PINNED-ONLY, keyed on playerId never an index — focus does not
#             pin, because aria-pressed on mere focus announces a state the reader never set
#             (the exact defect review-accessibility.md:29 filed against this interaction).
#             Measured at creation time rather than left to dev: all five --edge-weight stops
#             clear 3:1 on the pitch (3.83/5.06/6.60/8.55/10.77) but ADJACENT stops separate by
#             only 1.32/1.31/1.29/1.26 — so DESIGN's "stroke width rising in parallel" is the
#             encoding, not a flourish, and a colour-only ramp would read as one green smear.
#             Dim values measured too: nodes 0.55 (4.02/3.34:1, still hittable targets so still
#             >=3:1), edges 0.25 (1.42:1, a recorded exception — no single alpha both dims and
#             holds the floor across the ramp). 2.8 is the FIRST consumer of --edge-weight-*,
#             the same position from which 2.7's light-theme disaster surfaced, so Task 9.4
#             re-measures. Validation pass (2 fresh-context agents, 44 fact-checks) caught seven
#             CRITICAL defects in the first draft and all are fixed: node dimming had NO
#             mechanism (nodes are markers, unreachable from the underlay closure — now an
#             explicit selection.dimmedKeys), Enter/Space silently collided with PitchPanel's
#             existing popover handler (now a ruled three-way split; clustered nodes keep the
#             dialog and pin from its list items), "Esc clears" was unreachable (focus opens the
#             hover popover, so the guard always ate the first Esc — now a layered panel-level
#             handler, two presses, disclosed), team-switch clear was uncallable from the
#             section, cross-side dimming would have greyed out the opposing team, and the
#             legend's React key collides on narrow distributions. Two DEVIATIONS flagged for
#             Juan: the pass matrix ships PLAIN, not sortable (EXPERIENCE.md:207 lists it among
#             the Expert-layer tables 2.11 owns; UX-DR16's floor is still met in full), and
#             player names are plain text because /players/{slug} does not exist yet. Highest
#             unfiled risk recorded as Task 8.2: nobody has ever probed the "Passing Networks"
#             page, and three of the four Domain D families probed so far had unfulfillable
#             required per-row fields (1.11 CrossEvent, 1.13 ReceivingEvent) — PassNetworkNode.x/y
#             could be the third, and the 2.3 sign-off row walked the contract, not the PDF.
#             Touches app/ + locales only — coordinates around in-dev 1-8 and 1-13, which have
#             contract/, data/fixtures/ and app/src/lib/contract/* dirty; never `git add -A`.
#             Baseline re-measured live at 240 passed / 14 files.
# 2026-07-27: story 2-8 implemented and moved to review — pass-network visualization, the SECOND
#             consumer of 2.7's PitchPanel rather than a second pitch renderer. Nodes are
#             PitchMarkers (shared per-panel extent, Voronoi hit cells, >=44px floor, clustering,
#             popover, roving tabindex, focus ring, role="figure" all inherited); edges ride the
#             `underlay` seam 2.7 cut for exactly this story, inside the aria-hidden decorative
#             group under both the hit layer and the markers. All 16 ruled decisions implemented as
#             written. THE FULL PITCH IS AUTOMATIC AND PROVEN: pitchExtentFor returns {xMin:0}
#             unaided for all six team-innings at all six probed widths, so the halfway-line/
#             centre-circle/centre-spot path 2.7 unit-tested but never rendered was rendered and
#             inspected. The ramp and the quintile split are ONE partition, asserted directly
#             (edgeStop(v,t)==1 <=> v<=t[0] on every fixture edge); thresholds and stop
#             distributions were computed INDEPENDENTLY IN PYTHON before the TypeScript existed and
#             pinned as literals (m001 [4,7,11,14] 12/10/15/7/9; m002 [5,7,13,15] 12/7/10/8/9;
#             m074 [4,9,13,15] 12/7/10/9/7) — one test literal was wrong and the implementation
#             right (m001's top band is 15-18: 18 is the POOLED max, Mexico's own side tops at 17,
#             which is the pooling ruling proving itself). Isolation verified LIVE: Enter pins,
#             aria-pressed=true, exactly 1 selection ring, 8/11 nodes and 18/20 edges dimmed on its
#             OWN figure while the other figure stays 0/0 (decision 15), second Enter clears, and
#             the node's transform is BYTE-IDENTICAL before and after (AR-6). Clustered path also
#             verified: Enter opens the dialog and does NOT pin; its list items do. Two Escapes
#             clear a pin — the ruled, disclosed consequence of focus opening the popover. <md
#             declutter measured: 16 edges drawn -> 20 after "Mostrar todos los pases". Reflow at a
#             TRUE 320 CSS px: scrollWidth == clientWidth == 320, #pass-networks NOT in the overflow
#             list (a 5px in-section bleed is TacticalSection's expanded-state chevron, proven
#             pre-existing by reproducing it identically on #shot-maps). 0 animations in the
#             subtree, 0 console messages on all three fixtures. LIGHT THEME: 2.8 is the first
#             consumer of --edge-weight-*, and all eleven on-pitch tokens read byte-identical in
#             both themes — no .light override exists and none was added. TWO DEVIATIONS for Juan:
#             the pass matrix ships PLAIN not sortable (routed to 2.11 with plug-in points named;
#             UX-DR16's floor is met in full — this is the one decision to overrule if sorting is
#             wanted in 2.8), and player names are plain text because /players/{slug} does not
#             exist. THREE declared departures: passNetworkMarkers takes edges + a valuePhrase
#             callback (degree is required in the accessible name by Task 2.6 and cannot be
#             computed or worded inside src/viz); quintileBands DROPS an empty band rather than
#             labelling an impossible "8-7" range; and a node with an unreadable x/y THROWS naming
#             the playerId (no honest placeholder exists for a position — NaN paints an invisible
#             marker, a drop publishes an invisible absence). TWO items flagged, NOT changed:
#             pitchMarkings draws goal furniture at ONE end only, so the full pitch has a bare
#             defending half (shipped 2.7 code, aria-hidden decoration, no ruled reference for the
#             second end — 2.8 is just the first surface wide enough to expose it); and at the
#             shipped >=lg figure width (527px HORIZONTAL) only 4 of 22 nodes are singletons, so
#             decision 6a's dialog path is the PRIMARY pin route, not the exception — the story's
#             "most clusters are singletons" premise is false, though 6a rules the case correctly.
#             Highest unfiled risk NOW FILED: nobody has ever probed the "Passing Networks" page,
#             PassNetworkNode.x/y is required and must be extracted (AD-3), and three of the four
#             Domain D families probed so far had unfulfillable required per-row fields — routed to
#             1.14 with the note that 2.3's sign-off row for this surface walked the contract, not
#             the PDF. PitchPanel changes are strictly additive: with `selection` absent the Enter
#             branch calls openClusterOf VERBATIM and ShotMapsSection's output is unchanged (all
#             240 pre-existing tests green). No new dependency; d3-force stays uninstalled;
#             tactical-sections.ts's `&&` untouched. Suite 240 -> 307 (14 -> 15 files), build chain
#             green. Touches app/ + locales + the ledger only — coordinates around in-flight 1-8
#             and 1-13; never `git add -A`.
# 2026-07-26: story 2-7 adversarial code review (3 layers over f8ca7ee..1e67e25): 25 unique
#             findings — 4 decisions ruled, 18 patches applied, 1 deferred (ShotLogRow's dead
#             minute fields, routed to 2.11), 5 dismissed. Two defects the 237-test suite
#             structurally could not see, because neither is reachable in a jsdom-less harness:
#             (a) hit cells were seeded on cluster CENTROIDS, so a click dead-centre on a marker
#             could open a popover not containing it — 12 reproduced cases across all three
#             fixtures at 320/386/527/768 px, incl. m074 Germany's crosses at the shipped >=lg
#             width; now seeded per MARKER (AC 3 as written) with the cluster still the hit unit,
#             pinned by 3 new tests that fail if centroid seeding returns. (b) a stale
#             open.clusterIndex passed an `!== null` guard as `undefined` and threw the whole
#             Tactical section into the error boundary on any resize that shrank the cluster
#             count (the story's own measurement: 12 cells at 1920px, 4 at 386px). Also LIGHT
#             THEME: 2.7 is the first-ever consumer of --pitch-surface and the five --shot-*
#             tokens, and .light overrode them to light-canvas variants on a theme-invariant
#             green — every marker 1.62–2.05:1 and every panel string 1.45–1.60:1. The .light
#             --shot-* overrides are deleted (pitch-only hues are theme-invariant by definition)
#             and --ink-on-pitch{,-secondary} / --viz-team-{a,b}-on-pitch added; all on-pitch
#             tokens now 4.58–11.14:1 in BOTH themes. Ruled decisions 3 (extent now per PANEL,
#             not per side) and 9 (popover follows focus — AC 3's third trigger) amended.
#             Co-commit disclosed: 1e67e25 also carried 1-8/1-10/1-13 sprint flips and the 1-10
#             review's ledger entries; ruled to stand as accurate, the Completion Notes claim
#             corrected. Suite 237 -> 240; build chain green. Status review -> done.
# 2026-07-26: story 2-7 implemented and moved to review — app/src/viz/ created (the architecture's
#             own structural seed, first use) plus the reusable PitchPanel that 2.8/2.9 build on.
#             The testability split IS the story: the harness has no jsdom, so projection,
#             ordering, clustering, Voronoi, the encoding maps, the log rows and the figure counts
#             all live in pure src/viz/ modules with real tests (90 of the 99 new tests) and the
#             components stay thin. ONE new runtime dep, d3-delaunay@6.0.4 — exactly the Voronoi
#             module inside the pinned d3 7.9.0, deliberately not the monolith; React owns every
#             SVG node, d3 never touches the DOM. Layout arithmetic in MEASURED CSS px via a
#             ResizeObserver hook, because a scaling viewBox turns the 44 px hit floor into 31 px
#             on a 288 px phone: measured smallest Voronoi cell is 98x118 px at 1920 and
#             135x104 px at 386. AR-6 held and PROVEN rather than asserted — the SM-3 spot check
#             at <md reproduces Story 1.3's permanent ground truth for spike/mex_rsa.pdf (16
#             markers, 2/2/8/3/1), the attacking goal is up the page (penalty area flush to the
#             top edge, goal mouth outside the goal line), and one marker was hand-derived from
#             the artifact to the rendered translate() exactly (95.28/32.00 -> 104.5,32.5). No
#             transposition, no mirroring, no clamping. m074 is the honesty fixture and passes:
#             Germany renders 21 markers and ZERO goal markers with a figure summary reading
#             "0 goles" while keyStatistics.home.goals == 1 (counts come from the marks drawn,
#             never from keyStatistics — ruled decision 12); the own-goal note renders; the log
#             carries 8 PAR rows with "Gol (a.g.)" against 7 PAR markers; none of the 9 shoot-out
#             attempts appear anywhere. Keyboard contract verified live: roving by minute, no
#             wrap, Enter opens a real role=dialog on a 3-marker cluster with focus on the
#             opening marker's own list item, Esc returns focus, and the marker's transform is
#             BYTE-IDENTICAL before and after (overlapping markers are never displaced); repeat
#             pointer clicks cycle the z-order. FD-1 applied throughout (uniform markers, no xG
#             row, no xG column) with tests proving both APPEAR once a constructed shot carries a
#             value. CS-1-proof by construction: maps ShotOutcome only, never ShotOutcomeDetail.
#             CLOSES 2.5 review decision D7 — sectionDataState("shot-maps") now reads BOTH tables
#             and is empty only when shots AND crosses are both null; 2.5's shots=null->"empty"
#             assertion was replaced by the four-way truth table and the ledger entry is struck.
#             Suite 138 -> 237 (10 -> 14 files), build chain green, no console errors. Three
#             recorded departures from the task text, each with its reason: clusterMarkers adds a
#             centroid-stabilisation pass (single-link ALONE makes Task 3.6's ">=44 px by
#             construction" comment and Task 3.8's property test FALSE — an adversarial ring
#             centroids onto a lone marker; the ring is now a named test); CROSS_COMPLETED_ENCODING
#             is split into a static shape Record + a function of the team accent (the colour is
#             per side, so a frozen table cannot hold it); and four singular/plural counter keys
#             were added because t() has no plural machinery and "1 goles" is a visible defect.
#             Flagged for review, NOT changed: #shot-maps renders a heading button at >=lg, which
#             reads against 2.5's own sprint note — confirmed pre-existing (#pass-networks does
#             the same; buildSectionPlans sets collapsible width-independently) and TacticalSection
#             is on this story's do-not-touch list. Reflow: at 316 px client width scrollWidth ==
#             clientWidth and NO #shot-maps element ever appears in the overflow sweep; the 7 px
#             overflow below ~305 px is a Key Statistics tile (2.5 code, already 2.19's).
#             Touches app/ + locales + the ledger only — pipeline/ never staged.
# 2026-07-25: story 1-10 implemented and moved to review — pipeline/extract/domain_g.py, the
#             third instance of the 1.6/1.7 extractor convention and the FIRST extractor that
#             joins to another domain's output. Four page families on one shared row reader
#             over lines.py; ordinal column assignment guarded by the per-family exact-count
#             assertion (14/8/15/9), deliberately NOT x-band classification (three families
#             centre-aligned, physical right-aligned); raw locale-neutral typing with
#             high_speed_runs/sprints asserted integral before narrowing, never rounded.
#             The join is within-report VERBATIM name identity to Domain A's lineups with the
#             shirt number as corroborating key, and it is asymmetric: a page row with no
#             lineup player fails loud (PlayerJoinError), a lineup player WITH minutes and no
#             row fails loud (MissingFieldError), an unused substitute has no row and that is
#             correct — 2,103 such entries (= the story's 8,412 entry x family pairs; 3,289 +
#             2,103 = 5,392 lineup entries, reconciled). Four recorded checks with tolerances
#             derived from printed precision then corpus-verified, incl. the goals
#             reconciliation carrying Story 1.6's own-goal term (naive form is corpus-FALSE on
#             14 team-innings). Parser swept over all 104 reports BEFORE wiring in: 104/104,
#             3,289 rows, 0 check failures, worst drifts 0.200 m / 0.0499 km / 0.500 — every
#             story-pinned tolerance reproduced exactly. Batch 104/104 extracted, 0 failed, all
#             four checks pass on every report (run result FAIL only from 1-12's two
#             pre-existing ledgered discrepancies, not Domain G); gate PASS 0 deviations, both
#             new ids in checks_run, two runs identical apart from run_timestamp. Suite
#             811+1 -> 879+1 (+68 tests), ONE forced repair (the checks_run list, named by
#             Task 6.4). Discovery: the committed m001 fixture's physical.totalDistance
#             disagrees with the printed page on 30 of 31 players while its 8 sibling physical
#             fields match EXACTLY — a fixture-data finding, AD-14 note filed and pinned by a
#             dedicated test. Two further notes filed (playerId is 1.15's; hsr/sprints '.0').
# 2026-07-24: story 2-4 (match route + Hero layer) started — status ready-for-dev -> in-progress.
# 2026-07-24: story 2-4 code review — 16 patches applied, 7 decisions deferred to deferred-work.md; review -> done.
# 2026-07-24: story 2-4 implemented and moved to review — first dynamic route /matches/[slug]
#             (SSG over tournament.json manifest, dynamicParams=false, 3 routes pre-rendered),
#             AR-11 split (build-data fs reader for generateStaticParams/metadata/Hero;
#             MatchBundleRegion fetches only its own bundle at runtime, FR-34). Hero as useT()
#             client components (Spanish canonical HTML + post-hydration swap): stage chip,
#             context line (venue-local kickoff), score row (display-score once, team->/teams
#             links), decidedBy caption (shootout "Penales: 3–4"; extra-time type-first +
#             unit-tested, FR-1 gap), scorers grouped by benefiting team (m074 GOMEZ own-goal
#             in Germany's column, AD-6), five Story-Stat tiles with ▲+accent+sr-only leader
#             (never color-only), lineups disclosure (one aria-expanded; player->/players
#             links ship in DOM). Locale: enums.stage/position filled + match.* namespace.
#             build-data client-import seam added (+3 gate fixtures). Build chain green; 98
#             tests pass (68->98). Browser-verified ES/EN swap (no mixed-language) + light
#             theme + disclosure expand. Note for review: stage chip uses type-label-caps
#             (no uppercase transform) per the story's explicit token over the mockup CSS.
# 2026-07-27: story 1-8 adversarial code review (3 layers over the 1.8-scoped working-tree diff):
#             31 raw findings merged to 25 — 6 decisions ruled, 16 patches applied, 3 dismissed.
#             All four ACs audited as satisfied and every substantive Dev Record claim verified
#             against the codebase rather than taken on trust. Rulings: (a) momentum-coverage was
#             largely TAUTOLOGICAL — two of four clauses unreachable because _clock_structure
#             already fails loud on every clock inconsistency; narrowed and re-documented as the
#             staged-payload backstop it is, with the real strength moved INTO _clock_structure as
#             hard stoppage/FT bounds. (b) the v2 schema encoded NONE of the invariants its own
#             descriptions assert — `{"samples": []}`, reversed order and duplicate stamps all
#             validated clean against AC 3's "never []"; minItems/uniqueItems added inside the bump
#             (order is not expressible in JSON Schema and stays pinned by check + fixture test).
#             (c) Task 3.5's fail-loud text and (d) Task 3.3's 429.13 literal are both STALE — the
#             code is right and the story text now says so. (e) the bump silently invalidated story
#             2-6's aria-valuetext AC: at.minute is NOT unique (m001 carries minute 45 x5, 90 x8),
#             so 2-6 must index its slider by SAMPLE, not minute — filed as an AD-14 note and
#             recorded in the schema description; EXPERIENCE.md deliberately left to UX/2-6.
#             (f) the fixtures gave the chart its own empty page, stripping every co-tenant of the
#             real lineups page — four co-tenant tests added via the unused `decorate` hook.
#             Parser hardening: stoppage bounded against the contract's StoppageMinute (corpus max
#             is 19, so no false fire), a bar-signature template change now fails LOUD instead of
#             staging momentum:null on all 104, legend reads are order-independent and normalized,
#             gridlines cluster by tolerance so unrelated page furniture no longer aborts a report.
#             Re-verified: batch 104/0/2 (M19,M58) with momentum checks 104/104 pass and the
#             extraction byte-identical to pre-patch; gate PASS 0 deviations; app build green at
#             schemaVersion 2. Suite 1136 passed / 1 skipped / 2 failed — both failures pass in
#             isolation and are the shared-working-tree artifact below, not 1.8 defects.
#             PROCESS FINDING (needs a ruling): stories 1-8, 1-9 and 1-13 are being developed in
#             ONE working tree, and code_version() fingerprints all of pipeline/**/*.py. Every
#             batch run therefore computes a different fingerprint and correctly invalidates all
#             104 records, so NO story can verify a skip-unchanged or byte-identity claim (task
#             8.3) while that holds — and long test runs flake on any file another session saves.
# 2026-07-27: story 1-8 implemented and moved to review — momentum-series extraction plus the
#             project's FIRST contract bump (schemaVersion 1 -> 2, AD-14). OQ-5 verified in code
#             over all 104 reports: every pinned fact reproduced (title once per report on page 1,
#             baseline y=429.13 single-valued, peak 50.3805pt, width/pitch 0.7000-0.7004, 0
#             non-integer heights, printed top label == derived peak, orange->home / purple->away
#             proven by matching each legend swatch's name to that report's cover). THREE findings
#             sharpen the story's table: the chart draws TEN grey lines (nine value gridlines plus
#             an axis rule 0.75pt below), so a naive max(y) puts the half height at 50.755 and
#             corrupts the peak cross-check; the peak equals the axis half height EXACTLY on
#             104/104 (an independent confirmation of the auto-scale, now asserted); and the
#             '45HT'/'90FT' merge hazard is neutralized by construction (tick reading is
#             character-level, regrouped on the digit/alpha boundary, so both spellings parse).
#             Task 1.3 CLOSED: first half slot = minute-1 (0 violations/104); the HT tick marks the
#             FIRST slot of the second half (slots 48-56) and slot(M)=M-46+HT_slot holds with 0
#             violations on the 101 reports printing HT — 3 omit it (M67/M86/M104) and infer it
#             from 60/75/90, which must agree; the FT tick is the grid's last slot on 94/94 that
#             print it and 1 omits it (M42, fallback recorded); the 9 extra-time reports print NO
#             ET break anywhere, so ET1 opens after FT and the 120 tick's own 15 regular minutes
#             place ET2 — a derivation, filed in deferred-work.md. TWO numerical bugs were caught
#             by the corpus run and not by unit tests: the floored Euclidean GCD converged on PDF
#             coordinate noise (~0.001pt "unit" on 13/104; fixed by nearest-multiple remainder +
#             smallest-first divisor), and a GCD over individual bar-gap distances drifts (replaced
#             by the outermost span over its whole slot count). ONE design correction mid-story:
#             deriving the unit as peak/printed-label made momentum-axis-scale TAUTOLOGICAL, so
#             the unit is now geometric (GCD over heights) and the printed label is the recorded
#             contrast — geometry contradicting itself still raises, the printed axis
#             contradicting geometry is recorded (SM-C1). Like 1.12's possession-regain map there
#             is NO printed row total, and none was manufactured. Contract: MomentumSample
#             {minute,home,away} -> {at: MinuteStamp, home/away integer >= 0} (the bare Minute
#             could not represent stoppage slots at all — every first-half stoppage minute
#             collapsed onto 45), version 2 across all five artifact schemas, types regenerated
#             for contract/ and app/, all seven fixtures re-stamped, m001 (101 samples) and m074
#             (138) regenerated from real corpus data, m002 LEFT null by design and now pinned by
#             a test + a fixtures-README note. Also did the ledgered _check extraction (momentum
#             would have been the fifth copy) -> pipeline.extract.check_entry. Batch --force:
#             104 extracted / 0 failed / exactly 2 self-validation failures (M19, M58 — 1.12's
#             ledgered pair) with the ruled non-zero exit; momentum-axis-scale and
#             momentum-coverage pass 104/104, 0 momentum warnings, 10,954 samples total (96-145
#             per report). Re-run: 104/104 skipped-unchanged, records byte-identical. Gate PASS
#             0 deviations with both new ids in checks_run; two gate runs byte-identical apart
#             from run_timestamp. Suite 1040 passed / 1 skipped (incl. in-flight 1-10 and 1-13);
#             app 240 passed, lint + check:types + build green. Four forced test repairs, all
#             expected: checks_run list, anchor count 30/47 -> 31/48, version.json == 1, and the
#             app's assert-schema-version test (now reads the generated constant). Coordinated
#             with in-flight 1-13 in the same tree: all shared-file edits append-only.
project: wc-stats
# 2026-07-22: story 1-1 completed and moved to review.
# 2026-07-22: story 1-1 code review complete — 5 decisions resolved, 35 patches applied,
#             3 items deferred to deferred-work.md. Status review -> done.
# 2026-07-22: story 1-2 context created and moved to ready-for-dev.
# 2026-07-22: story 1-2 implemented and moved to review — 104/104 reports extracted,
#             0 failures, all 47 anchors resolved on every report (1.4 sampled 16).
# 2026-07-23: story 2-1 context created and moved to ready-for-dev; epic-2 in-progress.
# 2026-07-23: story 1-2 code review complete — 3 decisions resolved, 31 patches applied,
#             6 items deferred to deferred-work.md, ~20 dismissed. All 7 ACs satisfied.
#             Decisions: requirements.txt folded into code_version; orphan records now
#             fail the run (exit 1); match number bounded 1..104 per the contract.
#             Re-verified after patching: 104/104 extracted, re-run 104/104 skipped (42x),
#             --force byte-identical, 1.4 gate clean, 440 tests pass. Status review -> done.
# 2026-07-23: story 1-3 context created and moved to ready-for-dev.
# 2026-07-23: story 2-1 implemented and moved to review — app/ scaffold (Next 16.2.11
#             static export), full DESIGN.md tokens, typed es/en i18n + Intl helpers,
#             generated contract types, AR-13 build chain green, 24 tests, 4 negative
#             gate proofs, zero-external-request audit clean.
# 2026-07-23: story 1-6 context created and moved to ready-for-dev (Domain A: metadata
#             normalization + lineup-page parse; establishes pipeline/extract/; tabular
#             only — coordinates around in-flight 1-3 (markers/) and 2-1 (app/)).
# 2026-07-23: story 1-3 implemented and moved to review — pipeline/markers/ filter chain
#             + shots parser, AD-6 frame verified on ground truth (16 markers, 2/2/8/3/1),
#             Self-Validation in records + manifest, shots-parse/shots-count-match gate
#             checks. Full run 104/104 PASS, all 104 Self-Validations pass, gate PASS.
#             Discovery: 37 reports' attempts tables span two pages; parser sums table
#             pages, count check unchanged. Suite green at verification point (496+1);
#             later reds in test_ingest_*/test_extract_domain_a stem from 1-6's in-flight
#             pipeline/extract/ fixture needs (venue table / lineup content), not 1-3.
# 2026-07-23: story 1-6 implemented and moved to review — pipeline/extract/ established
#             (Domain A: stage/date/kickoff normalization + 16-venue offset table +
#             two-column lineup parse with glyph-classified goal/own-goal/sub/card
#             minutes), 6 self-validation checks appended beside 1-3's, 2 FR-15 gate
#             checks. Full run 104/104 PASS (all match_metadata populated, all checks
#             pass), gate PASS 0 deviations, re-runs byte-identical, suite 588+1 green
#             (the 1-3-noted reds resolved: make_report lineups page + real venue,
#             shots-check filters, kickoff purity exemption). Discovery: red-football
#             glyph = own goal (score = goals + opponent own-goals verified on all 104);
#             second-yellow unrecoverable from card RGBs — AD-14 note filed.
# 2026-07-23: story 1-3 code review complete — 1 decision resolved (off-shape staged
#             self_validation disqualifies the skip: re-extract, per the match_id trust
#             precedent), 11 patches applied (stroked pitch-frame constraint restored,
#             shape-aware failure summary, e2e ground-truth self-validation assertion,
#             gate parse memo, 5 new tests, doc fixes), 3 deferred to deferred-work.md,
#             6 dismissed on full-corpus evidence. Re-verified after patching: suite
#             593+1 green, batch 104/104 PASS, gate PASS 0 deviations. Status review -> done.
# 2026-07-23: story 2-1 code review complete — 4 decisions resolved, 16 patches applied,
#             3 items deferred to deferred-work.md, 3 dismissed. Highlights: i18n lint
#             gate hardened (bypasses closed, component copy-props gated, 13 lint-fixture
#             regression tests), format helpers fail loudly (Z-kickoff/invalid date/time/
#             non-finite), storage no longer resurrects deleted keys, vendored components
#             reconciled to tokens, shadcn package dep dropped, --ink-on-cyan light
#             ratified in DESIGN.md. Chain + 41 tests green. Status review -> done.
# 2026-07-23: story 2-2 context created and moved to ready-for-dev (site chrome: header,
#             ES|EN + theme toggles with pre-paint bootstrap, attribution footer, static
#             404; resolves the deferred t() boundary policy; touches app/ only —
#             coordinates around in-flight 1-5/1-6 pipeline work).
# 2026-07-23: story 1-5 context created and moved to ready-for-dev (marker–event linking;
#             scoping probe on spike/mex_rsa.pdf settled the digit-glyph semantics: on-marker
#             ordinal labels 1..N joining table row position; outcome cross-check via the
#             contract's x-maps-to-outcome; xG stays null per the contract's corpus-verified
#             decision. Touches pipeline/markers/ only — coordinates around in-review 1-6).
# 2026-07-23: story 2-2 implemented and moved to review — site chrome shell (pre-paint
#             locale/theme bootstrap script generated from tested pure functions, Theme/
#             Locale Contexts with wcstats.* persistence + polite live-region announce,
#             sticky header with ES|EN radiogroup + theme toggles, skip link, attribution
#             footer + /about stub, static out/404.html). Deferred t() boundary items
#             resolved: production fallback-to-es policy + no-restricted-imports client
#             seam (5 lint fixtures). Chain green, tests 41 -> 67, browser-verified
#             (no hydration warnings; persistence across reload; en swap on 404/about).
# 2026-07-23: story 1-6 code review complete — 2 decisions resolved (unknown minute
#             glyph -> unknown-rgb like shots; formation-sum stays a recorded check per
#             Task 7.2), 24 patches applied (5 new fail-loud parser guards, typed
#             MalformedFieldError + calendar/clock validation, full §6 completeness walk,
#             gate memo + PipelineError single-attribution, strict self-validation
#             aggregator moved to the package seam, exact-path kickoff purity exemption,
#             AD-14 own-goal ledger note vs stale contract $comment, 14 new tests, 1.6
#             tests + story finally committed), 2 deferred, 6 dismissed. Re-verified:
#             105 tests in 1.6 files green; Domain A sweep over all 104 PDFs clean (the
#             full batch/gate is currently blocked by in-flight 1-5 label mapping, not
#             1-6). Status review -> done.
# 2026-07-23: story 1-7 context created and moved to ready-for-dev (Domains B & C:
#             Key Statistics 19-field block + contested possession, Phases of Play 8+9,
#             line-height/team-length pages with the 3-per-panel metre investigation;
#             AC 2's "block sum ~100%" corrected on corpus evidence — blocks are
#             independent rates (contract $comment + mex_rsa 43/49); possession-sum +
#             metre-bounds are the recorded checks. Tabular extract/ only — coordinates
#             around in-flight 1-5 (markers/); batch/gate acceptance caveat noted).
# 2026-07-23: story 1-5 implemented and moved to review — pipeline/markers/attempts.py
#             (column-segmented row extraction, frozen label->enum maps) + linking.py
#             (digit-glyph collection, two-pass bijective assignment with outcome-
#             compatibility constraint, merged-word readings, link-rate checks); events
#             enriched with linked/ordinal/time_raw/shirt/player/labels/xG-null;
#             Self-Validation completed (count AND 100% link rate); marker-event-link-rate
#             gate check. Heuristic iterated over 5 full-corpus runs to 2571/2571 markers
#             linked; batch 104/104 PASS, gate PASS 0 deviations, re-runs byte-identical
#             (unblocks the batch/gate caveat noted by 1-6's review). Discovery: 3 AD-14
#             contract gaps — bare 'Incomplete'/'On Target' outcome labels missing from
#             the closed enum; deflected-on-target-defensive-event drawn in BOTH marker
#             colours (10 incomplete / 1 on-target of 11 rows); stoppage-minute finding
#             (time_raw prints cumulative; period only from row order) deferred to 1.16.
# 2026-07-23: story 2-3 context created and moved to ready-for-dev (AD-14 gate: per-surface
#             contract sign-off. Verification story — walks the 1.1 pre-computed checklist
#             against /contract v1 + fixtures; 10 pre-analyzed findings F1-F10 to adjudicate
#             incl. per-shot xG conflict, own-goal stale $comment, and 1.5's ShotOutcomeDetail
#             enum gaps routed to this sign-off; change requests batch into ONE change-set,
#             no schemaVersion bump landing mid-flight under 1-5/1-7).
# 2026-07-23: story 2-3 implemented and moved to review — AD-14 gate walked: 24 checklist
#             rows (15 PASS, 8 PASS-with-note, shot-maps carries the 2 change requests;
#             counts as re-tallied by the 2026-07-23 review — original line said 15+7=24),
#             F1-F10 all adjudicated with evidence citations. Outcome SIGNED-OFF-WITH-
#             CHANGE-REQUESTS: CR-1 (ShotOutcomeDetail + bare incomplete/on-target) and
#             CR-2 (deflected-on-target-defensive-event one-to-many map) batched into
#             change-set CS-1 (one atomic bump commit, must land before 1.16 emission;
#             F2 stale own-goal $comment correction rides it); FR-1 fixture-coverage
#             request routed to 1.18 (no bump); FD-1 rendering decision binds 2.7/2.11
#             (uniform markers, popover omits null xG); F3/F8 NO-CHANGE. Epic 1 extraction
#             1.7-1.15 UNBLOCKED; 1.16 blocked-pending CS-1. Durable record appended to
#             contract/README.md; ledger entries struck/added. No contract file changed;
#             baseline + post-edit guards green (192+1 pytest, check:types, schema-version).
# 2026-07-23: story 1-5 adversarial code review (3 layers over d60827c..e6cfa7e): 19 unique
#             findings — 3 decisions (2 pre-adjudicated by 2.3's CR-1/CR-2; cross-check-as-
#             assignment-constraint ratified by user), 11 patches applied (commit-scope
#             disclosure in the story record — 19816fc carried 1.6's Domain A repairs that
#             fixed a 4-commit ImportError on main; tautological row-count assert removed;
#             exclude_legend_rows back in the production path; _word_readings capped;
#             format_summary dispatch by check id; 7 new tests incl. merged-word rescue
#             assignment-level coverage), 1 deferred (_domain_a_memo, ledgered), 5 dismissed.
#             Re-verified: suite 663+1, batch 104/104 PASS, gate PASS 0 deviations.
#             Status review -> done. Spatial-join stories (1-11..1-14, 2-7+) unblocked.
project_key: NOKEY
# 2026-07-23: story 1-11 context created and moved to ready-for-dev (crosses map parser;
#             reuses the 1.3 filter chain via MarkerSpec per-type tuning; crosses anchor
#             already registered; contract pins CrossEvent completed:boolean + 6-value
#             CrossDeliveryType with NO cross RGB legend anywhere — mandatory probe task
#             first, AD-14 dev-note candidates pre-filed. Self-validation counts against
#             the crosses page's OWN table rows, never key_statistics (1.7's Domain B).
#             Touches pipeline/markers/ + the two extract seams + gate checks only —
#             coordinates around in-dev 1-7; shared-file edits append-only).
# 2026-07-23: story 2-3 adversarial code review (3 layers over e6cfa7e..063544e): 18 unique
#             findings — 3 decisions resolved (CR-2 array entry stands, enum-split recorded
#             as rejected alternative; CS-1 tracked via 1-16 BLOCKED annotation + Task 6.2
#             coordination rule restored + 2.7/2.13/2.18 bound to post-CS-1 enum; fixtures-
#             only no-bump carve-out ratified in the AD-14 section), 15 patches applied
#             (headline: CS-1 recipe now names its pipeline consumers — test_markers_attempts
#             frozen-map asserts, BOTH test_fixtures outcome/detail asserts, attempts.py
#             constants — and proves by the FULL pipeline suite; "nothing else consumes the
#             changed values" was false. Also: project_key restored after accidental delete;
#             1.5-review ledger co-commit disclosed in the Debug Log; verdict tally corrected
#             15+7->15+8 with row 13 reclassified PASS-with-note; stale own-goal README table
#             row corrected; FR-1 guard-test scope added; FD-1 annotated at epic 2.7's AC),
#             0 deferred, 3 dismissed. Status review -> done. 1-16 stays blocked-pending CS-1.
# 2026-07-23: story 1-7 implemented and moved to review — pipeline/extract/domain_b.py
#             (Key Statistics 19-field block + contested possession, label-relative span
#             classification, left=home side verification) + domain_c.py (Phases 8+9,
#             defensive-block projection, line-height bracket classification). Task 4.3
#             investigation resolved the 3-per-panel semantics from measurement-bracket
#             geometry: line height (vertical, touches goal line) / team length (vertical,
#             spans block) / team WIDTH (horizontal) — verified on all 3,744 corpus values.
#             4 recorded checks + 4 FR-15 gate checks; AC 2's corrected checks verified
#             corpus-wide (possession-sum ±0.2, metre bounds; blocks confirmed non-summing).
#             Batch 104/104 PASS, gate PASS 0 deviations, gate re-runs identical apart from
#             run_timestamp, suite 663+1 -> 726+1 (63 new tests). Two forced test repairs
#             (checks_run list; kebab panel keys vs snake-case walk — documented). AD-14
#             line-height shape note + 1.8 momentum location note filed in deferred-work.md.
# 2026-07-23: story 2-4 context created and moved to ready-for-dev (match route + Hero:
#             first dynamic route /matches/[slug] via generateStaticParams over the
#             tournament.json manifest; AR-11 split — Hero pre-rendered from metadata+
#             storyStats via useT() client components, below-Hero fetches only its own
#             bundle; 2.3 sign-off gate verified — all Hero rows PASS (title/OG row
#             PASS-with-note F3), CR-1/CR-2/FD-1 are shot-maps-only, so no Hero-relevant
#             contract change is unresolved; ET decidedBy branch built type-first (FR-1
#             fixture gap, unit-tested); validation pass applied 9 corrections (substitutes
#             field name, tile digit pinning, knockoutScore always-present). Touches app/ +
#             locales only — coordinates around in-review 1-7 and in-dev 1-11 pipeline work).
# 2026-07-23: story 1-11 implemented and moved to review — pipeline/markers/crosses.py on
#             the shared filter chain (7.4pt 2-color markers -> completed:bool; 9pt legend
#             excluded by size window; additive MarkerSpec.pitch_margin_pt=1.0 admits 9
#             corpus touchline crosses, coords clamp to [0,100]; orange+blue bit-identical
#             two-tone double-draws collapse to one completed event, 17/16 pages — real
#             same-spot pairs never deduped). Probe overturned the spec's shots-shaped
#             assumptions: section is ONE page/team (208/208), table is per-player delivery
#             AGGREGATES (no per-event rows, no ordinal glyphs) -> Task 4.2 no-linking
#             branch; SV counts markers vs SUM of Total Attempted column (== printed
#             Attempted panel 208/208). crosses-parse/crosses-count-match gate checks.
#             Suite 764+1 green, batch 104/104 PASS (208/208 crosses checks pass, re-run
#             byte-identical), gate PASS 0 deviations (sample includes the anomaly reports).
#             3 AD-14 notes filed — headline: CrossEvent's required playerId/playerName/at/
#             deliveryType are unfulfillable from this page; 1.16 emission needs an AD-14
#             decision (ride CS-1's successor, not CS-1).
# 2026-07-24: story 1-11 adversarial code review (3 layers: Blind Hunter, Edge Case
#             Hunter, Acceptance Auditor). Acceptance Auditor: 0 AC/constraint violations
#             — all 3 ACs + every Dev Note verified against code, batch (104/104 PASS) and
#             gate (PASS). 1 decision resolved → patch: the unconditional coordinate clamp
#             could silently rewrite a mis-normalized coordinate to a plausible boundary
#             (AD-8 tension, count check blind to it). Fix: COORD_CLAMP_TOLERANCE=0.5 +
#             _clamp_coord helper raising the new typed CrossesCoordinateError beyond the
#             sub-tolerance touchline overshoot (1pt-margin markers overshoot ≤~0.4, so the
#             real case still clamps; existing 100.1 test green). 0 deferred, 13 dismissed
#             (fail-loud, unreachable in the 208/208 corpus, or already ledgered).
#             Re-verified: 170 tests pass (crosses + ingest_record/batch + checks_registry
#             + runner). Status review -> done.
# 2026-07-24: story 1-12 context created and moved to ready-for-dev (defensive-actions map
#             parser; second map family on the shared 1.3 chain, following 1.11's per-type
#             tuning template. Scoping probe (spike + M01-M04, 10 pages) pre-filed in the
#             story: the page carries TWO stroked pitch panels of EXACTLY equal area
#             (61,168 pt2) titled "Forced Turnovers" / "Possession Regain", so
#             detect_pitch_frame's max() silently discards one map — the story's central
#             chain gap (additive multi-frame accessor, shots/crosses byte-identical).
#             All markers share ONE fill (0.18,0.3,1.0), so actionType comes from the
#             panel title, not the colour; the 9.0 pt bullet swatches share that exact
#             blue and sit 0.13 pt above the marker size — size window + panel containment
#             are the only defenses. Left panel count == printed Forced Turnovers on 10/10
#             pages; right panel consistently EXCEEDS printed Possession Regained (and the
#             per-player table sum, which match each other exactly) — unresolved, gating
#             Task 5's SV design and AC 2's documented-absence branch. No linking possible
#             (no ordinal glyphs, no per-event rows). Touches pipeline/markers/ + the two
#             extract seams + gate checks only — coordinates around in-dev 1-10; shared-file
#             edits append-only. Note: 1-12 claims the `defensive-actions-count-match` id
#             that test_checks_registry.py currently uses as its unclaimed placeholder.)
# 2026-07-24: story 1-10 context created and moved to ready-for-dev (Domain G: per-player
#             in/out-of-possession + physical. Tabular only — the third instance of the
#             1.6/1.7 extractor convention; no new anchor spec (1.2 registered all four
#             per-team specs). Story-creation probe + full 104-report sweep pinned every
#             structural question BEFORE dev: all 8 G anchors resolve to exactly ONE page
#             (832/832, no 1.3-style overflow); numeric columns invariant at 14/8/15/9 per
#             family over 3,289 rows each; 0 non-numeric tokens; hsr/sprints print '.0' but
#             are integral 3289/3289. Join integrity resolved in BOTH directions and it is
#             ASYMMETRIC — 3,289 page rows join 1:1 to a lineup player with minutes (0
#             unmatched, 0 shirt disagreements) while 8,412 unused-substitute entries
#             legitimately have NO row; the natural "every lineup player needs a row" check
#             would fire on all 104. Four recorded checks corpus-verified with derived
#             tolerances: zone-sum (worst 0.200 m of 0.35 bound), cross-domain distance
#             reconciliation vs Domain B (worst 0.0499 km), internal consistency (offers
#             movement types sum EXACT; completion worst 0.500), and goals reconciliation —
#             corrected at creation time to include Story 1.6's own-goal term after the
#             naive form was found corpus-FALSE on exactly 14 team-innings (the corpus's 14
#             own goals); with the term, 0 mismatches / 208. Suite baselined 766 passed +
#             1 skipped at f932f6c. Touches pipeline/extract/ + the record/gate seams +
#             conftest only — stays off the marker/parser-chain family for in-dev 1-12.)
# 2026-07-24: story 1-12 implemented and moved to review — pipeline/markers/
#             defensive_actions.py on the shared filter chain. Corpus probe (104 reports /
#             208 pages) confirmed the pre-filed findings and sharpened three: the two
#             panels are NOT exactly equal in area (61,168.1435 vs 61,168.1451), so
#             detect_pitch_frame's max() deterministically discarded the LEFT (Forced
#             Turnovers) map on 208/208 — closed by an additive detect_pitch_frames
#             (proven identical on all 5,448 pages; shots+crosses payloads byte-identical
#             over all 104 reports); each panel also draws 4 stroke-only corner arcs at the
#             marker's exact width (only fill-is-None excludes them) and WHITE filled
#             penalty/centre spots inside the pitch (only marker_min_pt excludes them —
#             admitting either aborts all 104 on an unknown fill). Task 1.2's open question
#             RESOLVED by measurement: the possession-regain map matches NO printed number
#             (best single field 4/208; exhaustive ±1 linear combos never reach 150/208;
#             dedup at 7 thresholds reconciles ≤16/208 and destroys the left panel's own
#             206/208; markers form one contiguous draw block) and R−PR ranges −3..+36,
#             negative on 2 pages — so it takes AC 2's documented-absence branch (table:
#             null, no check, one per-report warning) while forced-turnover checks against
#             the page's printed headline. Panel→type is text-anchored via the frozen
#             label map; no linking possible (0 digit glyphs in-panel), contest_type null.
#             Suite 766 -> 811. HEADS-UP: batch is 104/104 extracted, 0 failed, but 2
#             reports self-validate FAIL — PMSR-M19-ARG-V-ALG (39 markers drawn vs 40
#             printed) and PMSR-M58-TUN-V-NED (33 vs 34), verified by rendering and hand-
#             counting as genuine source self-contradictions, not parse defects. The check
#             was NOT loosened (SM-C1/AD-8), so the full-corpus run now exits 1; filed as
#             NEEDS ADJUDICATION at the top of deferred-work.md with three options. FR-15
#             gate PASS 0 deviations (neither report is in the 16-report sample; the
#             count-mismatch path was proven directly on the anomalous pair). Re-run
#             104/104 skipped-unchanged, records byte-identical.
# 2026-07-26: story 1-12 code review complete and moved to done — 4 decisions ruled,
#             15 patches applied, 2 items deferred to deferred-work.md. Production
#             changes: per-panel DIRECTION-label orientation assertion, both printed
#             headline totals staged as printed_total, panel-count check before title
#             lookup, x-restricted player_x, two-line-name row floor, unambiguous
#             headline column match. Suite green at 895 passed / 1 skipped (that
#             figure includes in-flight 1-10). RULED: the full-corpus batch exits 1
#             by design from 1-12 onward; the clean-run baseline is 104 extracted /
#             0 failed / exactly 2 self-validation failures (M19, M58) — later
#             stories must assert that baseline, never a zero exit code.
# 2026-07-24: story 2-5 context created and moved to ready-for-dev (Tactical Layer shell +
#             Key Statistics + the shared empty-state pattern 2.6-2.10 depend on. Builds
#             ONTO 2.4's route without touching the AR-11 split: the layer mounts inside
#             MatchBundleRegion's loaded branch, so it is client-only — no Tactical markup
#             exists in out/, static-output tests cannot cover it (turned into an assertion
#             instead), and deep links must be re-resolved on mount because the browser
#             gives up on #momentum before the target exists. 11 section shells in the
#             normative order from a pure registry; disclosure precedence ruled: empty
#             sections never collapse (an absence you must tap to find is still silent),
#             key-stats + momentum never collapse (both mockups render them expanded at
#             390px; UJ-1 reads them), the other nine collapse <lg via heading-wrapping
#             <button aria-expanded> with lazy-mounted content (deliberately NOT 2.4's
#             hidden-in-DOM pattern). Key Statistics = the full 19-field Domain B block,
#             paired tiles, higher-value-leads + ▲ + sr-only «líder»; at <md the six
#             mockup-led rows show with the rest behind "Ver todas las estadísticas" —
#             the ruled resolution of AC 2's two clauses (full block vs. one scroll still
#             reaching momentum). contestedPossession is match-level, rendered as a caption.
#             null vs [] is load-bearing: only null triggers the empty state. Registry keys
#             enums.metric by TeamKeyStatistics field name, which is string-identical to
#             MetricCode for 18 of 19 — 2.13 inherits the labels. Closes 2.4 deferred
#             decision D4 (bundle now consumed, so matchId/schemaVersion are validated).
#             Route container widens max-w-2xl -> max-w-6xl for the ≥md two-column grid.
#             Touches app/ + locales only — coordinates around in-dev 1-10 and in-review
#             1-12 pipeline work; never `git add -A`.)
# 2026-07-24: story 2-5 implemented and moved to review — Tactical Layer shell mounted inside
#             MatchBundleRegion's loaded branch (client-only, AR-11 untouched): 11 section
#             shells from a pure registry (tactical-sections.ts) in the normative order, each
#             with its stable anchor. Disclosure resolved against the mockups: at ≥lg the nine
#             collapsible sections render in the NON-collapsible presentation (plain <h2>, no
#             trigger, no summary — the desktop mockup's only aria-expanded is the Hero's), at
#             <lg they are heading-wrapping <button aria-expanded> shells with one-line summary
#             and LAZY-MOUNTED content (deliberately not 2.4's hidden-in-DOM pattern); key-stats
#             and momentum never collapse, and an empty section never collapses at any width.
#             Focus-on-expand is nonce-driven so a ≥lg render or a resize never steals focus.
#             Key Statistics: 19 paired tiles (higher value leads + ▲ + sr-only líder), six
#             compact rows + "Ver todas las estadísticas" at <md, contestedPossession as a
#             caption. Shared EmptyStatePanel + a distinct PendingSectionPanel (a ready section
#             with no view yet must never claim the report omits it). Anchors re-resolved on
#             mount + hashchange because the browser gives up on #momentum before the layer
#             exists; scroll-padding-top added for the sticky header. Closes 2.4 deferred
#             decision D4: the bundle is now held and validated (matchId + SCHEMA_VERSION)
#             before "loaded". Route container max-w-2xl -> max-w-6xl (Hero keeps its measure).
#             Locale: tactical.* namespace + enums.metric (19, keyed by field name so 2.13
#             inherits 18) + enums.unit.km. Suite 103 -> 131, build chain green. Browser-verified
#             at ≥lg and <lg (fold, keyboard, deep links + hashchange, m002 empty state, ES/EN,
#             light theme). One item filed: the page reflows cleanly at 320 CSS px but not at
#             195 (header + Hero + key-stats together) — routed to 2.19.
# 2026-07-26: story 2-7 context created and moved to ready-for-dev (pitch-panel infrastructure
#             + shot & cross maps — the reusable base 2.8/2.9 build on). Creates app/src/viz/
#             (the architecture's own structural seed, first use) and extends the ESLint
#             client-import seam to cover it — closing, for the new directory, the residual gap
#             the 2.2/2.4 reviews filed. ONE new runtime dep: d3-delaunay@6.0.4, exactly the
#             Voronoi module inside the pinned d3 7.9.0, deliberately not the monolith; the
#             affine projections are plain arithmetic and React owns every SVG node (d3 never
#             touches the DOM). Testability drove the split: the harness has no jsdom, so
#             projection/ordering/clustering/Voronoi/encoding/log-rows all live in pure
#             src/viz/ modules with real tests and the components stay thin — the same shape
#             that made buildSectionPlans the tested heart of 2.5. Twelve ruled decisions;
#             the load-bearing ones: the cross map is a SECOND PANEL inside #shot-maps (the
#             registry stays at eleven sections); `shot-maps` is empty only when shots AND
#             crosses are both null, with a single missing table naming its own absence in
#             its panel slot — this CLOSES 2.5 review decision D7, deferred here with the
#             reason "2.7 owns shot/cross maps and should rule", and it BREAKS 2.5's existing
#             `shots = null -> empty` unit assertion, which the story names as a required
#             update; cross markers carry the TEAM ACCENT with completion dual-encoded by
#             fill, not the shot-outcome hues (borrowing two would put one hex on two meanings
#             inside one section); half pitch by default but full pitch the moment any event
#             has x < 50 (never clamp — AR-6); layout arithmetic in MEASURED CSS px, because a
#             scaling viewBox turns the 44 px hit floor into 31 px on a 288 px phone; and the
#             root <svg> is NOT aria-hidden (AC 3's literal wording describes an axe
#             aria-hidden-focus violation — decorative subtrees are hidden, the marker group
#             is exposed). FD-1 applied throughout: uniform marker size, no xG row, no xG
#             column. Story-creation probe pinned the traps BEFORE dev: m074 Germany has
#             keyStatistics.goals == 1 and ZERO goal markers (GOMEZ's own goal benefits
#             Germany but is excluded from the map per AD-6), so figure summaries count
#             rendered marks and never read keyStatistics.goals; CrossEvent's playerId/
#             playerName/at/deliveryType are handcrafted fixture values (Story 1.11 proved
#             them unfulfillable from the source page — 1.16 emission is blocked pending an
#             AD-14 decision), so the cross popover/table must be null-defensive today. SM-3
#             has real ground truth: spike/mex_rsa.pdf IS the m001 report and Story 1.3 pinned
#             Mexico at 16 markers, 2/2/8/3/1 — the verify task compares the rendered map
#             against it at <md, where the vertical attacking-goal-up orientation is the
#             source's own. CS-1-proof by construction: this story maps ShotOutcome (stable
#             five values) only, never ShotOutcomeDetail. Touches app/ + locales only —
#             coordinates around in-review 1-10; never `git add -A`. Baseline measured at
#             f8ca7ee: 138 passed / 10 files, build green.
# 2026-07-26: story 1-8 context created and moved to ready-for-dev (momentum-series
#             extraction + the project's FIRST contract bump). OQ-5 was RESOLVED at story
#             creation by a full 104-report probe rather than deferred to dev: the series is
#             the page-1 (lineups page) chart titled "Distribution in the Final Third" —
#             present exactly once per report on 104/104, and the report's ONLY per-minute
#             two-team series (no page anywhere contains the text "Momentum", confirming
#             1.7's Task 9.3 note). Verified 104/104: baseline y=429.13 constant, home fill
#             (1.0,0.239,0.0) grows UP / away fill (0.702,0.533,1.0) grows DOWN, colour->team
#             proven by matching the legend name to each record's metadata, bar width =
#             0.70 x slot pitch exactly, peak bar ALWAYS 50.38pt (the chart auto-scales), and
#             the printed y-axis top label == the peak value in units — the one printed
#             cross-check the page offers. Values are exact non-negative INTEGERS (0
#             non-integer heights corpus-wide); pitch and unit are PER-REPORT (1.95-2.95pt /
#             2.40-5.60pt), so only the baseline, peak height and width ratio are constants —
#             a fixed-width filter silently caught only 75/104 on the first probe pass. 9
#             extra-time reports carry a 120 tick (span 131-144 vs 94-113). Like 1.12's
#             possession-regain map there is NO printed row total: the bar sum matches no
#             Domain B field over all 208 team-innings (receptions_in_final_third is
#             consistently smaller), so the axis-scale check is the genuine validation and
#             must not be manufactured into a fake reconciliation. Contract finding that
#             shapes Task 6: the provisional MomentumSample.minute $refs Minute (0-120,
#             "capped at the end of the period", stoppage carried separately), so it CANNOT
#             represent the 94-144 stoppage-inclusive slots — every first-half stoppage slot
#             collides on minute 45. Fix follows existing precedent: minute -> at:
#             MinuteStamp (the {minute, stoppageMinute} composite already used by every
#             other `at:` field), plus number -> non-negative integer. Bump is NOT strictly
#             additive and the story says so. Safe anyway: an app/src sweep found NO
#             field-level consumer — 2.5 (done) only tests `bundle.momentum !== null`, 2.6 is
#             backlog. Fixture trap flagged: m002's `momentum: null` is a DELIBERATE synthetic
#             edge case (AD-14 requires one) and must NOT be regenerated — all 104 real
#             reports have a band, so the null branch never fires on real data. Anchor
#             "Distribution in the Final Third" is NOT in the registry; story adds it. One
#             question left to dev: slot->minute mapping (first half is stable at slot=minute-1;
#             everything after HT shifts by the per-report stoppage allotment, and tick labels
#             merge as '45HT'/'90FT' on 13 reports). Touches pipeline/extract/ + the record/
#             gate seams + /contract + fixtures — 1-10 (review) and 2-7 (in-dev) touch neither
#             /contract nor extract/momentum; app sessions MUST regenerate types after the
#             bump lands. Suite baseline 895 passed / 1 skipped at f8ca7ee.
# 2026-07-26: story 1-13 context created and moved to ready-for-dev — and the story-creation
#             probe OVERTURNED the epic's premise, corpus-wide over all 104 reports / 416
#             receiving pages: NEITHER `Offering to Receive` nor `Movement to Receive` is a
#             marker-scatter pitch map. They are dashboards. The offers page's two titled
#             panels each hold exactly 11 filled 8.229 pt dots whose positions are identical
#             between the two panels on 208/208 AND identical across every team-page sampled —
#             a static formation template carrying zero data; the movement page's single pitch
#             panel holds ZERO markers (a three-thirds bar chart), and its donut slice values
#             are inside RASTER IMAGES (one text word per donut, the centre total). There is no
#             coordinate, no per-event row and no ordinal glyph anywhere in the family, so
#             `ReceivingEvent` (playerId/playerName/type/movementType/at/x/y per row) is
#             UNFULFILLABLE — strictly harder than 1.11's CrossEvent blocker, which at least
#             had real coordinates. Scope ruled by Juan: re-scope inside the story rather than
#             correct-course. The story extracts what the pages prove (offers: 5 KPIs, the two
#             in-panel shape badges, Most-Offers block, per-player made/received/% table;
#             movement: 4 donut centre totals, the 15-cell third x type grid, Top Ranked
#             Players) and NINE reconciliations were measured exact corpus-wide to key
#             Self-Validation on — incl. three cross-domain ones against Domain G's per-player
#             offers (total made/received, and the grid's per-type totals == G's FIVE-type sum,
#             never its six-type sum, which is what IDENTIFIES what the movement page counts),
#             plus the table percentage == round(100*received/made, 1) exact on 3,208/3,208
#             rows (81 rows print made == 0 — the family's one crash branch). Two documented
#             absences take AC 2's absence branch: raster-only slice values, and the per-phase
#             totals which are NOT a partition (phase sum minus total ranges -48..+314, equal
#             on only 3/208) — never sum them. The chain is still composed (frame detect +
#             an 11-dot/one-fill census tripwire) so a future template that draws real markers
#             aborts loud instead of publishing silence; no chain edit. Gate ids ruled
#             `receiving-parse`/`receiving-count-match` deliberately — `offers-count-match` is
#             test_checks_registry.py's unclaimed placeholder (1.12 moved it there) and
#             claiming it breaks that test. Task 7 owes the AD-14 filings: the 1.16 emission
#             blocker (`events.receiving` can only be null), the re-scope notice for Story 2.9,
#             and the notice that 2.3's offers/movement sign-off row (PASS-with-note on an
#             unfixtured `movementType: null`) was filed without knowing the source cannot fill
#             the shape at all. Touches pipeline/markers/ + the two extract seams + gate checks
#             only — stays off pipeline/extract/ for in-review 1-10; deferred-work.md's
#             filter-chain advisory reaches its LAST named story here.
# 2026-07-26: story 1-13 implemented and moved to review — pipeline/markers/receiving.py.
#             The story-creation probe's premise overturn was RE-DERIVED from scratch over
#             all 104 reports / 416 receiving pages by an INDEPENDENT prototype extractor
#             written before the module existed, then compared cell-for-cell with the
#             shipped parser: 208/208 sides agree, 0 disagreements. Every pre-filed figure
#             reproduced exactly — offers 4 qualifying rects / 2 distinct panels 208/208,
#             11 dots per panel on 416/416 panels at 8.229 pt in ONE fill with a
#             BYTE-IDENTICAL 11-offset signature on all 208 pages, exactly 1 word inside
#             each panel (the badge), row counts 16/15/17/14/13 = 154/26/15/11/2, movement
#             exactly 1 stroked panel with ZERO markers and 48 in-panel digits (15 grid +
#             33 axis ticks), 5 deduped image rects with exactly 1 digit inside each titled
#             donut, 5 Top-Ranked rows 208/208. All NINE reconciliations exact: r1-r5
#             208/208, r6-r8 208/208 (incl. #8, the grid == Domain G's FIVE-type sum that
#             IDENTIFIES what the page counts), r9 3,208/3,208 rows worst deviation 0.0 pp.
#             The crash branch measured: 81 rows print offers_made == 0 and all 81 print
#             "0%" — skipped by count, never divided, never coerced. Both documented
#             absences confirmed: donut slices raster-only, phase delta -48..+314 zero on
#             3/208 — recorded as null counterparts + one warning each, NO check (the
#             binary aggregator would fail every record). Seven check ids / 26 checks per
#             report, both operands always present; the two cross-domain families computed
#             at the extract seam and emitting NOTHING when Domain G is unavailable. The
#             chain is composed but stages nothing: the 11-dot census is a template-revision
#             tripwire so a future page that draws real markers aborts loud instead of
#             publishing silence — NO chain edit. Gate ids receiving-parse /
#             receiving-count-match (offers-* deliberately unclaimed — it is
#             test_checks_registry's placeholder). DISCOVERY not in the pre-filed probe:
#             four corpus pages print a THREE-LINE player name straddling its numeric row,
#             which the first full sweep failed on; closed with the crosses/defensive-actions
#             name x-band. ONE forced repair, and it was the named trap: counts keyed
#             grid_by_type by kebab codes (the 1.12 slip), caught by
#             test_record_keys_are_snake_case. Suite 906+2F -> 1040 passed / 1 skipped /
#             0 failed (+66 tests from this story). Batch --force 104 extracted / 0 failed /
#             exactly 2 SV failures (M19, M58 — 1.12's ledgered pair, no third), re-run
#             104/104 skipped-unchanged, records byte-identical 104/104. FR-15 gate PASS
#             0 deviations with both new ids in checks_run; deviation path proven live on a
#             forced-mismatch directory (one deviation per failing team AND check id, both
#             operands in specifics). Task 7 filed six AD-14 notes — headline: ReceivingEvent
#             is unfulfillable in EVERY required field, so 1.16 can only emit
#             events.receiving: null, Story 2.9 must be re-scoped against the aggregates,
#             and 2.3's offers/movement sign-off row is superseded. deferred-work.md's
#             filter-chain advisory reached its LAST named story and is CLOSED (facets a/b
#             never engage — one fill; facet c held on two independent geometry defenses).
# 2026-07-27: story 1-13 code review — 3 decisions ruled, 14 patches applied, status done.
#             Three adversarial layers over the 1.13-scoped diff; all three ACs audited as
#             satisfied and every Task 2.x/3.x/4.x constraint plus the scope boundaries
#             verified independently. The substantive theme was TEST EVIDENCE, not parser
#             correctness: every player row carries the same DEFAULT_OFFERS, so total_made
#             is always 21 x N — divisible by 3 for any lineup — and the fixture's
#             total//3 and total//2 splits printed 84/84/84 thirds and 126/126 shape
#             badges. test_panel_typing_is_text_anchored_not_positional was therefore
#             asserting 126 == 126 and passed whether panel typing read the printed titles
#             or merely the x order; the movement grid was identical across all three
#             thirds, so any permutation was invisible. Fixtures de-degenerated to
#             asymmetric splits. RULED: the grid now asserts 15 DISTINCT (pitch third,
#             movement type) pairs — the 5-per-third guard alone was satisfied by a
#             cross-band swap, and no reconciliation is third-sensitive (grid-total and
#             domain-g both sum across thirds); phase_sum REMOVED from counts (Task 7.4
#             tells downstream never to sum the phases, and staging a pre-computed sum is
#             what invites it); receiving-offers-table-pct now records both operands. The
#             gate's receiving anchor loop was hand-rolled and skipped _domain_anchor_pages'
#             registry-drift LookupError, so an authoring bug would have surfaced as
#             ReceivingPageLayoutError(pages=None) on all 104 reports — routed through the
#             shared helper. Also: top_ranked_players asserts five DISTINCT types (a future
#             sixth "No Movement" row was silently skipped while the 5-row count still
#             passed), _count_check spreads **extra first so a caller keyword cannot
#             overwrite the verdict, _rotated_lines uses the direction tolerance instead of
#             bit-exact equality, _labelled_value splits its two diagnoses, and two
#             cross-domain FAILING-case tests were added (Task 5.2 required pass and fail
#             for each of the seven ids; those two had only the green path). Three doc
#             figures corrected: 78 -> 81 zero-made rows, 66 -> 67 tests, "six AD-14 notes"
#             -> five (Tasks 7.1-7.5) plus two ordinary deferrals. 7 findings deferred to
#             the ledger, 5 dismissed. Suite 1041 passed / 1 skipped (+2 from this review);
#             the lone test_code_version_is_stable_across_calls failure is the documented
#             in-flight-edit artifact (Story 1.9 was editing pipeline/** during the run)
#             and passes clean in isolation.
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-1: in-progress
  # De-risking gate first (per sprint-planning directive): venue × matchday template check
  1-4-template-consistency-verification-across-the-venue-matchday-sample: done
  # Contract bootstrap (AD-14) — unblocks Epic 2 via fixtures
  1-1-contract-v1-schemas-fixtures-type-generation-spike: done
  1-2-batch-ingestion-run-manifest-text-anchored-page-discovery: done
  1-3-shots-pitch-map-parser-with-marker-count-self-validation: done
  # Gates all spatial-join stories (1-11..1-14 and Epic 2 spatial surfaces)
  1-5-marker-event-linking-via-digit-glyph-proximity: done
  1-6-domain-a-extraction-metadata-lineups-formations: done
  1-7-domains-b-c-extraction-key-statistics-tactical-identity: done
  1-8-momentum-series-extraction-oq-5-resolution: done
  1-9-domains-e-f-extraction-goalkeeping-set-plays: in-progress
  1-10-domain-g-extraction-per-player-performance-physical-data: done
  1-11-crosses-map-parser: done
  1-12-defensive-actions-map-parser: done
  1-13-offers-movement-to-receive-parsers: done
  1-14-pass-network-extraction-nodes-edges: backlog
  1-15-cross-match-identity-resolution-normalized-spine: backlog
  # BLOCKED-pending CS-1 (Story 2.3 sign-off change-set: ShotOutcomeDetail enum + map +
  # pipeline constant/test updates — see deferred-work.md "Filed by Story 2.3 sign-off").
  # Do NOT start 1-16 before CS-1 lands as its atomic AD-14 bump commit.
  1-16-match-bundle-emission-canonical-serialization-version-stamp-budget-gate: backlog
  1-17-tournament-index-results-standings-leaderboards: backlog
  1-18-team-player-profile-artifacts: backlog
  1-19-full-batch-run-batch-report-104-104-acceptance: backlog
  epic-1-retrospective: optional
  epic-2: in-progress
  # i18n scaffolding first within Epic 2 (per sprint-planning directive); requires 1-1 fixtures
  2-1-static-app-scaffold-with-design-tokens-i18n-structure-build-gates: done
  2-2-site-chrome-header-language-theme-toggles-footer-404: done
  # AD-14 gate: sign-off before Epic 1 proceeds past the sample set
  2-3-contract-v1-per-surface-sign-off: done
  2-4-match-route-hero-layer: done
  2-5-tactical-layer-shell-key-statistics-empty-state-pattern: done
  2-6-momentum-timeline: backlog
  # Spatial surfaces from here need 1-5 (marker–event linking) semantics in fixtures/data
  2-7-pitch-panel-infrastructure-with-shot-cross-maps: done
  2-8-pass-network-visualization: review
  2-9-receiving-defensive-action-maps-heatmap-decision: backlog
  2-10-phases-pressing-blocks-set-plays-goalkeeping-sections: backlog
  2-11-expert-layer-per-player-tables-event-logs: backlog
  2-12-tournament-hub-results-standings: backlog
  2-13-tournament-leaderboards: backlog
  2-14-header-search: backlog
  2-15-player-profile: backlog
  2-16-team-profile: backlog
  2-17-comparison-mode: backlog
  2-18-glossary-about-terminology-completion: backlog
  # Requires committed real /data from 1-19
  2-19-performance-accessibility-hardening-real-data-swap-launch: backlog
  epic-2-retrospective: optional
