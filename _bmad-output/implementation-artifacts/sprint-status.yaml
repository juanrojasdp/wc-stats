# generated: 2026-07-22
# last_updated: 2026-07-24
# project: wc-stats
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# SEQUENCING PLAN (order below = planned work order):
# ===================================================
# - Story 1-4 (template-consistency verification) is deliberately first: it de-risks
#   the top extraction threat (silent mid-tournament template revision) and gates
#   confidence in everything downstream in Epic 1. Its first run is anchor-level only
#   (no extractors yet) and pulls forward a minimal text-anchored discovery slice from 1-2;
#   it is re-run as a gate by every subsequent extraction story.
# - Story 1-1 (contract v1 + fixtures) is second: fixtures/sample bundles are Epic 2's
#   unblocking artifact (AD-14). No Epic 2 story starts before 1-1 is done.
# - Story 1-5 (marker → event-row linking) gates ALL spatial-join stories: it precedes
#   1-11/1-12/1-13/1-14 in Epic 1 and is a data prerequisite for Epic 2's shot-map
#   surfaces with time/player/xG (2-7 onward).
# - Epic 2 interleaves with Epic 1 once 1-1 lands: schema-only stories (2-1, 2-2, 2-3,
#   and fixture-driven UI stories) may proceed against data/fixtures/; stories needing
#   real data at scale (2-19) wait on 1-19.
# - Story 2-1 (i18n scaffolding + tokens + build gates) is first within Epic 2 — never
#   retrofitted. Story 2-3 (contract sign-off) gates Epic 1 proceeding past the sample set.

generated: 2026-07-22
last_updated: 2026-08-04
# 2026-08-04: story 2-11b context created and moved to ready-for-dev — the Expert Layer shell and
#             the Domain G per-player tables. Baseline moved 163fa20 -> 3d27ed0, and the re-verify
#             at that baseline found THREE CLAIMS IN THE SPLIT STUB HAD GONE STALE, each of which
#             would have cost the dev a wrong implementation: (1) the TacticalErrorBoundary
#             copy-override parameterisation the stub rules as work to do ALREADY SHIPPED in 2.18
#             (optional headlineKey/explanationKey) — only the hardcoded console label remains, and
#             it must be named logLabel because `label` is on the i18n gate's sixteen gated prop
#             names; (2) `enums.unit.m` ALREADY EXISTS alongside km and kmh, so the metres ruling
#             costs no minting; (3) the stub's "exhaustiveness test" worry is misaimed — there is no
#             orphan-key or top-level-namespace assertion anywhere, so `expert.*` is safe; what
#             actually binds is the AD-12 mirroring test and the forbidden-register sweep, whose
#             `a puerta` clause a naive "Intentos a puerta" label would trip.
#             THE STORY'S REAL COST WAS FOUND AT THE TABLE PRIMITIVE, not the shell: 2.11a's
#             DataTable ships FOUR props, `border-collapse`, and no scroll container, so AC 2's
#             sticky-header residual and AC 3's sticky player column are unreachable without
#             extending it. Ruled additive rather than forked (2.11a decision 1, ruled by Juan, is
#             ONE shared table): two optional presentation fields on TableColumn, `sticky` and
#             `tableName` on DataTable, every default path byte-identical. Under `border-collapse`
#             the TABLE paints cell borders, so a sticky header's bottom border scrolls away —
#             border-separate is mandatory, not cosmetic. The z-ladder is spelled out because a flat
#             one fails silently: <tbody> is later in the DOM than <thead>, so equal z means body
#             cells paint over header cells on diagonal scroll.
#             ONE MEASUREMENT IS LEFT TO THE DEV RATHER THAN GUESSED: at 390px a three-column sticky
#             run leaves ~146px of data columns. The task requires measuring it and records a
#             PRE-AUTHORISED escape (a <md team ToggleGroup, PitchPanel precedent) if fewer than two
#             data columns are visible — a decision rule instead of a coin flip.
#             Also ruled here, both routed by the 2.11a review: the announcement-ambiguity mechanism
#             ships (optional tableName) while the ~23 remaining table identifiers re-file, since
#             2.11b adds exactly ONE table and cannot verify the multi-table case; and
#             PendingSectionPanel is KEPT with its delete re-routed — the Expert Layer's absence
#             state is a real EmptyStatePanel, so it declines the shell 2.10 decision 20 offered it.
# 2026-08-04: story 2-18 implemented and moved to review — the TERMINOLOGY GATE closes, plus
#             /glossary (42 terms), /about filled, and the project's FIRST WCAG 1.4.13 surface.
#             THE STORY'S OWN ABORT RULE FIRED MID-IMPLEMENTATION and was escalated rather than
#             worked around: the 2.11a session began writing app/ and took es.ts, en.ts and
#             i18n.test.ts — the exact three files the rule names. Tasks 1-7 (append-only) were
#             completed under the rule, then JUAN RULED PROCEED, and Tasks 8-11 landed with
#             targeted string-replacement edits only, every anchor re-grepped at edit time.
#             ALL THREE VIOLATED POLICY ROWS REMEDIATED: "Córners … laterales" -> "Tiros de esquina
#             … saques de banda" (it contradicted the SEVEN sibling viz.setPlays keys in its own
#             section); "en propia puerta" -> "en contra" (the peninsular goal-frame noun, and the
#             reason the new register sweep could not go green before it); and row 38's possession
#             vocabulary, where the naive "repoint the six leaves" would have turned "Altura de la
#             línea con balón" into "En posesión" — only TWO leaves are standalone vocabulary, the
#             other four are compound metric labels and were rephrased.
#             THE POLICY TABLE WAS RECOUNTED AND NOW SUMS: 26 compliant + 3 violated + 7 not-yet-used
#             + 2 partial = 38, against a creation audit whose own partition did not sum. The recount
#             found a NINTH undischarged item the audit's list of eight missed — `faltas` (row 32)
#             occurs 0 times in es.ts and has no glossary id either, since the row is classified as
#             scaffolding. Filed.
#             FOUR DEFECTS FOUND BY DRIVING THE REAL THING, not by reading code. (1) ESC WAS A NO-OP
#             FOR KEYBOARD USERS: Esc with focus inside the panel closed it, Radix's onCloseAutoFocus
#             returned focus to the trigger, and the trigger's own focus handler instantly RE-OPENED
#             it — so the panel could never be dismissed. Fixed with a 150ms dismissal suppression;
#             all four Esc clauses now pass live. (2) The first ESLint closure flagged the DICTIONARY
#             KEY inside `value={t("…")}` and recharts' own `position: "insideLeft"` layout keyword —
#             both would have broken the build; the selectors are now direct-child and scoped to the
#             copy-bearing object members. (3) Task 8.13's ruled summary rewrite had been applied to
#             es.ts ONLY — caught by this story's own new ruled-term pin. (4) A trailingSlash slip in
#             a new static-output assertion.
#             VERIFIED LIVE, NOT ASSERTED: WCAG 1.4.13 hoverable+persistent proven with a REAL
#             pointer travelling from the term across the offset gap and resting on the panel's link
#             (`:hover` confirmed) with the panel still open 600ms later; role="dialog" with an
#             accessible name and ZERO <h2> in the panel; the panel's link is the IMMEDIATELY NEXT
#             focusable after the trigger with nothing between (the no-portal clause); `#{id} button
#             button` EMPTY across all eleven sections (decision 6's whole reason for existing);
#             aria-describedby on the nine collapsible triggers flattens to the complete unmangled
#             sentence even with a focusable term inside it, so decision 6's declared fallback was
#             NOT taken; single-open holds across 6 triggers; decision 13's counterpart subtitle
#             INVERTS on a real locale toggle, with xG showing the ruled gloss and momentum showing
#             neither. Contrast measured in BOTH themes with the method validated first (13.56/10.30
#             and 14.13/3.30 reproduced exactly): every text pair clears its floor, light-theme
#             definition ink 15.43, link 4.68, dotted underline 4.99-5.36. Reflow clean at 320/390 on
#             both new routes; the 5px match-route overflow is the PRE-EXISTING tile-value pair and
#             NONE of the six marked surfaces or popovers is an offender.
#             THE PER-SECTION ERROR BOUNDARY IS PROVEN, twice: a probe throw showed #phases carrying
#             the new fallback copy while THE OTHER TEN STILL RENDERED and console.error still fired,
#             and a throw-once probe showed the `${id}-${open}` key producing a fresh instance. The
#             residual limits are filed rather than glossed: a throw during sectionContent()'s eager
#             evaluation still reaches only the outer boundary, and key-stats/momentum never collapse
#             so a crash in either is permanent for the page's life.
#             FILED, NOT FIXED: FIVE Tactical sections cannot carry a mark at all — their ruled 2.5
#             summaries contain no policy term while their titles, which decision 6 forbids marking,
#             do. Juan ruled: file it, ship six marks (five sections + the Hero's xG). Also filed:
#             the two viz.momentum copy holes (Task 8.8 discharged as a filing), the
#             DESIGN.md/EXPERIENCE.md footer disagreement, the <title>/OG-stays-Spanish decision
#             (which /glossary makes one route worse, and why /about was deliberately given NO
#             metadata export), and the CS-1 forward note — CS-1 has NOT landed (7 artifacts at
#             schemaVersion 2) and both tripwires stay green, now in TWO places.
#             Suite 577/23 -> 660/24, full chain green. deferred-work.md appended under its own
#             heading with the append-only property PROVEN programmatically (first 189,602 bytes
#             byte-identical, 16,273 appended). PROPOSED AND AWAITING JUAN: about.credits and
#             about.project have no ruled wording in any spine.
#             HISTORY-INTEGRITY DEFECT, DISCLOSED: story 2.18's ENTIRE implementation was committed
#             by the CONCURRENT SESSION under another story's title — commit 54f7093 "Story 2.10 code
#             review: ..." carries all of 2.18's code plus the three untracked 2-11a/b/c story files
#             alongside its own 2.10-review work. This story never ran `git add -A` or `git add app/`;
#             the sweep came from the other direction. Content integrity was VERIFIED (HEAD carries
#             the final state of every 2.18 file, including the Esc-dismissal fix and all three
#             remediated strings), so it is an ATTRIBUTION defect only. Ruled per this repo's own
#             precedent — the 1.14 review's ruling on commit 5344fac — RECORD IT, DO NOT REPAIR
#             HISTORY. A reviewer looking for 2.18's diff must read 54f7093.
# 2026-08-04: story 2-18 code review — 5 decisions ruled, 23 patches applied, 5 deferred,
#             4 dismissed; review -> done. Three adversarial layers over 892766c..HEAD scoped to
#             the story's own File List (the concurrent session's 22 files excluded); 58 raw
#             findings deduped to 37, each re-read against the code before rating.
#             TWO REAL FOCUS DEFECTS IN THE POPOVER, both on the one path the harness cannot test
#             and both invisible to tsc, ESLint and a jsdom-less suite. (1) The hover grace timer
#             read only the two POINTER refs, never focus — so Tab to the trigger, Tab to the link,
#             then let the mouse cross the term and leave, and 180ms later the panel unmounted under
#             the focused link, onCloseAutoFocus took its focus-return branch and the trigger's focus
#             handler RE-OPENED it. (2) focusInPanel was never reset on close: removing a focused
#             node fires no focusout, so after the first Esc-from-inside the flag stayed latched and
#             the NEXT close yanked focus across the page — verbatim the failure onCloseAutoFocus
#             exists to prevent, and the reason Task 11.4(e) passed without covering it. Also fixed:
#             the panel never closed on focus-out, and single-open eviction left pointer refs stale
#             so a popover could stick open with the pointer nowhere near it.
#             A FALSE COPY CLAIM ON THE TERMINOLOGY-GATE STORY ITSELF: glossary.pressing.definition
#             said the report "reparte el tiempo sin balón entre presión alta, media y baja" —
#             contradicting its own sibling glossary.phases-of-play entry, the shipped
#             viz.pressing.note, and the NINE-value outOfPossessionPhase enum. Exactly the defect
#             class this story exists to prevent.
#             THE <p>-><div> DEPARTURE WAS ONE OF THREE SITES: the non-portalled panel also mounts
#             inside <h2> (momentum's heading mark) and inside <span> (the Hero's xG). React's
#             validateDOMNesting warns on p>div but NOT on h2>div or span>div, which is precisely
#             why Task 11.14's zero-console check caught one and was structurally unable to see the
#             other two. Juan ruled: restructure both containers, keep decision 9's no-portal clause
#             (it is what makes the Tab-reachable link possible). The residual accessible-name
#             absorption while a panel is open is FILED, not faked — it follows from the no-portal
#             rule and cannot be restructured away.
#             TASK 8.6 WAS HALF-APPLIED: realigning enums.distributionType left the es technique
#             headings carrying "Técnicas de saque de volea" / "Técnicas de saque con la mano" —
#             the two labels 8.6 replaced BECAUSE they were confusable — printed one line below the
#             corrected family labels. en was consistent; only es was left half-migrated.
#             ALSO: decision 11 violated by the Spanish id `salida` (renamed coming-off-the-line);
#             decision 12's "do not hardcode the count" violated in two comments while the ledger
#             asserted "no count is hardcoded anywhere in this story"; /glossary shipped a metadata
#             export on the exact grounds /about refuses one, minting dead en keys (Juan ruled: drop
#             it, both routes now wait on the same open ruling); the /glossary xG entry rendered
#             "goles esperados" and then told the reader no Spanish form exists; four of the five
#             marks had NO guard that they still resolve (now asserted in both locales against the
#             real registry); findTermSpan had no LEADING word boundary; isAlwaysExpandedSectionId
#             hand-copied a partition the codebase derives; four residual shapes of the ESLint hole
#             closed (quoted object key, ternary-in-object, ternary-on-Label, namespaced element).
#             RECORD REPAIRED: a superseded pause-time Completion Notes block (THREE departures /
#             suite 609) contradicted the final one on the two numbers a reader most needs and was
#             removed; the co-commit disclosure named one co-tenant when the diff carries two — the
#             2.10 review's locale work was in there un-attributed, and the goalkeeping-heading
#             defect above sits exactly on that seam; five differently-discharged subtasks recorded.
#             VERIFIED IN AN ISOLATED WORKTREE at 3d27ed0, and that was necessary rather than
#             fastidious: the concurrent session's in-flight 2.11b files broke the shared tree three
#             times (untracked expert-model.ts, a viz.table.position leaf in es.ts but not en.ts, an
#             undefined ExpertLayer). In isolation the full chain is green — eslint --max-warnings 0,
#             tsc --noEmit, assert:schema-version, next build (all five routes exporting), copy-data
#             — and the suite is 670/24 with 0 failures, with the static-output guards run against a
#             FRESHLY BUILT out/ (four patches change strings those guards assert and one renames a
#             glossary id the anchors are built from, so a stale export would have proved nothing).
# 2026-08-04: story 2-11a implemented and moved to review — the ONE sortable data-table contract.
#             ALL TWENTY INSTANCES ACROSS ALL TEN FILES RETROFITTED AND ALL TEN PRIVATE DataTable
#             COPIES DELETED. Re-counted at Task 1.2 rather than trusted (10 files / 20 instances,
#             exactly the story's table); post-retrofit the same greps return ONE definition (the
#             shared component) and twenty instances, all through @/components/DataTable. Ships
#             lib/table-sort.ts (pure; the only React reference is an erased ReactNode TYPE),
#             components/DataTable.tsx, and ONE SortAnnouncer provider mounted once in
#             MatchBundleRegion — twenty tables would otherwise have minted twenty live regions,
#             each conditionally mounted inside its own disclosure, and a region that mounts
#             already-populated does not announce reliably.
#             REGRESSION PROVEN BY DIFFERENTIAL BUILD, not by inspection: pristine 892766c was built
#             and served alongside the 2.11a build, and a digest over every table's caption, column
#             set, row count and every cell's text in the DEFAULT (unsorted) state is BYTE-IDENTICAL
#             on all three fixtures — m001 22 tables/451 rows/2241e22c, m002 21/364/4f07f977, m074
#             22/600/99508d0d. m002's 21 is a presence gate closing, evaluated identically on both.
#             VERIFIED WITH REAL KEY PRESSES: Enter -> ascending (glyph ▲, "Ordenado por Minuto,
#             ascendente."), Space -> descending (order reversed 66'..3', glyph ▼), Enter -> every
#             <th> back to aria-sort="none" with the artifact order restored BYTE-IDENTICALLY and
#             "Se restauró el orden original de la tabla." Focus held throughout; arrow keys do not
#             hijack (the button carries onClick only — Enter/Space are native button semantics).
#             112 sortable heads all aria-sort="none" ON MOUNT (decision 5: there is no defaultSort,
#             because "none" IS the artifact order and AD-5 reserves that to the artifact); exactly
#             one active at a time; ZERO aria-pressed inside any table; exactly THREE polite live
#             regions page-wide with ZERO inside any table and no role=status/alert; ZERO sticky
#             theads; ZERO animations; no zebra; all 112 header buttons exactly 44px.
#             DECISION 2'S PAYOFF PROVEN LIVE: with a sort active on a DictionaryKey column, the EN
#             toggle RE-SORTS the rows (Al arco/Bloqueado/Desviado/Gol/Incompleto -> Blocked/Goal/
#             Incomplete/Off target/On target), because sort.valueOf returns the CALL-SITE-RESOLVED
#             label rather than the raw key.
#             TWO DECLARED DEPARTURES, both filed. (a) HEADERS ARE NOT STICKY, and the reason is
#             structural: ViewDataDisclosure's region is overflow-x-auto with NO height bound, so
#             CSS forces overflow-y to auto and it is BOTH the sticky ancestor AND a scrollport that
#             never scrolls — position:sticky never offsets. It would have shipped green and
#             silently dead. Routed to 2.11b's bounded container; do NOT close it by giving the
#             shared disclosure a height. (b) DESIGN's data-table.sort-active-color is used on the
#             CANVAS as specified (11.27 dark / 4.99 light) but REJECTED on the pitch, where the
#             light --accent-cyan computes 2.28:1; the pitch uses a theme-invariant lightness step
#             (--ink-on-pitch 11.14 over --ink-on-pitch-secondary 5.55) plus the direction glyph.
#             Contrast method VALIDATED FIRST by reproducing all seven published figures to 2dp.
#             --ink-muted: ZERO uses in any table, both themes.
#             LEDGER: the `?? 0` item is CLOSED (decision 3) and its "cross-map-model.ts does the
#             same" clause CONFIRMED STALE — only ShotLogRow was still wrong; Cross already used
#             ?? null and Defensive was fixed by 2.9's review. Correction APPENDED, original entry
#             untouched; append-only verified programmatically (+5,701 bytes, file starts with the
#             pre-edit bytes exactly). The 2.6/2.7/2.8/2.9 plug-in points are DISCHARGED. Task 8.3's
#             "re-file if 2-10 has not landed" branch is DEAD and recorded as such.
#             COORDINATION — THE HAZARD THE STORY WAS WRITTEN AROUND CAME BACK. A concurrent Story
#             2.18 (glossary) session was writing app/ from Task 7 onward, and its in-flight state
#             DOES NOT TYPECHECK, so `npm run build` in the shared tree fails on ITS work. Rather
#             than touch another session's files or report a red chain as this story's, the full
#             chain was verified in a GIT WORKTREE AT 892766c CARRYING EXACTLY THIS STORY'S 24 FILES
#             — lint, tsc, assert-schema-version, next build (7 pages), copy-data all GREEN. Shared
#             suite 608/24 all green, of which this story contributes 555 -> 586 across 23 files
#             (+22 table-sort, +4 i18n, +4 shot-map, +1 momentum); the other 22 are 2.18's glossary
#             tests. NOTHING IS COMMITTED: locales/{es,en}.ts, i18n.test.ts, deferred-work.md,
#             PhasesSection and PressingSection are genuinely co-mingled with 2.18's and a 2-10
#             review patch, and committing now would capture 2.18's half-finished, non-compiling
#             work. Every shared-file edit was an anchor-phrase INSERT — nothing deleted or
#             rewritten — and `git add -A` / `git add app/` were never used. Staging is Juan's call.
#             COUNT CORRECTION AT HAND-OFF: the twenty retrofitted instances are those that existed
#             at 892766c. A concurrent session has since added FOUR MORE in GoalkeepingSection, so
#             the Task 1.2 grep now returns 24 in the shared tree. All four are NOT this story's and
#             all four consume the shared columns/rows/surface API correctly — the contract being
#             adopted, not a defect. Recorded so a reviewer re-running the grep is not surprised.
# 2026-08-04: story 2-18 context created and moved to ready-for-dev — the TERMINOLOGY GATE, plus
#             /glossary, /about and the project's FIRST WCAG 1.4.13 surface. app/ + locales only.
#             THE AUDIT INVERTED THE STORY'S SHAPE: ten stories shipped ~417 locale leaves under the
#             honour system and NOTHING in the build chain has ever compared a shipped Spanish string
#             to the EXPERIENCE.md policy table. Three rows are VIOLATED — "Córners" (forbidden
#             register, contradicting the SEVEN sibling viz.setPlays keys in the same section),
#             row 38's possession vocabulary (2.10 shipped the form the table logs as REJECTED), and
#             row 21's momentum tooltip, whose ruled text is FACTUALLY FALSE for a series 1.8 proved
#             is final-third entries per minute. Eight rows have no surface yet, so AC 1's
#             "implemented verbatim" is RULED discharged in the GLOSSARY rather than by minting dead
#             viz.*/enums.* keys — es.ts's own docblock reserves those namespaces for PER-SURFACE
#             stories and the 2.9 review already patched five dead keys. The objection that
#             result-letters and the "CARR. ALTA VEL." abbreviation are scaffolding the AC names by
#             category is RECORDED so review can overturn it cheaply.
#             FOUR RULINGS BY JUAN. (1) The /about methodology note ships a TWO-SENTENCE honest form,
#             not the AC's clause: FD-1 records that per-shot xG does not exist in the source at all
#             (team totals only), so "used as-is" alone is true of the totals and misleading about
#             per-shot values — the first time FD-1 becomes user-visible copy, and the reason the
#             uniform marker size stops being unexplained. (2) step-in -> irrupción is FINAL:
#             `stepIns` is a REQUIRED member of PlayerInPossession printed as "Step Ins" between
#             "Take Ons" and "Attempts at Goal", so "salto" is affirmatively contradicted and
#             "conducción interior" invents a direction the PMSR never prints. The PMSR has NO
#             glossary page — 0 hits across all 52 pages — so "verified against the PMSR definitions"
#             means page placement and reconciliation arithmetic, and the story says so rather than
#             pretending. (3) All four remediation items taken. (4) 2.18 TAKES THE PER-SECTION ERROR
#             BOUNDARY — the whole-layer blast radius is now filed FIVE times (2.8, 2.6, 2.9, 1.14,
#             2.10) and routed every time to "whichever story next touches TacticalSection", which
#             widening `title` makes this story.
#             VALIDATED BY THREE FRESH-CONTEXT SUBAGENTS, and the pass changed the build four times
#             at decision level. (a) THE RADIX CONFIGURATION IN THE FIRST DRAFT SHIPPED AC 2 BROKEN:
#             traced through react-popover and react-focus-scope, a PORTALLED non-modal popover with
#             onOpenAutoFocus prevented puts the panel at the end of <body>, so Tab from an inline
#             trigger never reaches the "Ver en el glosario" link — and that same Tab fires a focusin
#             outside the layer, which DismissableLayer treats as a DISMISSAL. The Tab meant to reach
#             the link closes the popover. onCloseAutoFocus was unspecified and its default calls
#             triggerRef.focus() on EVERY close, so a hover-opened popover timing out would steal a
#             keyboard user's focus. Now: no portal, onCloseAutoFocus pinned, the trigger's
#             unconditional click toggle intercepted, and Popover.Title asChild (a plain span gives
#             role="dialog" with NO accessible name; the default emits an <h2> into a match route).
#             (b) DECISION 6's "mechanically safe" WAS FALSE FOR NINE OF ELEVEN SECTIONS: {title}
#             renders inside the accordion <button> for every collapsible section and nine of eleven
#             are collapsible AT EVERY WIDTH, so marking those headings would nest a focusable
#             popover trigger inside a button and a click on the term would toggle the section.
#             Ruled: heading-mark only key-stats and momentum (the two never-collapsible, and the
#             ledger's own named case), summary-mark the other nine. That partition makes "once per
#             section" true BY CONSTRUCTION, so the marking context the draft invented was deleted
#             along with its new-Context exemption. (c) ROW 38 IS NOT SIX IDENTICAL LEAVES: only two
#             carry the bare "Con balón"/"Sin balón"; the other four are compound metric labels, and
#             the drafted "repoint the six" would have turned "Altura de la línea con balón" into
#             "En posesión". The six exact replacements are now written out and en.ts needs no edit.
#             (d) THREE TASKS WOULD HAVE BROKEN THE BUILD: adding `value` to the shared gated-prop
#             regex fails on SiteHeader's two ToggleGroupItem state tokens; the drafted
#             enums.metric/KEY_STAT_UNIT route for the AD-7 unit fix turns i18n.test.ts red
#             (topSpeed is not in KEY_STAT_FIELDS) and needs a do-not-touch file; and both
#             viz.momentum copy holes need two do-not-touch components, so that task became a filing.
#             Also corrected: fetchArtifact DOES have callers and is the runtime path (the draft
#             claimed the opposite); the two flagship ledger grep anchors returned ZERO hits because
#             the file hard-wraps mid-phrase; a puerta is not zero (it hides inside "en propia
#             puerta", so the new register sweep is red until the fix lands); and the
#             matches/static-output.test.ts remedy the draft proposed was already shipped. Nine
#             user-visible sentences the draft left to the dev are now ruled verbatim in both
#             locales. RECORDED OBJECTION, unresolved: one reviewer rated the error boundary and the
#             ESLint gate closure hidden scope no AC requires and recommended cutting both — kept on
#             Juan's ruling, with a live boundary proof as the mitigation.
#             COORDINATION CHANGED DURING CREATION: baseline moved 163fa20 -> 892766c because STORY
#             2.10 LANDED MID-WRITE, so the tree is clean and every "expect to rebase onto 2.10"
#             clause was removed — but a 2.11 session appeared with THREE untracked context files
#             (2.11 splitting into 2-11a/2-11b) whose first app/ targets will be the same three files
#             this story rewrites more of than any story so far. Append-only discipline stands and an
#             explicit ABORT RULE was added: if 2.11 starts writing the locale files, stop before the
#             remediation task, which changes existing values rather than appending.
# 2026-08-04: epic story 2.11 SPLIT THREE WAYS at story creation, ruled by Juan; 2-11a created and
#             moved to ready-for-dev. epics.md is NOT edited — AC 2 becomes 2.11a, AC 1's per-player
#             half plus AC 3 become 2.11b, AC 1's log clause becomes 2.11c. Reason: the combined
#             story measured ~30 files, ~200 new locale strings and TWENTY table retrofits in one
#             commit, against FIVE files a concurrent 2-10 session is actively editing, in a harness
#             with NO jsdom where nothing rendered can be unit-tested. Two independent validators
#             called it three stories.
#             THE SHELL DOES NOT EXIST, which the epic's AC assumes it does. AC 1 opens "Given the
#             Expert Layer shell (#expert, EXPERTO/EXPERT pill)" — there is no ExpertLayer, no
#             #expert section, no pill, ZERO `experto` keys in either locale, and
#             tactical-sections.test.ts actively asserts sectionDataState THROWS on "expert". 2.11b
#             builds the third layer of the Match Dashboard from nothing; it is a SIBLING of
#             TacticalLayer, never a twelfth SectionId.
#             THE RETROFIT SURFACE IS TEN FILES / TWENTY TABLES, not the three the brief named and
#             not the nine a first draft counted (its own enumeration summed to ten). Six committed
#             files carry ten instances; 2-10 is AUTHORING FOUR MORE private DataTable copies with
#             ten more instances, and its story file lists "the private DataTable copies" under Do
#             not "fix" — so the debt this story exists to retire is growing while it is written.
#             2.11a re-counts at Task 1.2 and re-files whatever it cannot reach.
#             TWO OF THE FIVE LEDGER-ROUTED ITEMS CAME BACK WRONG when checked against code rather
#             than trusted. (a) CrossLogRow ALREADY uses `?? null`; DefensiveLogRow was fixed by
#             2.9's review with a docblock naming 2.11 as owner — only ShotLogRow still carries the
#             `?? 0`, so the ledger's "cross-map-model.ts does the same" is stale. (b) The defensive
#             log's absent columns are REMOVED, not em dashes: 2.9 extended the FD-1 gate to all
#             three of anyContestType / anyPlayerName / anyMinute.
#             THE RECEIVING LOG IS NOT "UNBUILDABLE" AND THAT WORD IS RETIRED. The fixtures ship 270
#             receiving events with ALL EIGHT fields non-null on 270/270 rows, so all four of
#             EXPERIENCE.md:221's columns have a source in the data the app renders TODAY. True of
#             the corpus, false of the build. Juan RULED the symmetric answer the first draft missed:
#             build it behind `anyReceivingEvents`, exactly as showXg and the three defensive gates
#             work — renders on fixtures, self-removes on corpus, AC satisfied by construction with
#             no re-scope and no waiver. (1.13 enumerates SEVEN unfulfillable fields, not the
#             ledger's eight; teamId is derivable.)
#             THE STICKY-HEADER AC IS UNIMPLEMENTABLE WHERE IT WAS ASKED FOR, and would have shipped
#             green and silently dead. ViewDataDisclosure's region is `overflow-x-auto` with NO
#             height bound; CSS forces overflow-y to auto, so it is the sticky ancestor AND never
#             scrolls vertically — position:sticky never offsets. 2.11a ships non-sticky headers with
#             the departure FILED; 2.11b introduces a bounded container where they work. Related and
#             measured: DESIGN's data-table.header-background token is a 1.00:1 NO-OP, because
#             --surface-raised and --card are BYTE-IDENTICAL in both themes.
#             ONE MORE RULING AND ONE WITHDRAWN. Juan ruled the Expert Layer gets its OWN sibling
#             error boundary (additive, discharging half the carry-forward blast radius) — but the
#             shipped boundary's copy says "el análisis táctico", a FALSE statement over an Expert
#             crash, so 2.11b parameterises it with defaults that keep the Tactical mount
#             byte-identical. Withdrawn: a drafted "declared departure" to active-locale collation —
#             compareText's default IS 'es', so calling it with two arguments satisfies UX-DR12
#             verbatim and no departure was ever needed. Measured: es and en orders are IDENTICAL on
#             all 96 fixture names (0/9,216 pairs), so the choice is provable only in a unit test.
#             ALSO CORRECTED AT VALIDATION: a --viz-team-a light contrast figure (7.08 was
#             --accent-lime; the real value is 4.99), the test baseline (462/19 committed), the
#             plug-in-point census (4 + 3 = 7 across FOUR filing stories), a `grep expert` premise
#             that would have tripped a stop-work gate, and a Task-8.6 delete recipe that would have
#             stripped live ShotOutcomeDetail docblocks belonging to 2.13/2.18. Three misquotes fixed.
#             FILED, NOT FIXED: the three fixtures violate the shipped `domain-g-zone-sum` pipeline
#             check on 79 of 96 player rows (worst 4.400 m) against a corpus-verified worst of
#             0.200 m over 3,289 rows — the fixtures are not corpus-faithful for Domain G physical
#             data. Also filed: the mobile mockup carries a complete Expert Layer block (pill CSS,
#             ruled <h2> copy, hairline separator) that no draft had consulted.
#             VALIDATED BY THREE FRESH-CONTEXT SUBAGENTS before ready-for-dev.
# 2026-08-03: story 2-10 context created and moved to ready-for-dev — the LAST four Tactical
#             sections (#phases, #pressing, #set-plays, #goalkeeping) and the four remaining
#             PendingSectionPanel fall-throughs, so 2.10 CLOSES the Tactical Layer at eleven of
#             eleven. app/ ONLY; no predicate change is needed or authorised in
#             tactical-sections.ts, which already answers all four ids correctly.
#             THE EPIC'S #goalkeeping AC IS OVERTURNED, and by MORE than AD-14 (d) recorded. Beyond
#             the per-keeper/per-team mismatch 1.9 proved, FIVE contract-REQUIRED sub-blocks are
#             null on 208/208 team-innings — distribution.{feet,hands,throw}Techniques,
#             goalPrevention.byBodyType, aerialControl.crossesFacedCompleted — while the fixtures
#             populate all five (data/fixtures/README.md files Domain E goalkeeping as synthetic
#             "in full"). Juan RULED: team-grouped blocks with the keeper list as CONTEXT, both
#             names on the 7 two-keeper innings with neither record summed (AD-5), and the five
#             dead panels behind PRESENCE GATES on the shipped FD-1 showXg / 2.9 anyContestType
#             precedent — plus one ruled sentence when a gate closes, because silent panel-level
#             absence is what FR-22 exists to forbid.
#             A SECOND CONTRACT GAP NOBODY HAD FILED: GoalkeeperInvolvementSample.minute is a bare
#             Minute (0-120, no stoppage field), but the corpus draws 95-145 slots per team-inning
#             with 2,506 of 21,764 in stoppage — so minutes are NOT unique on real data. Exactly
#             what 1.8's schemaVersion 2 bump fixed for MomentumSample.at and what invalidated
#             2.6's slider AC. Ruled: index by SAMPLE, never by minute (2.6 decision 1 repeated);
#             filed as a BLOCKER for 2.19's cutover, not an open note.
#             THE FIXTURES LIE IN THREE SEPARATE DIRECTIONS, all measured: free-kick nesting
#             (direct == on+off) is corpus-FALSE on 208/208 yet fixture-TRUE on 3/3, because the
#             fixture README says the splits were "synthesised so that they add up to those
#             totals"; corner delivery STYLE is a partition on only 96/208 (112 under, 0 over);
#             and every goalkeeping timeline is a 5-minute bucket series summing exactly to its
#             total, against a corpus delta of 0-5 that is exact on only 59/208. Corner delivery
#             TYPE (208/208), corner SIDE (208/208, and read from the PRECOMPUTED cornersBySide per
#             README section 14) and direct+indirect==totalFreeKicks (208/208) are the relations
#             that DO hold. Juan ruled: use the true ones, bar the false ones, and drop the
#             indentation that would smuggle the banned claim back in visually.
#             Domain C: the 8+9 phases and defensiveBlockDistribution are REAL (they match the
#             staged records exactly); lineHeight/teamLength are the ONLY synthetic part — the
#             corpus prints THREE panels per possession state with three measures each including an
#             unmodelled team_width, and m001's fixture 44.4 matches no panel and no mean. Juan
#             ruled: render them, invent no aggregation, file the gap to 1.16. Phases sums run
#             84-149 in possession and 73-97 out, so NOTHING may normalize or pie them.
#             AMENDED AFTER VALIDATION: #pressing takes the four press rates as well as the blocks
#             and the metres. A blocks-only #pressing would have shipped false copy on its own
#             frozen summary ("...e intensidad de la presion."), which is also the <lg
#             collapsed-shell line a phone reader reaches first.
#             VALIDATED BY THREE FRESH-CONTEXT SUBAGENTS before ready-for-dev: ~180 factual claims
#             audited (3 corrected, 8 tightened), 17 implementability gaps closed of which 4 were
#             build-breaking, and 11 adversarial findings ruled. COORDINATION CHANGED MID-CREATION:
#             a 2.9 CODE-REVIEW session began writing app/ (nine files, including both locale files
#             and i18n.test.ts), so `git add app/` is now unsafe and the story says so.
# 2026-08-03: story 2-9 implemented and moved to review — and the story's premise overturn HELD, so
#             Epic 2 shipped its FIRST in-story re-scope: ONE pitch map (#defensive-actions) plus TWO
#             AGGREGATE CARD SURFACES (#offers-to-receive, #movement-to-receive), not three maps.
#             `events.receiving` is read by nothing in app/ any more; both receiving predicates in
#             tactical-sections.ts now read `bundle.players` (the one ruled exception to that file's
#             do-not-touch), and their empty-state copy is overridden in BOTH halves because a Domain
#             G absence is not a receiving-section absence — shipping "El informe oficial no incluye
#             esta seccion." there would have been a false statement.
#             RE-DERIVED BEFORE BUILDING, exactly as the story demands: sum(offersByMovementType) ==
#             totalOffers on 96/96 fixture player rows, 0 mismatches, shipped as a test with the row
#             count asserted — so the proportion bar states something true, unlike 1.13's by_phase
#             totals and Domain C's "never normalize" phases. The AD-5 roll-up reproduces the source
#             page's own headline (MEX 390/196/50.26%, RSA 396/162/40.91%) and its docblock carries
#             the DECLARED reading of "exactly one surface" plus the binding to DELETE the derivation
#             if 1.16 ever emits a team-level offers block.
#             HEATMAP: DEFERRED with the rationale logged (AC 2's second branch), routed to 1.16 BY
#             NAME. Evaluated not skipped — the ramp's measured luminances and pitch ratios reproduce
#             DESIGN.md:286 exactly and it has zero consumers; what is missing is an INPUT, not a
#             ramp. /contract untouched, no AD-14 change request filed.
#             VERIFIED LIVE, NOT ASSERTED. Triangle renders at exactly 13.86 x 12.00 px with vertices
#             (0,-8) (6.928,4) (-6.928,4) — decision 7's ruled numbers, measured in the browser beside
#             a 10x10 hollow circle and an 11x11 square. Contrast method validated FIRST by
#             reproducing the published 9.56 / 7.26 on-pitch figures exactly, then measured on the
#             card: team accents 13.56/10.30 dark and 4.99/5.36 light, bar hairline separators
#             10.31/7.84 dark and 3.79/4.07 light, canvas "Ver los datos" 17.30/16.44 — every value
#             clears its floor in both themes. Real key presses: Home lands on minute 2', Enter opens
#             the 15-item cluster dialog focused on that marker's own item, arrows rove, Escape
#             returns focus — and ZERO aria-pressed anywhere, confirming decision 18. EN toggle and
#             theme toggle after load clean on all three fixtures, 0 console messages.
#             THE 320px OVERFLOW IS PROVEN PRE-EXISTING rather than assumed: the two offending
#             elements are Key Statistics' tile, OUTSIDE all three new sections, and none of the three
#             appears in the offender list at 320 or 390. At 390 scrollWidth == clientWidth exactly.
#             <md confirmed in a same-origin iframe: both card sections render two figures with ZERO
#             team tabs (decision 17's declared departure from EXPERIENCE.md:130), while
#             #defensive-actions keeps PitchPanel's shipped tabs untouched.
#             MEASURED FINDING WORTH THE LEDGER: the dialog path DOMINATES at every shipped width
#             below 1920 (singleton share 0-42% at 320/386), the >=44px hit floor is never breached
#             (~59-131px across), and at CORPUS density (153 markers, vs the fixtures' 30-59) the
#             whole figure collapses to ONE cluster at 320px with zero singletons at any width — so
#             on a phone with real data the map is effectively a 153-item dialog. Filed for 2.19.
#             Suite 364/16 -> 439/19, full chain green. NINE ledger entries, every one APPENDED and
#             the append-only property verified programmatically. Task 8.7's "correct in place" was
#             served by an appended correction rather than rewriting another story's paragraph.
#             Staged exactly app/ + this story + deferred-work.md + sprint-status.yaml; pipeline/**
#             never touched, so no in-flight 1.9 / 1.15 PIPELINE lines are carried.
# 2026-08-03: CODE REVIEW CORRECTION to the two lines above. The original claim ("there is no
#             co-commit") was FALSE and contradicted story 2-9's own Completion Notes, which
#             disclose that the commit DOES carry other sessions' lines in the two SHARED
#             ARTIFACTS. It carries ~164 lines of story 1-15's status note here, plus six
#             deferred-work.md entries belonging to 1-15's review. No pipeline/** file is in the
#             commit, which is the narrower true statement. Corrected so a reader of the board and
#             a reader of the story file get the same fact.
# 2026-08-03: CODE REVIEW CORRECTION — story 1-9 was flipped to `done` by story 2-9's commit. That
#             was wrong twice over: it is not 2-9's status to set, and 1-9 is NOT done — its own
#             last commit is titled "Story 1.9: Domains E & F code-review patches (IN PROGRESS —
#             Decision 3 open)". Restored to `in-progress`, which is what 2-9's Completion Notes
#             said the flip had been.
# 2026-08-03: story 2-9 context created and moved to ready-for-dev — and the story-creation probe
#             OVERTURNED THE EPIC'S PREMISE, ruled by Juan as an IN-STORY re-scope on the 1.9/1.13
#             precedent (the first time that precedent is applied inside Epic 2). The epic asks for
#             THREE marker maps; the corpus supports ONE. `ReceivingEvent` is unfulfillable in all
#             EIGHT required fields (1.13, 104 reports / 416 pages), so `events.receiving` can only
#             ever be null and #offers-to-receive / #movement-to-receive have no event data at all.
#             THE FIND THAT MADE THE RE-SCOPE BUILDABLE TODAY, and which the brief's option space
#             did not contain: the receiving surfaces do NOT need 1.13's staged `domains.receiving`
#             aggregates (which are gitignored under work/, have NO contracted destination, and are
#             read by nothing in app/). They read `bundle.players[].inPossession` — `totalOffers`,
#             `offersReceived` and the six-value `offersByMovementType` split — all three `required`
#             in the contract, present in all three fixtures, and extracted for real by Story 1.10
#             (done). Zero contract change, zero pipeline dependency, and unlike events.receiving it
#             survives the 2.19 cutover as REAL data.
#             MEASURED, NOT ASSERTED, and re-measured independently by a fresh-context validator:
#             `sum(offers_by_movement_type) == total_offers` on 3,289/3,289 staged player rows with
#             a delta histogram of exactly {0: 3289} — so a proportion bar states something true,
#             unlike 1.13's by_phase totals (-48..+314) and Domain C's "never normalize" phases.
#             And the AD-5 roll-up is validated rather than merely legal: per-team offers made
#             69/333/634 and received 20/119.5/247 reproduce `domains.receiving.offers.*` — the
#             receiving page's OWN PRINTED HEADLINE — with 0 mismatches and max absolute difference
#             0, record by record over all 208 team-innings.
#             HEATMAP: DEFERRED with the rationale logged (AC 2's second branch), ruled by Juan.
#             Evaluated, not skipped: --heat-1..5 already ship theme-invariant and on-pitch with
#             ZERO consumers, and the measured luminances (0.2668->0.8857, strictly monotonic) and
#             pitch ratios (3.68/4.83/5.95/8.08/10.87) reproduce DESIGN.md:286 EXACTLY. What is
#             missing is an INPUT, not a ramp — every candidate Domain D family is under an
#             unresolved AD-14 emission blocker, so a heatmap built now would bin synthetic fixture
#             coordinates and have no real input at cutover. Routed to 1.16 by name.
#             THE FIXTURES ACTIVELY CONTRADICT THE CORPUS and the story is written to stop the dev
#             building to them: they populate `at` and `playerName` on 100% of defensive rows and
#             make block+possession-contest 44/47/55% of markers, while the corpus has contest_type
#             null on 20,169/20,169, no player/clock carrier at all, and only TWO of four action
#             types with any coordinates. They also under-state density 2-3x (fixtures 30-59 per
#             figure; corpus min 62 / median 97 / max 153).
#             VALIDATION PASS (2 fresh-context agents) found 16 must-fix gaps and 14 numeric or
#             citation defects in the first draft, all fixed. Headline: (a) the mirrored goal
#             furniture — routed to 2.9 BY NAME twice in the ledger — was drafted as an
#             implementation task that CANNOT be executed inside the story's own scope boundary
#             (pitchMarkings returns a flat record rendered BY NAME in PitchDrawing, so it needs
#             PitchMarkings interface fields AND new PitchPanel elements, plus two non-projective
#             steps); it is now RULED here ("yes, mirror") and its implementation routed onward,
#             which is what both ledger entries actually ask for. (b) Decisions 5/6/8 contradicted:
#             one shape + team-only colour makes the two plottable action types VISUALLY IDENTICAL,
#             so the drafted per-type "derived legend" would have claimed a distinction the map does
#             not draw — the legend is now one entry per team. (c) The triangle constant was wrong
#             AND would have failed the story's own test (1.5570 vs 1.55512), and equal-area sizing
#             yields a 16.16px glyph outside DESIGN's 8-14px band; re-ruled to R = 4r/3 (13.86 x
#             12.00, height == the circle's diameter). Also fixed: the two receiving sections would
#             have shipped the generic empty-state sentence "El informe oficial no incluye esta
#             sección" over a Domain G absence (a false statement, and the mirror of the FR-22
#             inversion the predicate change exists to prevent); no role="figure" for the two
#             non-PitchPanel surfaces; data tables that did not carry the numbers the tiles print;
#             an unruled six-category palette that does not exist in DESIGN; the camelCase-vs-kebab
#             enum mapping; and a constructed test event that is not type-constructible without an
#             authorised `as unknown as` cast.
#             Touches app/ + locales + the ledger only — coordinates around in-flight 1-9 and 1-15,
#             which hold pipeline/ dirty; every shared-artifact edit APPEND-ONLY; never
#             `git add -A`. Baseline dd3dfc3, app/ clean, suite 364 passed / 16 files.
# 2026-08-03: story 1-15 CODE REVIEW — 3 decisions ruled, 34 patches applied, status done.
#             DECISION 1: the AD-3 first-seen tiebreak was claimed by four documents and two
#             test NAMES and implemented by none of them — the code raises IdentityCollisionError,
#             which is what AC 1's binding block demands while AC 4 rows 2-3 and Task 3.4 demand
#             the tiebreak. Ruled in favour of RAISING (a tiebreak on a corpus with 0 such
#             collisions can only fire on a defect, and silently minting two ids from one printed
#             name is unfalsifiable downstream); the docstrings, README and test names now say so.
#             DECISION 2: commit 32fc131 flipped story 1-9 from in-progress to REVIEW while 1.9's
#             implementation (domain_e.py +602) was still uncommitted in the working tree — the
#             commit message's COMMIT SCOPE note declared the checks.py/README seam but not this.
#             Reverted to in-progress; the board describes committed reality.
#             DECISION 3: precompute over a partial corpus died with a wall of group-arithmetic
#             errors. Ruled: refuse subset runs early and plainly — matchdayRound is required by
#             both bundle schemas, so staging a spine without it only moves the failure into 1.16.
#             Highest-severity patch: identity-pinning re-minted from the PDF and never consulted
#             OVERRIDES, while resolve_players applies OVERRIDES BEFORE pinning — so the very
#             first override (the story's advertised data-only fix for 219 as-listed players)
#             would have reported a count-mismatch on every sampled report against a correct
#             registry. Also: OVERRIDES values are now gated against PlayerId; --write-registry
#             refuses to DROP a pin; an empty manifest is a finding, not a PASS; the exit-code
#             contract now distinguishes findings (1) from a broken harness (2) in both
#             directions; the spine's team-name exhaustiveness no longer skips every key but
#             home/away; write_spine removes stale match files; write_registry is atomic. New
#             test modules test_precompute_run.py and test_checks_identity.py close the two zero-
#             coverage gaps (the CLI, and both FR-15 gate checks), and the committed registry is
#             now pinned by a regenerate-and-compare against its real bytes rather than a
#             one-match synthetic corpus.
# 2026-08-03: story 1-15 implemented and moved to review — pipeline/precompute/, AD-9's SECOND
#             phase and the FIRST module in the pipeline that reads NO PDF at all. The story's
#             premise inversion HELD and was re-derived independently rather than copied forward:
#             all three of OQ-4's named ambiguous cases are corpus-EMPTY (0 non-ASCII characters in
#             1,247 distinct player names — the full inventory is A-Z, a-z, space, 55 hyphens and 5
#             periods in ONE name, no apostrophe; 0 normalized name+team collisions; 0 players
#             wearing >1 shirt, distribution exactly {1: 1248}), so (team_id, shirt_number) is by
#             itself a globally unique key and the AD-3 first-seen tiebreak is DEAD CODE on this
#             corpus — implemented anyway, reached only by constructed tests, with no fixture
#             pretending it is corpus-real.
#             THE ACCEPTANCE CHECK PASSES: the ruled CAPS-RUN rule reproduces 155/155 committed
#             fixture player ids with 0 mismatches. The story's warning about the walk was hit
#             LIVE and is worth repeating: keying on objects carrying both playerId AND playerName
#             finds only 96 ids, because fixture LINEUP entries key the name as `name` — walking
#             metadata.lineups.* as well yields 155, of which 59 are unreachable from players[],
#             including romero-gamarra-alejandro-par which exists in exactly ONE substitutes list.
#             The discrimination claim was re-verified: the rejected LAST-TOKEN rule also yields
#             1,248 collision-free slugs, differs on 1,009 entries, inverts 26/26 Korea Republic
#             players and fails the fixtures on 27 ids — while TRAILING-CAPS differs on 0/5,392 and
#             is useless as a mutation target. So collision count cannot discriminate and only the
#             fixture reproduction can, exactly as the story rules.
#             SHIPPED: 1,400 pinned ids in a committed PYTHON registry (48 team codes, 48 teams,
#             104 matches, 1,248 players; OVERRIDES ships EMPTY). Python because code_version()
#             fingerprints pipeline/**/*.py — proven live: committing it moved code_version
#             f417cd0d -> e0296b63 and invalidated all 104 records, which is why the re-extract is
#             ordered AFTER the registry. Two-source pinning, because data/matches/ DOES NOT EXIST:
#             the run PRINTS 'committed /data baseline unavailable ... This is NOT a pass' and a
#             test asserts that branch does not report success, plus a test that goes RED by design
#             when 1.16 lands. The spine ADDS ids and never removes names (playerName is required
#             in eight $defs): 73,065 ids added across EXACTLY 25 name paths, matching the
#             independently measured 73,065 name occurrences one for one. Its exhaustiveness check
#             is the INVERSE of a path list and is pinned NON-VACUOUS by a test that plants an
#             unresolved name — an exhaustiveness check that has never failed is not evidence.
#             THREE DEV NOTES FIGURES CORRECTED, all filed: (a) the 'at least eight non-derivable
#             team codes' count is SIX by that test, and the RSA/COD parentheticals are factually
#             wrong (South Africa DOES contain an r) — the ruling survives over-determined, since
#             no first-three-letters rule yields rsa (sou) or cod (con) either; (b) the 416 display
#             names require counting the top-level metadata block (208 in domains + 208 there);
#             (c) the metadata block carries NEITHER report_id NOR source_path, contrary to the
#             Dev Notes, and it is on the matchday-round critical path. Also filed: the 219
#             as-listed players / 856 entries are a PER-TEAM printing convention, not scatter —
#             eight teams print all 26 of their squad in caps and contribute 208 of the 219 — so
#             Juan can rule per TEAM rather than per player, and 20 mononyms need no ruling at all.
#             VERIFIED: batch 104 extracted / 0 failed / 0 gaps / 0 orphans with exactly the two
#             ledgered self-validation failures (M19, M58), exit 1 by design; precompute twice with
#             the spine 105/105 BYTE-IDENTICAL and the registry byte-identical on regeneration;
#             gate PASS twice, 0 deviations, checks_run 27 -> 29 with both ids in sorted position,
#             the two manifests differing in run_timestamp ALONE. Full suite 1,258 -> 1,330
#             collected, FULLY GREEN at 1,329 passed / 1 skipped / 0 failed. The only forced repair
#             was the one the story named (test_runner's exact checks_run literal).
#             AC 2's 'and aggregates' clause is DEFERRED to 1.17 as a ruling, not an omission.
#             COORDINATION: implemented in a shared dirty tree with 1.9 and 1.14 in flight; every
#             shared-file edit is append-only, /contract, app/, spike/, data/ and every other
#             pipeline subpackage are import-only, and never . The pre-change suite
#             baseline was RETIRED after 2h30m/~46min CPU because at least three OTHER pytest runs
#             were active in this tree concurrently; the pre-change count is established as 1,258
#             by collection instead, matching the story's own recorded figure.
# 2026-08-03: story 1-14 adversarial code review (3 layers over c645cfe..5344fac, 15 files /
#             ~4,225 lines): 32 raw findings deduped to 22 — 2 decisions ruled, 19 patches
#             applied, 2 deferred, 12 dismissed. Status review -> done. All three acceptance
#             runs REPEATED after the patches (they had to be: the specifics patch changes
#             every staged record's content and every pipeline/*.py edit moves code_version):
#             batch 104 extracted / 0 failed / exactly 2 SV failures (M19, M58 — the ledgered
#             pair, NO third, and ZERO pass-network failures), re-run 104/104
#             skipped-unchanged, gate PASS 0 deviations with both new ids in checks_run.
#             647 tests re-run green across every module the patches can reach.
#             THE HEADLINE IS THAT THE PARSER IS SOUND AND THE EVIDENCE FOR IT WAS NOT.
#             Three layers hunted a column-assignment defect and none found one — the header
#             grid, the census, row-order==column-order and the two-way lineup join are all
#             real and all reachable. But NO SHIPPED CHECK CAN DETECT A COLUMN SLIP: all three
#             self-validation checks are invariant under any permutation of the matrix's
#             off-diagonal cells (top5 compares the SORTED multiset of volumes, total-bound
#             their sum, row-bound per-row sums), and pipeline/README.md claimed the printed
#             Top-5 panel validated "the matrix total — and therefore every cell". It does
#             not; the claim is corrected and the real guards are named in its place. Task
#             1.4's actual proof — the independent prototype agreeing on 52,103 cells — lived
#             ONLY in an uncommitted scratchpad script, so nothing in the repo could reproduce
#             it. It is now a committed test that re-reads BOTH spike/mex_rsa.pdf matrices by
#             a decomposition the parser does not share (get_text("words") grouped by y and
#             sorted by x, blank inserted at the diagonal) and compares all 496 off-diagonal
#             cells; the method was validated by reproducing the already-pinned RANGEL_ROW
#             exactly before being trusted. Ground truth went from 1 row of 16 with no away
#             cell to both matrices in full, plus two transcribed anchor rows.
#             THE FIXTURE WAS DEGENERATE IN THE ONE WAY ITS OWN DOCSTRING FORBIDS:
#             _pass_network_base collapsed to (3i)%3 == 0 for every i at j==0, so column 0
#             summed to EXACTLY ZERO at every size — the first player's passes_received was
#             always 0 and the column was indistinguishable from a parser writing zeros into
#             it. Worse, the "column sums fall on both sides of offers_received" property that
#             test_..._refuted_relations_false_by_construction asserts was resting on that
#             same defect. Now i*4 (column 0 reads i%3) with ONE deliberately damped, non-zero
#             quiet column, asserted in the factory. The first fix was itself wrong and the
#             suite caught it: picking size-1 as the quiet column collides with a Top-5 spike
#             at size 10 ((2k+1)%10 == 9), which broke a Domain A report test; it now picks
#             the rightmost column the spikes do not occupy, verified at every size 6..17.
#             TWO DECISIONS RULED BY JUAN. (1) Commit 5344fac DOES NOT IMPORT — it captured
#             the caller half of an in-flight Story 1.9 patch (extract_report.py:57,337 call
#             domain_e_goalkeeper_warnings, whose definition lands two commits later at
#             7f9b959), so any checkout of or bisect through it fails at collection; it also
#             carries a non-additive checks.py behaviour change plus conftest and E/F-test
#             edits, none disclosed against Task 9.6. Resolved at HEAD, so it is a
#             history-integrity defect only. RULED: record it, do not repair history.
#             (2) pass-network-row-bound failing when a matrix player has no Domain G row is
#             INTENDED — a genuine cross-domain inconsistency worth failing on — so the
#             double-attribution finding was dismissed rather than patched.
#             Other substantive patches: both BOUND checks discarded their delta census on
#             FAILING reports, the exact absorption Tasks 4.3/4.4 forbid (and for total-bound
#             one side's failure discarded the other side's passing delta); sibling payloads
#             were read with bare subscripting, so a Domain G or B shape change raised an
#             UNTYPED KeyError — the same defect 1.9's review patched out of _goalkeepers —
#             now routed through a _side_blocks reader that omits the check instead; the
#             half-ulp comparison gained a 1e-9 epsilon because the sign of the last-bit error
#             is an accident of the operands and a correctly-printed page can land at
#             0.050000000000000044; and Task 2.12's tripwire accepted a non-raising empty
#             return where the task says detect_pitch_frames is REQUIRED to raise — an
#             UNDECLARED LOOSENING, now the raise itself, alongside a fourth undeclared
#             deviation (Task 2.6's y-6..y+12 row-label band was never implemented; the
#             shipped header-bucket form is better and is left, but is now declared).
#             The tolerance test did not exercise the tolerance: it moved the printed value
#             TOWARDS the computed one (delta 0.0071, not 0.05), leaving the constant free
#             anywhere in ~[0.043, 7.85]; it now pins both edges to within 0.0001. Eight typed
#             raises had NO test, including the story's own declared header-contiguity
#             departure. The tail-pinning anti-pattern this very commit repaired in
#             test_extract_report_domains_ef.py was reintroduced in its own tests (ids[-3:],
#             warnings[-1]) and is now index-located. The do-not-ship test was a substring
#             filter that a future `pass-network-received-bound` would have passed green.
#             Two items deferred: _parse_rows silently skipping a body row with neither shirt
#             nor name span, and _filled_bezier_rects being narrower than the no-coordinates
#             negative the whole AD-14 filing rests on.
#             NOTE FOR 1.15: the gate's checks_run now carries identity-completeness and
#             identity-pinning, registered by a concurrent session mid-review — test_runner.py
#             pins that list exactly and owes the same forced repair 1.14 made.
# 2026-08-03: story 1-9 Decision 3 code review (3 layers over the 1.9-scoped working-tree
#             diff): 35 raw findings merged to 21 — 3 decisions ruled, 14 patches applied,
#             1 deferred, 6 dismissed. Status -> DONE. (Housekeeping: the development_status
#             field still read `in-progress` while the note below already said `review`, an
#             inconsistency left by the concurrent 1.14/1.15 commits; corrected here.)
#             THE SUBSTANCE HELD UNDER INDEPENDENT RE-DERIVATION. The Acceptance Auditor
#             wrote its OWN character-level tick reader and swept all 104 PDFs with no
#             sampling: every clock relation reproduced with 0 deviations (208/208,
#             1,872/1,872, 110/110, 122/122, 214/214, 18/18, 4/4), zero `FT` ticks, worst
#             residual 0.000529 slots, the `45HT` merged span on exactly the 4 named charts,
#             M88's 14-slot ET1 exact — and replaying the shipped parser against those
#             independent readings derived clean on 208/208 and matched all 208 staged
#             records with 0 mismatches. Task 3.11 is genuinely satisfied. It also RETIRED
#             the story's own chunked-run caveat: the three files ran in ONE invocation,
#             122 passed / 0 failed / 0 skipped.
#             THE HEADLINE DEFECT WAS A TEST GAP THAT HID TWO REAL ONES. Task 8.3 requires
#             pass AND fail branch tests per recorded check; `goalkeeping-involvement-clock`
#             had only pass assertions, because all 16 clock tests assert
#             `InvolvementClockError` at EXTRACTION time — a path that never reaches
#             `domain_e_checks`. Both behavioural defects found lived in those uncovered
#             clauses: (a) `final_minute` was unconditionally 120 whenever extra time
#             existed, so a short ET2 FAILED the check while the identically short ET1 that
#             the comment four lines above exists to permit PASSED — the same page shape
#             judged two ways; (b) the backstop declared itself a guard against corruption
#             of the staged block but its predicate read only `stamps`, so mutating
#             `second_half_slot` 49 -> 60 left the check PASSING while its own specifics
#             reported visibly wrong period lengths. Ten fail-branch tests now cover every
#             clause.
#             A FIXTURE DEFECT MADE THE ET1/ET2 ASYMMETRY UNTESTABLE BY CONSTRUCTION:
#             `gk_involvement_ticks` applied its own "print a tick only when its slot exists"
#             rule to the FIRST extra period and not the second, so a short ET2 put the 120'
#             tick one slot past the grid and could not be drawn at all. Fixed alongside the
#             predicate.
#             ALSO PATCHED: a RECURRENCE of the delta-absorption shape this story's own
#             2026-07-27 review filed and patched TWICE (`clock_facts` skipped by a
#             `continue` on the failing side, plus a dangling separator); the tick band read
#             from an absolute 30 pt page window that never consulted the chart's own
#             `bottom`, on a page carrying BOTH teams' charts; the one-line guard computed
#             over RUNS after merging, so it read 0.0 spread exactly when a splice happened
#             and fired only when a second line FAILED to splice (the harmless case);
#             overprinted glyphs concatenating through a negative x-gap; unbounded `N` in the
#             stoppage grammar; and two constants exempt from the import-time integrity rule.
#             ONE DECISION WITH A RECORD-SHAPE COST, ruled by Juan: `extra_time_slot` named
#             match minute 91 here but the printed 120' tick slot in `momentum.py:482` — one
#             key, two incompatible meanings, both consumed by 1.16 and 2.10. Renamed to
#             `first_extra_slot` in domain_e ONLY; momentum untouched; 104-record batch
#             re-run. Two decisions ruled AS-DESIGNED and dismissed: MAX_STOPPAGE_MINUTE
#             stays at 30 (the check already records the allotments, and tightening to the
#             measured 19 would fail a legitimate long-stoppage match — the 1.8/1.9/1.12
#             bounds-not-equalities rule), and the parallel `series`/`stamps` lists stay
#             (reshaping moves the ruled baseline for no correctness gain).
#             FIVE CORPUS FIGURES WERE WRONG IN COMMENTS AND DOCS, all corrected: 4,336 tick
#             readings is 4,508 (the story's OWN per-label census sums to 4,508); "207 of
#             208 charts" is 206 (M88 contributes TWO charts, and 207+2 > 208); "`45+N` on
#             110 charts" conflated charts with readings (108 charts / 110 readings; `90+N`
#             182 / 214); "the grid runs 0-7 slots PAST the last tick" is FALSE on 38 charts
#             where the last tick IS the last slot, and contradicted the comment four lines
#             below it; and "across all 18 extra-time charts" is 16, since PMSR-M74-GER-V-PAR
#             prints no `90+N` tick on either side. Story-doc drift also fixed (seven ->
#             eight recorded checks; 16 -> 17 new tests; "1,330 across 37 files", which
#             cannot hold — 39 files collect 1,330 and the two 1.15 files contribute 72).
#             ONE ITEM DEFERRED: `domain_e_checks` bare-subscripts its own payload throughout
#             (all eight keys, all predating Decision 3), so a record from an older checkout
#             raises KeyError rather than failing typed. RECLASSIFIED from patch mid-pass —
#             guarding one key of eight would be inconsistent noise, and the cited
#             2026-07-27 precedent was a different case (a SIBLING domain's payload). The
#             real fix is a record-version ruling. Owner: Story 1.16.
#             VERIFIED: 302 tests green across the 10 affected files (132 + 53 + 117, 0
#             failures); batch 104 extracted / 0 failed / 0 corpus gaps / 0 orphans with
#             exactly the ruled M19 + M58 defensive-actions baseline; the clock check passes
#             104/104; `first_extra_slot` staged on 208/208 with NO `extra_time_slot`
#             survivor; stamp count == series length on 208/208; 18 charts carry extra time;
#             M88 the only short-period note. NOTE: two long runs were KILLED EXTERNALLY
#             mid-flight — the same failure this story's own Completion Notes recorded — and
#             both were resumed to completion; every figure above comes from a run that
#             finished.
# 2026-08-03: story 1-9 in-progress -> review. Decision 3, the last open code-review item,
#             is implemented: the GK involvement chart's slot -> match-clock mapping, derived
#             from that chart's own printed x-ticks (Story 1.8's method, its own code — no FT
#             tick exists, HT prints on 122/208 charts, and stoppage ticks are printed here
#             and nowhere else). The ledgered blocker — an extra-time tick collision — was
#             measured away: it belongs to the naive second-half extension, not to the
#             source. New staged block `involvement_clock`, new typed InvolvementClockError,
#             new recorded check goalkeeping-involvement-clock (7 -> 8 E/F checks); NO new
#             gate id, so no runner/registry churn. Two corpus findings ledgered: pymupdf
#             merges `45`+`HT` into one span on 4 charts (the tick row must be read
#             character-level), and PMSR-M88-AUS-V-EGY draws a 14-slot first extra period,
#             recorded rather than failed so the ruled batch baseline is untouched.
#             Batch 104 extracted / 0 failed / exactly 2 self-validation failures (M19, M58 —
#             both defensive-actions); gate PASS, 0 deviations, two runs identical apart from
#             run_timestamp; idempotent re-run 0 extracted / 104 skipped, records
#             byte-identical. Suite 1,330 collected, all 37 files green.
# 2026-08-03: story 1-15 context created and moved to ready-for-dev — cross-match identity
#             resolution, the FIRST module of AD-9's second phase and the first Epic 1 story
#             that reads NO PDF. The story-creation probe INVERTED the epic's premise over all
#             104 records / 5,392 lineup entries / 1,248 players: OQ-4's three named ambiguous
#             cases are ALL corpus-EMPTY — 0 non-ASCII characters in any player name (accent-
#             stripping is a literal no-op; the full inventory is A-Z, a-z, space, 55 hyphens
#             and ONE period, no apostrophe), 0 normalized name+team collisions (the first-seen-
#             shirt tiebreak is never exercised), and 0 players wearing more than one shirt. So
#             `(team_id, shirt_number)` is by itself a globally unique key on all 104 reports.
#             THE REAL AMBIGUITY IS ONE THE EPIC NEVER NAMES: no record carries surname and
#             given name separately, and the corpus prints four incompatible grammars — given-
#             first `Raul RANGEL` (4,191), all-caps `GABRIEL MAGALHAES` where the boundary is
#             unknowable (707), SURNAME-FIRST `KIM Seunggyu` (78 — every Korea Republic player),
#             and mononyms `ALISSON` (107). RULED: the CAPS-RUN rule (all-caps tokens are the
#             surname wherever they sit; remainder is the given name in printed order; no caps
#             run or no remainder -> slug as listed). VALIDATED NOT ASSERTED: it reproduces
#             155/155 committed fixture player IDs with 0 mismatches and yields 1,248/1,248
#             collision-free corpus slugs. Declared residual: 856 entries / 219 players slug in
#             given-name-first order — unique and deterministic, cosmetic only, and every one is
#             a candidate OVERRIDES entry rather than a code change. Filed for Juan.
#             TWO-THIRDS OF THE WORK IS ALREADY SHIPPED and the story's main job is to stop the
#             dev rebuilding it: `pipeline/ingest/identity.py` already provides `team_slug`
#             (48 teams -> 48 slugs, 0 collisions) and `match_id_for`, every record already
#             carries its padded `match_id`, and canonical order is therefore a plain sorted().
#             The zero-padding question AD-3 leaves open was ALREADY RULED by contract/README's
#             logged decision 1 (m073, not m73) — cited, not reopened. What is genuinely missing:
#             `teamCode` has NO producer anywhere in the pipeline, exists only inside `report_id`,
#             and is the trailing segment of every PlayerId — so it is on the critical path, and
#             at least eight codes (KSA, CUW, CPV, RSA, ESP, SUI, MAR, COD) are not derivable
#             from the printed name, which proves a committed lookup is mandatory.
#             VALIDATION PASS (2 fresh-context agents, ~130 claims re-measured) caught NINE
#             CRITICAL defects in the first draft, all fixed. Headline: the draft's Task 5.3 told
#             the dev to STRIP player names from the spine, but `playerName` is `required` in
#             EIGHT contract $defs and every committed fixture carries playerId AND playerName
#             side by side — a name-stripping spine is unemittable; the assertion is now the
#             inverse (every known name must have a resolved id sibling). Also: the rejected
#             alternative was WRONG — trailing-caps is behaviourally IDENTICAL to the ruled rule
#             on 5,392/5,392 (because 'Seunggyu'.isupper() is False), so the mutation check could
#             not have failed; the rule that actually differs is LAST-TOKEN (1,009 entries, all 26
#             Korean players inverted) and that is now the mutation target. Critically, last-token
#             ALSO yields 1,248 collision-free slugs — so collision count cannot discriminate
#             between rules and only the fixture reproduction can, which is now stated as the
#             acceptance check. Further fixes: `most_offers` has NO shirt_number (23 of 25 name
#             paths carry one, not 25); the pin key was stated three different ways and is now
#             ruled `(team_id, shirt_number)` everywhere; the slug fallback dropped the team code
#             (minting a two-segment slug that validates clean as a TeamId and produces a dead
#             route — the exact defect test_fixtures.py:120-160 exists for); no CLI entry point
#             or public API was pinned; the batch command omitted its required --input-dir; and
#             the Dev Notes rule "use domains.match_metadata, not metadata" is FATAL for the
#             matchday-round derivation, which needs stage_text (absent there), uppercase group
#             and a "H:MM" kickoff — the ISO form raises ValueError in kickoff_sort_key.
#             Also corrected: 839 -> 856 as-listed entries (the UUU bucket was omitted); four ->
#             at least eight non-derivable team codes; 96 -> 155 fixture IDs (the draft walked
#             players[] only, missing the 2-token-surname case which lives in lineups); and four
#             stale file:line citations. M19/M58 are RULED CONSUMED (status "extracted" with
#             self_validation "fail"; their one failing check touches no identity input).
#             THE REGISTRY IS RULED A PYTHON MODULE, on the instruction fingerprint.py:35-39
#             addresses to this story by name: a .json registry falls outside the source glob, so
#             AD-8's "code version includes the committed slug registry" would silently stop
#             holding. Two-source pinning design, because `data/matches/` DOES NOT EXIST yet and
#             a check with no baseline that returns "passed" is a gate that cannot fail.
#             CORRECTION TO THE BRIEF: the working tree is NOT clean and HEAD is eec2397, not
#             7f9b959 — ~1,551 uncommitted insertions across 12 files at creation, growing to
#             ~1,750 across 13 within hours (1.9's Decision-3 work and 1.14's review patches).
#             Touches pipeline/precompute/ (all new) + the checks/test_runner/README seams only;
#             /contract, app/, spike/ and every other pipeline subpackage are import-only.
# 2026-07-27: story 1-9 adversarial code review (3 layers over the 1.9-scoped diff of
#             7279637..7306d7b): 39 raw findings merged to 33 — 3 decisions ruled, 22
#             patches applied, 7 deferred, 1 dismissed then UN-dismissed and patched.
#             Status review -> IN-PROGRESS, not done: Decision 3 is ruled but unimplemented.
#             The Acceptance Auditor independently re-measured all three contested corpus
#             claims over the full 104 and reproduced every figure exactly (624/624 and
#             604/624 with all 20 residuals in feet; 8 innings dropped by strict
#             containment; 0.161278 / 0.000001 / 206 dots / 21,764 dots) — none of the five
#             self-reported departures is fabricated. THREE RULINGS: (a) a Domain A failure
#             no longer suppresses ALL Domain E gate reporting — `lineups` is now optional,
#             `goalkeepers` stages null plus a per-side warning, and the four page families
#             still parse, which is Task 7.2 read literally (this was a SIXTH departure the
#             Completion Notes never recorded). (b) goalkeeping-distribution-printed is now
#             a TWO-sided bound 0 <= drawn-printed <= 2: the one-sided form left the
#             overshoot unbounded while pitch_margin_pt 0.0->0.5 can only push counts UP, so
#             nothing shipped could detect the margin admitting a non-marker — verified over
#             624 panels post-patch at 604 exact / 18 at +1 / 2 at +2, the maximum sitting
#             exactly at the bound. The margin departure itself STANDS; its measurement is
#             sound. (c) the involvement slot -> match-clock mapping (Task 3.11) was dropped
#             rather than reported — ruled IMPLEMENT, probed but NOT built, so 3.11 is
#             re-opened. The probe is the story-shaped finding: the involvement chart's tick
#             grammar is NOT momentum's — NO 'FT' tick exists anywhere, 'HT' prints on only
#             61/104 (momentum 101/104), stoppage prints as composite '45+N'/'90+N'/'120+5'
#             ticks momentum never had, and the last tick sits 0-7 slots BEFORE the grid end
#             where momentum's FT IS the last slot. The relations themselves are all exact,
#             0 deviations corpus-wide: slot(M)==M-1 on 1868/1868 first-half readings,
#             slot('45+N')==44+N on 110/110, all second-half ticks agreeing on one minute-46
#             slot on 208/208 with the HT tick equal to it on 118/118, and
#             slot('90+N')==m46+44+N on 214/214. One open question blocks assertion rather
#             than guessing: on the 9 extra-time reports the second-half stoppage ticks and
#             the ET minute ticks COLLIDE ('90+5' and '95' both land on m46+49). ONE finding
#             was dismissed in triage and then un-dismissed by its own regression test:
#             _gridline_run picking a run shifted one gridline down does NOT trip
#             `len(runs) != 1` — the top-anchored candidate is count+1 long and therefore
#             rejected, leaving the shifted run uniquely selected and every value reading one
#             unit high off a baseline that is not zero; the run must now also start at the
#             top of the grid. Other substantive patches: both bounds recorded their delta
#             census only on PASSING reports (the absorption their own comments forbid);
#             _goalkeepers read a sibling domain's payload with bare subscripting so a shape
#             change raised an untyped KeyError that escaped BOTH gate handlers and recorded
#             one root cause under two ids; domain_f's KPI reader reported a present-but-
#             mistyped value as MissingFieldError; _label_run returned only the leftmost
#             match, making the "printed twice is ambiguous" guard per-ROW on exactly the
#             rows that carry several labels each; _value_gridlines compared x-extents by
#             equality on 3-decimal rounding (a 0.001 pt tripwire in a module with a 0.05 pt
#             discipline); three constant tables the parser zips over were unguarded and zip
#             truncates silently; LINE_BREAKS_X_MIN was a hard page coordinate on the one
#             page whose panel rects are already resolved (now the Total panel's own x0,
#             verified +31.77 pt of clearance on 208/208). Four dead conftest hooks
#             (gk_involvement_axis_rule/_decorate, goal_prevention_decorate, aerial_decorate)
#             and a dead clean_registry fixture are now wired to real tests. Suite for the
#             three files 90 -> 105, all green; batch 104 records / 0 failed / the ruled 2
#             self-validation failures (M19, M58 — both defensive-actions, neither E nor F);
#             all seven E/F checks pass 104/104; gate PASS 0 deviations with all four E/F ids
#             in checks_run. 7 items deferred to deferred-work.md.
# 2026-07-27: story 2-6 implemented and moved to review — the Momentum Timeline, and with it THREE
#             firsts for this codebase: the first recharts surface, the first next/dynamic
#             code-split, and the first role="slider". ONE new runtime dep, recharts 3.10.1
#             (latest stable at install per ARCHITECTURE-SPINE.md:163), lock file committed, and
#             npm audit attributes ZERO of the tree's 12 pre-existing advisories to it.
#             THE RENDER SPIKE PASSED: recharts issue #6857 (blank charts under React 19.2.3,
#             closed "needs reproduction") does NOT reproduce on this app's React 19.2.8 — a
#             throwaway AreaChart painted real geometry and 8 axis ticks with zero console output,
#             so no react-is override was needed or added, and Task 5 was unblocked on evidence
#             rather than on hope. All 27 ruled decisions implemented.
#             AD-14 CLOSED: 1.8's review filed that the schemaVersion 2 bump silently invalidated
#             this story's slider AC because at.minute is not injective. 2.6's decision 1 shipped —
#             the slider INDEXES SAMPLES (m001 aria-valuemax 100, m074 137) and aria-valuetext
#             announces the stoppage offset, verified live ("Minuto 45+1′", "Minuto 120+3′", and
#             m001's indices 44-48 announcing five DISTINCT values for the same minute 45). Pinned
#             by a regression suite that fails if anyone re-indexes by minute. EXPERIENCE.md:74 and
#             epics.md:737 were deliberately NOT edited (decision 22) and their staleness is filed
#             for UX sign-off.
#             VERIFIED WITH REAL INPUT, NOT ASSERTED: 100 real ArrowRight presses from index 0 land
#             on exactly 100 and 60 ArrowLeft back on exactly 40 — zero dropped; PageUp/PageDown
#             exactly +/-10; no wrap at either end. The DRAG BAN is proven (pointerdown at index 2,
#             three pointermoves across the plot, pointerup at the far right -> still index 2), and
#             decision 26's pointer precedence is proven at the hit box EDGE, a pixel where
#             tap-to-position would compute index 12 while the marker correctly wins with 7.
#             BOTH LIGHT-THEME DEFECTS THE STORY PREDICTED WERE CONFIRMED BY LIVE MEASUREMENT AND
#             FIXED, and the method was validated first by reproducing the project's own published
#             dark numbers exactly (fills 5.62/4.48 vs review-accessibility.md:11's 5.62/4.48;
#             goal marker 9.80 vs DESIGN.md's 9.80). Light: the 60% fills compute 2.41/2.53 and
#             FAIL 3:1 — the full-opacity boundary stroke carries the floor at 4.99/5.36; the new
#             canvas-scoped --shot-goal-canvas computes 5.95 where --shot-goal would have been
#             1.77; ViewDataDisclosure's new surface="canvas" variant computes 17.67 where the
#             hardcoded on-pitch ink was 1.10. Every pitch consumer of --shot-goal untouched.
#             TWO DEFECTS FOUND BY VERIFICATION THAT A GREEN 358-TEST SUITE COULD NOT SEE, both
#             fixed: (a) recharts' AUTOMATIC y-ticks are non-uniform and OMIT ZERO on an un-nice
#             domain — m074 (peak 17) emitted +17/+1/-8/-17 which decision 6's sign-stripping then
#             rendered as an unreadable "17 1 8 17", on the one chart whose entire encoding is
#             above-or-below zero, while m001 (peak 10) came out clean and would have hidden it;
#             now explicit symmetric ticks from a property-tested momentumYTicks, and FILED to the
#             ledger because 2.10/2.13/2.15/2.16 all carry recharts charts and meet the same
#             default. (b) awayPlotted was NEGATIVE ZERO on all 53 corpus 0/0 samples, which plots
#             identically but formats as "-0" the instant it reaches a formatter.
#             TWO DECLARED DEPARTURES for Juan. (1) Activating a goal marker MOVES the cursor to
#             that goal's sample; decision 26's literal sentence says activation must not move it,
#             but decision 24 rules the marker role="button" and a button that does nothing on
#             Enter is its own defect — stopPropagation is still proven, so tap-to-position never
#             double-fires. One line to revert if the literal reading is preferred. (2) THE
#             MIDLINE SHIPS AT 2.81:1 over team A's fill in the DARK theme, below the 3:1 floor,
#             and is FILED rather than fixed. Decision 25 commissioned exactly this measurement to
#             replace DESIGN.md's 15.8:1 — which describes ink over the BARE CARD, the
#             reserved-gutter geometry that is NOT expressible in recharts, and which reproduced at
#             15.81 exactly. It is not fixable in-story: --ink-primary is already the lightest dark
#             ink and pure #ffffff reaches only 3.06:1, so clearing the floor needs either a lower
#             fill opacity (decision 9 forbids re-opening that ruled number) or a dedicated token,
#             both UX calls. A card-coloured backing band was evaluated and REJECTED on
#             measurement: at m074's peak 17 on a 122px <md plot one unit is 2.5px, so even a 4px
#             gutter would erase a 1-entry minute — the data lie decision 16 exists to prevent.
#             Light passes comfortably (7.32/6.97).
#             <md (decision 27) measured at 390 and 320 CSS px: full width, 122px plot (clears the
#             44px pointer floor), BOTH TEAMS IN ONE FRAME with no team tabs, ticks thinned to
#             30-minute steps, and ZERO tick-label overlap at 1280/640/390/320 (min gap 19px),
#             which also discharges the 200%-zoom gap. Reflow clean at 390; the 320px overflow is
#             PROVEN pre-existing rather than assumed — m002, which renders no chart at all,
#             overflows to the identical 326px with the identical Key Statistics tile, and
#             #momentum never appears in the sweep at any width. 0 animations on the mounted chart.
#             m002's empty branch shows the DEDICATED UX-DR13 sentence, asserted byte-equal, with
#             the h2 and anchor intact and no collapse; the other ten sections keep the unforked
#             useEmptyHeadline(). EN toggle complete, no mixed-language page. m074 the honesty
#             fixture passes: exactly 2 markers on a shootout, the own goal credited to germany.
#             BUNDLE, disclosed honestly: recharts is its own 337kB/99kB-gzip chunk, absent from /
#             and /about, but Next's static export emits it as an eager <script async> on match
#             routes — so next/dynamic buys route-level isolation (async never blocks the Hero) but
#             NOT on-demand loading, and m002 downloads it despite having no chart. Decision 21's
#             stated goal is met; "lands only when needed" is not.
#             Suite 307/15 -> 358/16 (+44 of those are momentum-model.test.ts; the rest are other
#             sessions' in-flight files in this shared tree). Full chain green, 0 console messages
#             on all three fixtures. tactical-sections.ts untouched; ViewDataDisclosure changed
#             ADDITIVELY so its one existing call site renders byte-identically. Touches app/ +
#             locales + the ledger only; both shared artifacts edited APPEND-ONLY; never
#             `git add -A` — coordinates around in-flight 1-9, 1-13 and 2-8.
# 2026-07-27: story 2-8 adversarial code review (3 layers over the 2.8-scoped working-tree diff,
#             8 files / ~2,011 lines): 22 findings after dedupe — 3 decisions ruled by Juan,
#             13 patches applied, 5 deferred, 4 dismissed. Suite 307 -> 355 files 15 -> 16, of
#             which 2.8 owns 74 (was 67); build chain green. Baseline claims were re-derived
#             rather than trusted: 307/15, the build chain, the threshold literals, the stop
#             distributions, fidalgo-alvaro-mex's degree-0/involvement-80, both locale key sets,
#             the untouched-file list and `selection`-absent backward compatibility all hold
#             exactly as the Dev Record states.
#             THE HEADLINE: AC 1's PRIMARY TRIGGER WAS NOT IMPLEMENTED. Every pointer event
#             resolves through the Voronoi cell layer into onCellActivate, which this story never
#             touched and which never called selection.onToggle; the marker layer is
#             pointerEvents="none" and a singleton's hover popover is an aria-hidden <dl> with no
#             buttons. So a singleton node could not be pinned by pointer AT ALL, and on touch —
#             where a marker can never take focus — it had NO isolation affordance by any input.
#             Task 9.3 is keyboard-scoped, so the live verification never exercised tap. Ruled
#             (decision 1): onCellActivate toggles on singleton clusters; clustered nodes keep the
#             6a dialog path. THE ROOT CAUSE IS ONE FALSE PREMISE: decision 6a assumed "with 11
#             nodes on a full pitch most clusters are singletons" — the dev's own record already
#             measured 18 of 22 clustered at >=lg, and the review measured 10 of 11 at the <md
#             fallback, so 6a handed the toggle to the minority in both directions. Consequence
#             (decision 2): aria-pressed lands on almost no node, so the pinned state now rides
#             the ACCESSIBLE NAME on markers that carry no aria-pressed (new viz.marker.pinned) —
#             putting aria-pressed on a marker that opens a dialog would be the exact defect 6a
#             exists to prevent. Decision 3: the <md default over-stated itself — counts came from
#             UNFILTERED geometry while the declutter hid stop-1 (m001 announced "20 conexiones"
#             with 16 lines drawn, RSA 33 vs 25, m074 PAR 21 vs 15, and it feeds the role="figure"
#             label) and the legend kept advertising the hidden band; both now follow the visible
#             set. Four guards added to a module whose own docstring calls itself defensive-by-
#             mandate, each pinned by a constructed test no fixture can produce: passNetworkNodes
#             [] with a populated edge table is the ZERO STATE, not a throw (sectionDataState
#             gates on `!== null` only, so it reaches the component as "ready" and every endpoint
#             was unresolvable — taking ALL ELEVEN Tactical sections down through the whole-layer
#             error boundary); a duplicate playerId fails loud instead of last-wins (it gave every
#             edge the last duplicate's coordinates AND handed React two markers one key); an
#             edge whose endpoints sit on a different team than its own teamId fails loud (it drew
#             on the wrong figure in the wrong coordinate frame — AR-6); and an unreadable volume
#             stays null instead of coercing to 0 (edgeWeightThresholds already EXCLUDED it, so
#             the figure drew a weight the legend did not cover while the table read "—" for the
#             same row). Also: the legend stated five bare integers with no unit anywhere and every
#             swatch is aria-hidden (new viz.passNetwork.edgeNote names the channel); an empty edge
#             table rendered a swatch labelled "0", a weight the contract forbids; and the three
#             selection constants moved to marker-model.ts, undoing a layering inversion in which
#             the generic panel imported from a story-specific model. FOUR DEV-RECORD CLAIMS
#             CORRECTED IN PLACE, each verified against the code: the two-Escape pin clear is
#             THREE from the dialog path (onDismiss refocuses the marker, whose onFocus reopens
#             the popover) and the dialog is the primary path at every shipped width; the
#             "strict majority" involvement test actually ran m001 alone and asserted "at least
#             one" (claim was true, so the TEST was widened to all 66 nodes / 38 differing); the
#             roving sequence logged as "matching the node table exactly" omits shirts #1 and #3;
#             and deferred-work.md is listed as Modified but is CLEAN — its four 2.8 entries
#             landed inside commit 7306d7b ("Story 1.13"), a co-commit in the direction Task 8.4
#             did not anticipate. Five items deferred with reasons: the whole-layer error-boundary
#             blast radius (pre-existing architecture, but 2.8 wires the least-validated family to
#             it — 1.14 has never run), pitchMarkings' one-ended goal furniture (already ledgered
#             by the dev; 2.8 is just the first surface wide enough to expose it), self-loop edges,
#             the selected-vs-fronted marker z-order, and the <md dead end where pinning a
#             low-volume player dims everything and lights nothing. NOTE FOR THE NEXT SESSION: the
#             tree gained Story 2.6's momentum work (MomentumChart/MomentumSection/momentum-model,
#             recharts in package.json, globals.css, ViewDataDisclosure) mid-review; none of it was
#             touched and none of 2.8's patches depend on it. Status review -> done.
# 2026-07-27: story 1-14 (pass-network extraction) started — status ready-for-dev -> in-progress.
# 2026-07-27: story 1-14 implemented and moved to review — pipeline/extract/pass_network.py, a
#             MATRIX parser rather than a marker parser, and the FIRST module in extract/ whose
#             reason for living there is that the page has NO spatial content at all. The story's
#             premise overturn HELD in full and was re-derived independently, not copied forward:
#             every pinned fact reproduced exactly over all 104 reports / 208 team-innings —
#             48,814 off-diagonal cells (= sum N(N-1) over the N distribution, the story's own
#             arithmetic check), 23,597 edges, 25,217 zeros, volumes 1-48, 9,151 reciprocal pairs
#             of which 6,835 ASYMMETRIC, widths 27.75-58.5pt non-uniform within the page on 156/208
#             with the 52 uniform pages all 36.0pt at N=15/16/17, 24 hyphen-wrap innings matching
#             the story's report list one for one, and the join 3,289/3,289 by verbatim name with
#             the shirt corroborating 3,289/3,289 (0 unmatched; 3,289 + 2,103 unused = 5,392 lineup
#             entries, reproducing Story 1.10's Domain G reconciliation EXACTLY including the same
#             lone anomaly). BOTH re-scope negatives independently confirmed, so the node re-scope
#             stands: 0 pitch frames on 208/208 by filter_chain's own qualifying rule, 0 filled
#             all-Bezier drawings at ANY size, and 0 pages titled average/positions across ALL
#             5,448 pages of the corpus. (Recorded: the naive title scan a reader would write —
#             first 200 chars of page text — hits 104 pages, every one of them the Defensive
#             Pressure page's "Avg Pressure Duration" KPI LABEL, not a title.)
#             THE REAL PROOF is Task 1.4's: an INDEPENDENT prototype extractor, written before and
#             separately from the shipped parser with a different internal decomposition, was run
#             beside it over all 104 reports and compared CELL BY CELL — 52,103 cells, 0
#             mismatches, 0 parse failures, 0 self-validation failures. A column-assignment slip is
#             invisible to every aggregate check, so nothing weaker would have been evidence.
#             ANSWERED a question the story had NOT measured (Task 1.3a): no row label wraps —
#             max 1 line over all 208 innings, 0 continuation lines, so 1.13's three-line-name trap
#             does not occur here. It is GUARDED anyway rather than assumed: a row-label line with
#             no shirt number now raises, naming the fragment. Task 1.3b likewise settled:
#             M92 HENDERSON's COLUMN is all-zero as well as his row, so both directions are ruled
#             and both are tested — a teammate passing TO a phantom raises, the direction Domain G
#             has no analogue for. TWO recorded departures, both TIGHTENINGS and both measured:
#             the no-markers assertion covers ANY size rather than a marker size window (a window
#             would let a re-scaled marker through the tripwire on the one day it matters), and a
#             header-cell CONTIGUITY assertion was added that the story does not ask for — a
#             missing header cell is the one failure the row census cannot see, since it drops a
#             whole column while every remaining column still parses and every row still carries
#             exactly one blank. Three Self-Validation ids ship: pass-network-top5-pct (the page's
#             OWN printed counterpart, the tightest in the epic — TRUE on 1,040/1,040 with worst
#             delta exactly 0.05, the 1-dp half-ulp) plus two BOUNDS (row sum <= Domain G, TRUE
#             3,289/3,289 with equality FALSE on 1,290; matrix_total <= Domain B, TRUE 208/208 and
#             strictly less on 208/208), each recording its delta in specifics on EVERY report so
#             the gap stays visible. The col-sum vs offers_received relation is corpus-FALSE both
#             ways (3,145/121/23) and is named do-not-ship at the would-be call site AND pinned by
#             a test, so a later story reaching for it finds a red test rather than only prose.
#             Batch 104 extracted / 0 failed / exactly 2 self-validation failures (M19, M58 — the
#             ledgered pair, NO third, none from pass-network); re-run 104/104 skipped-unchanged
#             with all 104 records byte-identical by SHA-256. Gate PASS 0 deviations, both new ids
#             in checks_run, two runs identical apart from run_timestamp; the deviation path is
#             proven non-silent over forced-mismatch corpora rather than left as an untested zero.
#             Suite 1137 -> 1227 (+90 tests), and the confirming full run once the tree settled is
#             FULLY GREEN at 1229 passed / 1 skipped / 0 failed (1229 not 1227 because the
#             concurrent 1.9 session added two more of its own in the interval).
#             Mutation check: transposing the parser's column
#             assignment turns 6 tests red, so the fixture is not degenerate. ONE forced repair the
#             story did NOT name: test_extract_report_domains_ef.py pinned Domain E/F's checks as
#             the list's TAIL (ids[-7:]), which a later appender is expressly meant to be allowed
#             to follow — the assertion was wrong, not the appender. SIX AD-14 notes filed,
#             headline: PassNetworkNode.x/y unfulfillable (the THIRD Domain D family, and the
#             NARROWEST — 5 of 7 required fields are available and only x/y are missing), with the
#             three owners each named and 1.16 BOUND to emit null and never [] because 2.8 fails
#             closed both ways. Also CLOSES an open 2.8 ledger question outright: the diagonal is
#             blank on 208/208, so a self-loop pass edge is unreachable from this source by
#             construction. /contract, app/, data/ and spike/ untouched; pipeline/markers/ imported
#             read-only as an assertion, never edited.
#             COORDINATION NOTE: implemented in a working tree a Story 1.9 code-review session was
#             actively writing to (domain_e.py, domain_f.py, checks.py, conftest.py and four test
#             modules). Every shared-file edit here is append-only and this story's warning appends
#             AFTER 1.9's, so the ordered warnings list stays correct for both. That co-tenancy also
#             cost one false alarm worth recording: the FIRST skip-unchanged attempt showed 104
#             re-extracted and all 104 hashes changed, because domain_e.py was saved mid-batch and
#             code_version() fingerprints all of pipeline/**/*.py — not a determinism defect, and
#             clean on repeat once the tree went quiet. Same root cause as story 1.8's review
#             PROCESS FINDING.
# 2026-07-27: story 2-6 context created and moved to ready-for-dev (momentum timeline — app/ only,
#             and the FIRST recharts surface in the project; recharts is not installed today, so
#             2.6 adds it, pinned 3.x-latest-at-install per ARCHITECTURE-SPINE.md:163). Prerequisite
#             verified live at creation rather than assumed: check:types up to date (237 declarations
#             / 6 schemas) and assert:schema-version green (7 artifacts at schemaVersion 2) against
#             baseline c645cfe — 1.8's bump is fully landed in app/src/lib/contract/, so nothing here
#             builds against stale types. Suite baseline 307 passed / 15 files.
#             27 ruled decisions. THE LOAD-BEARING ONE: story 1.8's code review filed an AD-14 note
#             that the schemaVersion 2 bump silently INVALIDATED this story's slider AC and left
#             EXPERIENCE.md:74 / epics.md:737 stale — at.minute is not unique (m001 carries minute 45
#             five times and 90 eight times; m074 45x7 / 90x6 / 105x5 / 120x4, re-measured and
#             confirmed at creation), so "aria-valuemin/max over match minutes" and "arrow keys move
#             +/-1 minute" no longer map onto samples. 2.6 rules it: the slider INDEXES SAMPLES,
#             aria-valuetext announces the stoppage offset, and the story FILES the EXPERIENCE.md
#             staleness for UX rather than silently editing a normative spine sentence.
#             TWO LIGHT-THEME DEFECTS FOUND BY MEASUREMENT AT CREATION, both landing on 2.6 because
#             it is the first theme-aware canvas to consume these tokens — the same position from
#             which 2.7's light-theme disaster surfaced. (a) The 60% team fills clear 3:1 in dark
#             (5.61 / 4.49, reproducing review-accessibility.md:11's published 5.62 / 4.48 and thereby
#             validating the method) but FAIL in light at 2.40 / 2.54 — nobody had ever measured the
#             light theme for this surface; fixed by a full-opacity boundary stroke (4.99 / 5.36
#             light, 13.56 dark) which also discharges DESIGN.md:266's standing Team-B dash rule.
#             (b) --shot-goal computes 1.77:1 on the white card; the light value DESIGN.md:288
#             requires was deleted in the 2.7 review on the rationale that --shot-* "render on the
#             pitch and nowhere else" — momentum is the counter-example that rationale missed.
#             Also measured: ViewDataDisclosure hardcodes text-ink-on-pitch, which is 1.10:1 on the
#             light card, so it gains a surface variant rather than a copy.
#             VALIDATION PASS (2 fresh-context agents) caught five CRITICAL defects in the first
#             draft, all fixed. Headline: the token fix as first written was UNIMPLEMENTABLE — this
#             codebase has no -light-suffixed custom property anywhere (it re-declares base names
#             inside .light), so --shot-goal-light would have been undefined in the dark theme and
#             the flagship chart's goal markers would have had no fill in the canonical theme; now
#             --shot-goal-canvas, declared in BOTH blocks, leaving every pitch consumer untouched.
#             Also: the <md presentation and the chart height were entirely absent (a height-less
#             ResponsiveContainer renders nothing, and Task 2.2 would have primed the dev to misread
#             that as the React 19 issue); the [-peak, peak] domain would have rendered negative
#             y-ticks against the story's own no-negatives rule; chart-overlay positioning had NO
#             mechanism (recharts does not expose its x-scale to a sibling node, and there is no
#             jsdom, so it was a blind iteration loop) and is now recharts-native; the DESIGN.md
#             reserved gutter is NOT expressible in recharts and ships as a declared, re-measured
#             deviation rather than a check that cannot fail; and a recharts <Tooltip> would have
#             left SIGHTED users with a bare minute while screen readers got both teams' values —
#             the cursor chip now carries the numbers and no Tooltip ships. Twelve deferred-work.md
#             citations were corrected for a +12-line drift from other sessions' in-flight edits,
#             and the story now cites by quoted anchor phrase so the next drift is survivable.
#             Two RISKS recorded for dev, not resolved on paper: recharts 3.x is ~147 kB gzipped on
#             a section that is in ALWAYS_EXPANDED_SECTION_IDS and therefore never gets UX-DR6's
#             lazy-mount deferral (hence a next/dynamic code-split, the codebase's first), and
#             recharts issue #6857 reports blank charts under React 19.2.3 closed as "needs
#             reproduction" while this app runs 19.2.8 — so Task 2.2 is a render spike that must
#             pass BEFORE the surface is built. Also filed: the i18n ESLint gate does NOT reach
#             recharts' object-shaped props (label={{value:"…"}} escapes the direct-child selector),
#             which matters more here than anywhere else in the app because recharts delivers all
#             text through props rather than JSX children.
#             Touches app/ + locales + the ledger only — coordinates around in-flight 1-9 and 1-13,
#             which have pipeline/, contract/ and data/fixtures/ dirty; never `git add -A`.
# 2026-07-27: story 1-14 context created and moved to ready-for-dev — and the story-creation probe
#             ANSWERED the risk story 2.8 routed here (deferred-work.md:294), overturning HALF the
#             epic's premise over all 104 reports / 208 team-innings. THE NODES HAVE NO COORDINATES:
#             the "Passing Networks {team}" page carries 0 pitch frames on 208/208 (measured with
#             filter_chain's own qualifying rule), 0 markers (not one filled all-Bezier drawing; the
#             only curves are two ~9pt header sort-arrows), and its single image is a 36x36pt logo.
#             No page anywhere in the corpus is titled with average/positions and 0 pitch-bearing
#             pages are unanchored. PassNetworkNode.x/y are therefore unfulfillable — the THIRD
#             Domain D family with an unfulfillable required field after 1.11's CrossEvent and
#             1.13's ReceivingEvent, but the NARROWEST: 5 of 7 node fields are available and only
#             x/y are missing. /contract stays READ-ONLY; filed as an AD-14 emission blocker riding
#             CS-1's successor. THE EDGE HALF IS THE CLEANEST EXTRACTION IN EPIC 1 and is built as
#             written: a square NxN directed matrix (N 13-17), 23,597 edges over 48,814 off-diagonal
#             cells, volumes 1-48, diagonal blank on 208/208, 9,151 reciprocal pairs of which 6,835
#             are ASYMMETRIC (so never symmetrize or dedup). The join is exact — all 3,289 rows join
#             to Domain A by verbatim name with the shirt corroborating 3,289/3,289, 0 unmatched —
#             and 3,289 + 2,103 unused substitutes = 5,392 lineup entries reproduces Story 1.10's
#             Domain G reconciliation EXACTLY, including the same lone anomaly (M92 away #14 Jordan
#             HENDERSON, all-zero row), so 1.10's asymmetric join rules transfer wholesale. Self-
#             Validation has a real printed counterpart for once: the page's own "Top 5" panel
#             reconciles to the matrix total on 1,040/1,040 percentages within 0.05 (the 1-dp
#             half-ulp). Two relations ship as BOUNDS not equalities per the 1.8/1.9/1.12 rule —
#             row sum <= Domain G passes_completed (true 3,289/3,289, equality FALSE on 1,290) and
#             matrix_total <= Domain B passes_completed (true 208/208, strictly less on 208/208) —
#             and one is corpus-FALSE both ways and named do-not-ship (col sum vs offers_received:
#             3,145 greater / 121 equal / 23 less). Ruled: module is pipeline/extract/pass_network.py
#             (the page has no pitch geometry at all, and it joins Domain A like domain_g); column
#             assignment is GEOMETRIC from the header rects, never text order — zeros are printed but
#             the blank diagonal makes every row ragged, and widths are non-uniform WITHIN the page on
#             156/208 (range 27.75-58.5pt), so neither a hardcoded 36pt nor a span/N model works.
#             Validation pass (2 fresh-context agents) caught 2 material defects in the first draft,
#             both fixed: the edge/reciprocity counts were self-contradictory (a sweep had silently
#             dropped the 24 hyphen-wrap innings — sum(N(N-1)) is the arithmetic check now written
#             into the story), and the column-geometry model was wrong in a way that was HARDER than
#             described. Also fixed: a false "the epic cites AD-4" correction (it cites AD-6, which is
#             correct — the extract-never-derive rule is AD-3 and epics.md:84's AR-3 drops it), a
#             Self-Validation id colliding with a gate id, three stale deferred-work line numbers, and
#             a self-contradiction claiming 25,217 zeros were absent from the text layer. TWO items
#             need a ruling BEFORE 1.16: (a) 2.8 fails CLOSED on this data — nodes:null blanks the
#             WHOLE #pass-networks section via tactical-sections.ts:124-125's `&&`, and nodes:[] throws
#             inside TacticalErrorBoundary killing all ELEVEN Tactical sections (open ledger item), so
#             1.16 must emit null and never []; (b) involvement must be derived from the MATRIX, not
#             from Domain G, or the fixture invariant tightening in test_fixtures.py is wrong.
#             Also CLOSES an open 2.8 ledger question outright: the blank diagonal makes a self-loop
#             pass edge unreachable from this source by construction. Touches pipeline/extract/ +
#             the record/gate seams + conftest only — coordinates around in-review 1-9 (extract/) and
#             2-8 (app/); every shared-file edit append-only. Baseline commit c645cfe.
# 2026-07-27: story 1-9 (Domains E & F extraction) started — status ready-for-dev -> in-progress.
# 2026-07-27: story 1-9 implemented and moved to review — pipeline/extract/domain_e.py and
#             domain_f.py, the FIFTH instance of the 1.6/1.7/1.10 extractor convention and the
#             first that is four different extraction problems under one name: one table page
#             (goal prevention), one half-table (aerial), one marker MAP (distribution) and one
#             per-minute CHART (involvement). Both parsers were swept over all 104 reports
#             BEFORE wiring in: 104/104, 208/208 team-innings, 0 parse failures, 0 self-validation
#             failures each. Every pinned fact re-derived independently — 4 equal-area panels
#             (59,516.0 pt2) on 208/208 with every frame at y1=406.5 on 832/832, 0 off-palette
#             markers, 21,764 involvement dots at 3.0 pt, slot counts 95-111 regulation /
#             129-145 extra time, goalkeepers 1 on 201 innings and 2 on 7 with the seven report
#             ids matching the story's list one for one. All four corpus-FALSE controls
#             reproduced EXACTLY (style-sum 112/208, direct==on+off 208/208, types==total_
#             interventions false 207/208, ti==af-nsa false 183/208) and the fixtures make them
#             false by construction so no test can bless them. AC 1's re-scope held: staged PER
#             TEAM with Domain A's keeper list carried beside the block, never joined; the
#             two-keeper case is pinned by a test asserting both names AND a byte-identical team
#             block. FIVE recorded departures, each measured not assumed. (1) pitch_margin_pt is
#             0.5, not 0.0: the story's stated reason for strict containment is measurably wrong
#             — the legend swatches are 9.0 pt (outside the size window) sitting 10.5 pt below
#             the frame and NO out-of-size circle is within 6.0 pt of any frame — while strict
#             containment DROPS real markers on 8 innings (max overshoot 0.2917 pt), and
#             admitting them makes 7 match their printed donut exactly (the 1.11 touchline
#             finding). (2) goalkeeping-distribution-printed ships as a BOUND: the story's
#             claimed 208/208 equality is false — drawn>=printed holds 624/624 while equality
#             holds 604/624, all 20 residuals in the feet panel, hands and throw exact 208/208.
#             Investigated, not manufactured away: the Total panel is the EXACT union of the
#             other three, so the drawn set is self-consistent and the map plots more feet
#             distributions than the technique donut counts. Delta recorded in specifics on
#             every report (Task 5.6's own reasoning). (3) extract_domain_e takes the cover team
#             names — ONE page carries BOTH charts and names them only in their titles, so the
#             split would otherwise be positional (AD-8); a test prints the AWAY chart on top and
#             the payload does not move. (4) The involvement baseline anchors on the zero
#             GRIDLINE, not the zero label — which RESOLVES the story's open tolerance item and
#             corrects its scope: the labels sit 1.81 pt above their gridlines and the offset
#             SCALES with the per-report unit, so the naive fit's worst residual is 0.161278 and
#             exceeds its own 0.15 bound on 206 DOTS, not on 2 charts. Gridline-anchored worst
#             residual is 0.000001 units over 21,764 dots. (5) The set-plays numeric census runs
#             AFTER the grammar, so a dropped value fails with its own row's message. One
#             hardening the tests forced out: the KPI upward walk was unbounded and the KPI
#             columns REPEAT down these pages, so a missing value silently adopted the tile
#             above's number — now bounded to 80 pt in both modules. Two guards were dead by
#             construction (below-zero, above-top-label) and are now live and tested. Both table
#             headers are closed constants asserted by EQUALITY (one distinct form on 208/208),
#             and the corpus's own M38 stray-ordinal trap is drawn on every synthetic page so
#             the x>=460 bound has a standing regression. Batch 104 extracted / 0 failed /
#             exactly 2 self-validation failures (M19, M58 — 1.12's ledgered pair, NO third,
#             none from E or F), exit 1 by design; re-run 104/104 skipped-unchanged with all
#             104 records byte-identical. Gate PASS 0 deviations, all four new ids in
#             checks_run, two runs identical apart from run_timestamp. Suite 1039 -> +91 tests;
#             final run 1138 passed / 1 skipped / 0 failed (the extra 8 are another session's,
#             which also edited checks.py mid-flight — all shared-file edits here are
#             append-only). TWO forced repairs: test_runner's checks_run list (named by Task
#             7.5) and test_ingest_record's EXACT warnings set (NOT named by the story — but
#             that test exists to fail when a story adds a documented absence, so the repair is
#             the intended one: three warnings -> six, sourced from domain_e_warnings()).
#             Eight AD-14 notes filed — headline: GoalkeeperRecord is per-keeper and the source
#             is per-team (1.16 emission needs a decision, Story 2.10 needs re-scoping), and
#             the contract's FreeKickCounts $comment is corpus-FALSE on 208/208.
#             /contract untouched; pipeline/markers/ imported, never edited.
#             COMMIT-SCOPE DISCLOSURE: story 1-9 has no commit of its own — commit 7306d7b,
#             authored by another session and titled "Story 1.13: offers & movement to receive
#             parsers, code review done", swept up EVERY 1.9 implementation file from the
#             shared working tree (domain_e.py +1424, domain_f.py +586, errors.py, extract_
#             report.py, checks.py, conftest.py, the three new test files, both forced repairs,
#             pipeline/README.md and the deferred-work filings). The committed content is
#             identical to the tree the final green run verified, so nothing is unverified —
#             but a reviewer diffing 1.13's commit will find 1.9's whole change set inside it.
# 2026-07-27: story 1-9 context created and moved to ready-for-dev — and the story-creation
#             probe OVERTURNED the epic's premise for Domain E, corpus-wide over all 104
#             reports / 936 E+F pages. Domain E is printed PER TEAM, not per goalkeeper: all
#             four goalkeeping page families are titled `{team}`, NO goalkeeper name appears
#             anywhere on any of them, and 7 of 208 team-innings used TWO keepers (M21, M41,
#             M53, M62, M66, M88, M98 — verified on M53, where Mexico's RANGEL came off at 78'
#             and the page still prints one chart, one total, no name). AC 1's "every
#             goalkeeper with minutes has a record" is therefore unfulfillable, and so is the
#             contract's per-keeper GoalkeeperRecord. Scope ruled by Juan: re-scope inside the
#             story (the 1.13 precedent), staging per team with Domain A's goalkeeper list
#             carried BESIDE the block so the attribution is recorded, never inferred. Domain E
#             is also four different extraction problems under one name — one table page (goal
#             prevention), one half-table (aerial control), one MAP page (distribution) and one
#             CHART page (involvement) — so the "tabular extractor" framing fits Domain F only.
#             Juan ruled BOTH hard parts IN: the involvement per-minute series is extracted now
#             (95-111 slots regulation / 129-145 extra-time, per-report exactly like 1.8's
#             momentum; scale from the printed y-axis labels, never gridline spacing), and the
#             distribution complete/incomplete split is recovered by counting markers through
#             the SHARED CHAIN read-only — detect_pitch_frames returns exactly 4 equal-area
#             panels (59,516.0 pt2 each) on 208/208, so 1.12's plural accessor + title-anchored
#             panel typing is mandatory and detect_pitch_frame's max() is unusable. The exact
#             MarkerSpec is corpus-verified and pitch_margin_pt=0.0 is LOAD-BEARING: every
#             panel frame ends at y1=406.5 while the Complete/Incomplete legend sits at y~411,
#             so strict containment excludes the legend by geometry (any positive margin
#             inflates every count by 2); 0 off-palette markers admitted on 208/208, and no
#             legend-row exclusion is needed at all. Eleven relations measured EXACT corpus-wide
#             to key Self-Validation on, incl. AC 1's (feet+hands+throw == total panel, 208/208),
#             AC 2's (corner sides sum to total corners, 208/208), the marker-count-vs-printed-
#             donut-centre cross-check, and sum(5 intervention types) == attempts faced
#             (208/208). SIX tempting relations are corpus-FALSE and are named as do-not-ship,
#             headline: the contract's FreeKickCounts $comment asserts direct == directOnTarget
#             + directOffTarget "holds across all six fixture team-innings" — it fails on
#             208/208 REAL team-innings (160 print on+off == 0 while direct > 0). Also false:
#             corner delivery STYLE is not a partition of corners (112/208), and sum(series) ==
#             Total Involvements (149/208) — so the involvement check ships as the BOUND
#             sum(series) <= total, true 208/208 (delta 0..5, never negative, mean 1.26); the
#             residual gap is the one open question left to dev, explicitly NOT to be closed by
#             making the numbers agree (the 1.8/1.12 rule). Three documented absences take the
#             absence branch, all raster-only like 1.13's donut slices: the distribution
#             technique breakdowns, goal prevention's byBodyType, and aerial
#             crossesFacedCompleted. Two page traps pre-filed: the goal-prevention 7-column
#             table must be found by header-anchor + x>=460 (naive "row of 7 digits" finds ZERO
#             on M38 home, where a stray pitch-marker ordinal shares the row, and TWO on
#             M34/M38 away), and the set-plays KPI values print ABOVE their labels with one
#             value row shared by two KPIs. No new AnchorSpec — 1.2 registered all five specs.
#             Gate ids ruled goalkeeping-{completeness,counts} / set-plays-{completeness,counts};
#             deliberately NOT offers-count-match, which is test_checks_registry.py's unclaimed
#             placeholder. Touches pipeline/extract/ + the record/gate seams + conftest only —
#             /contract stays READ-ONLY (1-8's schemaVersion 2 bump has already landed and is in
#             review; the v2 diff touches the momentum defs alone, so every E/F shape 1-9 cites
#             is unchanged), and pipeline/markers/ is read-only for in-dev 1-13 (filter_chain is
#             imported, never edited). Baseline commit 7279637.
# 2026-07-27: story 2-8 context created and moved to ready-for-dev (pass-network visualization —
#             the second PitchPanel consumer, app/ only). Builds on 2.7's seam rather than
#             re-implementing pitch rendering: nodes are PitchMarkers (so the shared extent,
#             Voronoi hit cells, >=44px floor, clustering, popover, roving tabindex and
#             role="figure" all come free) and edges ride the `underlay` prop 2.7 cut for exactly
#             this story. Sixteen ruled decisions; the load-bearing ones: node positions are
#             EXTRACTED not derived (AD-3, not AD-4 — no force layout, d3-force stays
#             uninstalled), the pass network is a FULL pitch by construction (fixture node x
#             spans 20.21-79.71, so pitchExtentFor returns xMin:0 on its own — the halfway-line/
#             centre-circle code path 2.7 unit-tested but never rendered); the edge ramp bins by
#             panel-pooled nearest-rank quintiles so that UX-DR17's "lowest weight quintile" and
#             DESIGN's 5-stop ramp become the SAME partition (resolving the unruled `<` vs `<=`);
#             and isolation is PINNED-ONLY, keyed on playerId never an index — focus does not
#             pin, because aria-pressed on mere focus announces a state the reader never set
#             (the exact defect review-accessibility.md:29 filed against this interaction).
#             Measured at creation time rather than left to dev: all five --edge-weight stops
#             clear 3:1 on the pitch (3.83/5.06/6.60/8.55/10.77) but ADJACENT stops separate by
#             only 1.32/1.31/1.29/1.26 — so DESIGN's "stroke width rising in parallel" is the
#             encoding, not a flourish, and a colour-only ramp would read as one green smear.
#             Dim values measured too: nodes 0.55 (4.02/3.34:1, still hittable targets so still
#             >=3:1), edges 0.25 (1.42:1, a recorded exception — no single alpha both dims and
#             holds the floor across the ramp). 2.8 is the FIRST consumer of --edge-weight-*,
#             the same position from which 2.7's light-theme disaster surfaced, so Task 9.4
#             re-measures. Validation pass (2 fresh-context agents, 44 fact-checks) caught seven
#             CRITICAL defects in the first draft and all are fixed: node dimming had NO
#             mechanism (nodes are markers, unreachable from the underlay closure — now an
#             explicit selection.dimmedKeys), Enter/Space silently collided with PitchPanel's
#             existing popover handler (now a ruled three-way split; clustered nodes keep the
#             dialog and pin from its list items), "Esc clears" was unreachable (focus opens the
#             hover popover, so the guard always ate the first Esc — now a layered panel-level
#             handler, two presses, disclosed), team-switch clear was uncallable from the
#             section, cross-side dimming would have greyed out the opposing team, and the
#             legend's React key collides on narrow distributions. Two DEVIATIONS flagged for
#             Juan: the pass matrix ships PLAIN, not sortable (EXPERIENCE.md:207 lists it among
#             the Expert-layer tables 2.11 owns; UX-DR16's floor is still met in full), and
#             player names are plain text because /players/{slug} does not exist yet. Highest
#             unfiled risk recorded as Task 8.2: nobody has ever probed the "Passing Networks"
#             page, and three of the four Domain D families probed so far had unfulfillable
#             required per-row fields (1.11 CrossEvent, 1.13 ReceivingEvent) — PassNetworkNode.x/y
#             could be the third, and the 2.3 sign-off row walked the contract, not the PDF.
#             Touches app/ + locales only — coordinates around in-dev 1-8 and 1-13, which have
#             contract/, data/fixtures/ and app/src/lib/contract/* dirty; never `git add -A`.
#             Baseline re-measured live at 240 passed / 14 files.
# 2026-07-27: story 2-8 implemented and moved to review — pass-network visualization, the SECOND
#             consumer of 2.7's PitchPanel rather than a second pitch renderer. Nodes are
#             PitchMarkers (shared per-panel extent, Voronoi hit cells, >=44px floor, clustering,
#             popover, roving tabindex, focus ring, role="figure" all inherited); edges ride the
#             `underlay` seam 2.7 cut for exactly this story, inside the aria-hidden decorative
#             group under both the hit layer and the markers. All 16 ruled decisions implemented as
#             written. THE FULL PITCH IS AUTOMATIC AND PROVEN: pitchExtentFor returns {xMin:0}
#             unaided for all six team-innings at all six probed widths, so the halfway-line/
#             centre-circle/centre-spot path 2.7 unit-tested but never rendered was rendered and
#             inspected. The ramp and the quintile split are ONE partition, asserted directly
#             (edgeStop(v,t)==1 <=> v<=t[0] on every fixture edge); thresholds and stop
#             distributions were computed INDEPENDENTLY IN PYTHON before the TypeScript existed and
#             pinned as literals (m001 [4,7,11,14] 12/10/15/7/9; m002 [5,7,13,15] 12/7/10/8/9;
#             m074 [4,9,13,15] 12/7/10/9/7) — one test literal was wrong and the implementation
#             right (m001's top band is 15-18: 18 is the POOLED max, Mexico's own side tops at 17,
#             which is the pooling ruling proving itself). Isolation verified LIVE: Enter pins,
#             aria-pressed=true, exactly 1 selection ring, 8/11 nodes and 18/20 edges dimmed on its
#             OWN figure while the other figure stays 0/0 (decision 15), second Enter clears, and
#             the node's transform is BYTE-IDENTICAL before and after (AR-6). Clustered path also
#             verified: Enter opens the dialog and does NOT pin; its list items do. Two Escapes
#             clear a pin — the ruled, disclosed consequence of focus opening the popover. <md
#             declutter measured: 16 edges drawn -> 20 after "Mostrar todos los pases". Reflow at a
#             TRUE 320 CSS px: scrollWidth == clientWidth == 320, #pass-networks NOT in the overflow
#             list (a 5px in-section bleed is TacticalSection's expanded-state chevron, proven
#             pre-existing by reproducing it identically on #shot-maps). 0 animations in the
#             subtree, 0 console messages on all three fixtures. LIGHT THEME: 2.8 is the first
#             consumer of --edge-weight-*, and all eleven on-pitch tokens read byte-identical in
#             both themes — no .light override exists and none was added. TWO DEVIATIONS for Juan:
#             the pass matrix ships PLAIN not sortable (routed to 2.11 with plug-in points named;
#             UX-DR16's floor is met in full — this is the one decision to overrule if sorting is
#             wanted in 2.8), and player names are plain text because /players/{slug} does not
#             exist. THREE declared departures: passNetworkMarkers takes edges + a valuePhrase
#             callback (degree is required in the accessible name by Task 2.6 and cannot be
#             computed or worded inside src/viz); quintileBands DROPS an empty band rather than
#             labelling an impossible "8-7" range; and a node with an unreadable x/y THROWS naming
#             the playerId (no honest placeholder exists for a position — NaN paints an invisible
#             marker, a drop publishes an invisible absence). TWO items flagged, NOT changed:
#             pitchMarkings draws goal furniture at ONE end only, so the full pitch has a bare
#             defending half (shipped 2.7 code, aria-hidden decoration, no ruled reference for the
#             second end — 2.8 is just the first surface wide enough to expose it); and at the
#             shipped >=lg figure width (527px HORIZONTAL) only 4 of 22 nodes are singletons, so
#             decision 6a's dialog path is the PRIMARY pin route, not the exception — the story's
#             "most clusters are singletons" premise is false, though 6a rules the case correctly.
#             Highest unfiled risk NOW FILED: nobody has ever probed the "Passing Networks" page,
#             PassNetworkNode.x/y is required and must be extracted (AD-3), and three of the four
#             Domain D families probed so far had unfulfillable required per-row fields — routed to
#             1.14 with the note that 2.3's sign-off row for this surface walked the contract, not
#             the PDF. PitchPanel changes are strictly additive: with `selection` absent the Enter
#             branch calls openClusterOf VERBATIM and ShotMapsSection's output is unchanged (all
#             240 pre-existing tests green). No new dependency; d3-force stays uninstalled;
#             tactical-sections.ts's `&&` untouched. Suite 240 -> 307 (14 -> 15 files), build chain
#             green. Touches app/ + locales + the ledger only — coordinates around in-flight 1-8
#             and 1-13; never `git add -A`.
# 2026-07-26: story 2-7 adversarial code review (3 layers over f8ca7ee..1e67e25): 25 unique
#             findings — 4 decisions ruled, 18 patches applied, 1 deferred (ShotLogRow's dead
#             minute fields, routed to 2.11), 5 dismissed. Two defects the 237-test suite
#             structurally could not see, because neither is reachable in a jsdom-less harness:
#             (a) hit cells were seeded on cluster CENTROIDS, so a click dead-centre on a marker
#             could open a popover not containing it — 12 reproduced cases across all three
#             fixtures at 320/386/527/768 px, incl. m074 Germany's crosses at the shipped >=lg
#             width; now seeded per MARKER (AC 3 as written) with the cluster still the hit unit,
#             pinned by 3 new tests that fail if centroid seeding returns. (b) a stale
#             open.clusterIndex passed an `!== null` guard as `undefined` and threw the whole
#             Tactical section into the error boundary on any resize that shrank the cluster
#             count (the story's own measurement: 12 cells at 1920px, 4 at 386px). Also LIGHT
#             THEME: 2.7 is the first-ever consumer of --pitch-surface and the five --shot-*
#             tokens, and .light overrode them to light-canvas variants on a theme-invariant
#             green — every marker 1.62–2.05:1 and every panel string 1.45–1.60:1. The .light
#             --shot-* overrides are deleted (pitch-only hues are theme-invariant by definition)
#             and --ink-on-pitch{,-secondary} / --viz-team-{a,b}-on-pitch added; all on-pitch
#             tokens now 4.58–11.14:1 in BOTH themes. Ruled decisions 3 (extent now per PANEL,
#             not per side) and 9 (popover follows focus — AC 3's third trigger) amended.
#             Co-commit disclosed: 1e67e25 also carried 1-8/1-10/1-13 sprint flips and the 1-10
#             review's ledger entries; ruled to stand as accurate, the Completion Notes claim
#             corrected. Suite 237 -> 240; build chain green. Status review -> done.
# 2026-07-26: story 2-7 implemented and moved to review — app/src/viz/ created (the architecture's
#             own structural seed, first use) plus the reusable PitchPanel that 2.8/2.9 build on.
#             The testability split IS the story: the harness has no jsdom, so projection,
#             ordering, clustering, Voronoi, the encoding maps, the log rows and the figure counts
#             all live in pure src/viz/ modules with real tests (90 of the 99 new tests) and the
#             components stay thin. ONE new runtime dep, d3-delaunay@6.0.4 — exactly the Voronoi
#             module inside the pinned d3 7.9.0, deliberately not the monolith; React owns every
#             SVG node, d3 never touches the DOM. Layout arithmetic in MEASURED CSS px via a
#             ResizeObserver hook, because a scaling viewBox turns the 44 px hit floor into 31 px
#             on a 288 px phone: measured smallest Voronoi cell is 98x118 px at 1920 and
#             135x104 px at 386. AR-6 held and PROVEN rather than asserted — the SM-3 spot check
#             at <md reproduces Story 1.3's permanent ground truth for spike/mex_rsa.pdf (16
#             markers, 2/2/8/3/1), the attacking goal is up the page (penalty area flush to the
#             top edge, goal mouth outside the goal line), and one marker was hand-derived from
#             the artifact to the rendered translate() exactly (95.28/32.00 -> 104.5,32.5). No
#             transposition, no mirroring, no clamping. m074 is the honesty fixture and passes:
#             Germany renders 21 markers and ZERO goal markers with a figure summary reading
#             "0 goles" while keyStatistics.home.goals == 1 (counts come from the marks drawn,
#             never from keyStatistics — ruled decision 12); the own-goal note renders; the log
#             carries 8 PAR rows with "Gol (a.g.)" against 7 PAR markers; none of the 9 shoot-out
#             attempts appear anywhere. Keyboard contract verified live: roving by minute, no
#             wrap, Enter opens a real role=dialog on a 3-marker cluster with focus on the
#             opening marker's own list item, Esc returns focus, and the marker's transform is
#             BYTE-IDENTICAL before and after (overlapping markers are never displaced); repeat
#             pointer clicks cycle the z-order. FD-1 applied throughout (uniform markers, no xG
#             row, no xG column) with tests proving both APPEAR once a constructed shot carries a
#             value. CS-1-proof by construction: maps ShotOutcome only, never ShotOutcomeDetail.
#             CLOSES 2.5 review decision D7 — sectionDataState("shot-maps") now reads BOTH tables
#             and is empty only when shots AND crosses are both null; 2.5's shots=null->"empty"
#             assertion was replaced by the four-way truth table and the ledger entry is struck.
#             Suite 138 -> 237 (10 -> 14 files), build chain green, no console errors. Three
#             recorded departures from the task text, each with its reason: clusterMarkers adds a
#             centroid-stabilisation pass (single-link ALONE makes Task 3.6's ">=44 px by
#             construction" comment and Task 3.8's property test FALSE — an adversarial ring
#             centroids onto a lone marker; the ring is now a named test); CROSS_COMPLETED_ENCODING
#             is split into a static shape Record + a function of the team accent (the colour is
#             per side, so a frozen table cannot hold it); and four singular/plural counter keys
#             were added because t() has no plural machinery and "1 goles" is a visible defect.
#             Flagged for review, NOT changed: #shot-maps renders a heading button at >=lg, which
#             reads against 2.5's own sprint note — confirmed pre-existing (#pass-networks does
#             the same; buildSectionPlans sets collapsible width-independently) and TacticalSection
#             is on this story's do-not-touch list. Reflow: at 316 px client width scrollWidth ==
#             clientWidth and NO #shot-maps element ever appears in the overflow sweep; the 7 px
#             overflow below ~305 px is a Key Statistics tile (2.5 code, already 2.19's).
#             Touches app/ + locales + the ledger only — pipeline/ never staged.
# 2026-07-25: story 1-10 implemented and moved to review — pipeline/extract/domain_g.py, the
#             third instance of the 1.6/1.7 extractor convention and the FIRST extractor that
#             joins to another domain's output. Four page families on one shared row reader
#             over lines.py; ordinal column assignment guarded by the per-family exact-count
#             assertion (14/8/15/9), deliberately NOT x-band classification (three families
#             centre-aligned, physical right-aligned); raw locale-neutral typing with
#             high_speed_runs/sprints asserted integral before narrowing, never rounded.
#             The join is within-report VERBATIM name identity to Domain A's lineups with the
#             shirt number as corroborating key, and it is asymmetric: a page row with no
#             lineup player fails loud (PlayerJoinError), a lineup player WITH minutes and no
#             row fails loud (MissingFieldError), an unused substitute has no row and that is
#             correct — 2,103 such entries (= the story's 8,412 entry x family pairs; 3,289 +
#             2,103 = 5,392 lineup entries, reconciled). Four recorded checks with tolerances
#             derived from printed precision then corpus-verified, incl. the goals
#             reconciliation carrying Story 1.6's own-goal term (naive form is corpus-FALSE on
#             14 team-innings). Parser swept over all 104 reports BEFORE wiring in: 104/104,
#             3,289 rows, 0 check failures, worst drifts 0.200 m / 0.0499 km / 0.500 — every
#             story-pinned tolerance reproduced exactly. Batch 104/104 extracted, 0 failed, all
#             four checks pass on every report (run result FAIL only from 1-12's two
#             pre-existing ledgered discrepancies, not Domain G); gate PASS 0 deviations, both
#             new ids in checks_run, two runs identical apart from run_timestamp. Suite
#             811+1 -> 879+1 (+68 tests), ONE forced repair (the checks_run list, named by
#             Task 6.4). Discovery: the committed m001 fixture's physical.totalDistance
#             disagrees with the printed page on 30 of 31 players while its 8 sibling physical
#             fields match EXACTLY — a fixture-data finding, AD-14 note filed and pinned by a
#             dedicated test. Two further notes filed (playerId is 1.15's; hsr/sprints '.0').
# 2026-07-24: story 2-4 (match route + Hero layer) started — status ready-for-dev -> in-progress.
# 2026-07-24: story 2-4 code review — 16 patches applied, 7 decisions deferred to deferred-work.md; review -> done.
# 2026-07-24: story 2-4 implemented and moved to review — first dynamic route /matches/[slug]
#             (SSG over tournament.json manifest, dynamicParams=false, 3 routes pre-rendered),
#             AR-11 split (build-data fs reader for generateStaticParams/metadata/Hero;
#             MatchBundleRegion fetches only its own bundle at runtime, FR-34). Hero as useT()
#             client components (Spanish canonical HTML + post-hydration swap): stage chip,
#             context line (venue-local kickoff), score row (display-score once, team->/teams
#             links), decidedBy caption (shootout "Penales: 3–4"; extra-time type-first +
#             unit-tested, FR-1 gap), scorers grouped by benefiting team (m074 GOMEZ own-goal
#             in Germany's column, AD-6), five Story-Stat tiles with ▲+accent+sr-only leader
#             (never color-only), lineups disclosure (one aria-expanded; player->/players
#             links ship in DOM). Locale: enums.stage/position filled + match.* namespace.
#             build-data client-import seam added (+3 gate fixtures). Build chain green; 98
#             tests pass (68->98). Browser-verified ES/EN swap (no mixed-language) + light
#             theme + disclosure expand. Note for review: stage chip uses type-label-caps
#             (no uppercase transform) per the story's explicit token over the mockup CSS.
# 2026-07-27: story 1-8 adversarial code review (3 layers over the 1.8-scoped working-tree diff):
#             31 raw findings merged to 25 — 6 decisions ruled, 16 patches applied, 3 dismissed.
#             All four ACs audited as satisfied and every substantive Dev Record claim verified
#             against the codebase rather than taken on trust. Rulings: (a) momentum-coverage was
#             largely TAUTOLOGICAL — two of four clauses unreachable because _clock_structure
#             already fails loud on every clock inconsistency; narrowed and re-documented as the
#             staged-payload backstop it is, with the real strength moved INTO _clock_structure as
#             hard stoppage/FT bounds. (b) the v2 schema encoded NONE of the invariants its own
#             descriptions assert — `{"samples": []}`, reversed order and duplicate stamps all
#             validated clean against AC 3's "never []"; minItems/uniqueItems added inside the bump
#             (order is not expressible in JSON Schema and stays pinned by check + fixture test).
#             (c) Task 3.5's fail-loud text and (d) Task 3.3's 429.13 literal are both STALE — the
#             code is right and the story text now says so. (e) the bump silently invalidated story
#             2-6's aria-valuetext AC: at.minute is NOT unique (m001 carries minute 45 x5, 90 x8),
#             so 2-6 must index its slider by SAMPLE, not minute — filed as an AD-14 note and
#             recorded in the schema description; EXPERIENCE.md deliberately left to UX/2-6.
#             (f) the fixtures gave the chart its own empty page, stripping every co-tenant of the
#             real lineups page — four co-tenant tests added via the unused `decorate` hook.
#             Parser hardening: stoppage bounded against the contract's StoppageMinute (corpus max
#             is 19, so no false fire), a bar-signature template change now fails LOUD instead of
#             staging momentum:null on all 104, legend reads are order-independent and normalized,
#             gridlines cluster by tolerance so unrelated page furniture no longer aborts a report.
#             Re-verified: batch 104/0/2 (M19,M58) with momentum checks 104/104 pass and the
#             extraction byte-identical to pre-patch; gate PASS 0 deviations; app build green at
#             schemaVersion 2. Suite 1136 passed / 1 skipped / 2 failed — both failures pass in
#             isolation and are the shared-working-tree artifact below, not 1.8 defects.
#             PROCESS FINDING (needs a ruling): stories 1-8, 1-9 and 1-13 are being developed in
#             ONE working tree, and code_version() fingerprints all of pipeline/**/*.py. Every
#             batch run therefore computes a different fingerprint and correctly invalidates all
#             104 records, so NO story can verify a skip-unchanged or byte-identity claim (task
#             8.3) while that holds — and long test runs flake on any file another session saves.
# 2026-07-27: story 1-8 implemented and moved to review — momentum-series extraction plus the
#             project's FIRST contract bump (schemaVersion 1 -> 2, AD-14). OQ-5 verified in code
#             over all 104 reports: every pinned fact reproduced (title once per report on page 1,
#             baseline y=429.13 single-valued, peak 50.3805pt, width/pitch 0.7000-0.7004, 0
#             non-integer heights, printed top label == derived peak, orange->home / purple->away
#             proven by matching each legend swatch's name to that report's cover). THREE findings
#             sharpen the story's table: the chart draws TEN grey lines (nine value gridlines plus
#             an axis rule 0.75pt below), so a naive max(y) puts the half height at 50.755 and
#             corrupts the peak cross-check; the peak equals the axis half height EXACTLY on
#             104/104 (an independent confirmation of the auto-scale, now asserted); and the
#             '45HT'/'90FT' merge hazard is neutralized by construction (tick reading is
#             character-level, regrouped on the digit/alpha boundary, so both spellings parse).
#             Task 1.3 CLOSED: first half slot = minute-1 (0 violations/104); the HT tick marks the
#             FIRST slot of the second half (slots 48-56) and slot(M)=M-46+HT_slot holds with 0
#             violations on the 101 reports printing HT — 3 omit it (M67/M86/M104) and infer it
#             from 60/75/90, which must agree; the FT tick is the grid's last slot on 94/94 that
#             print it and 1 omits it (M42, fallback recorded); the 9 extra-time reports print NO
#             ET break anywhere, so ET1 opens after FT and the 120 tick's own 15 regular minutes
#             place ET2 — a derivation, filed in deferred-work.md. TWO numerical bugs were caught
#             by the corpus run and not by unit tests: the floored Euclidean GCD converged on PDF
#             coordinate noise (~0.001pt "unit" on 13/104; fixed by nearest-multiple remainder +
#             smallest-first divisor), and a GCD over individual bar-gap distances drifts (replaced
#             by the outermost span over its whole slot count). ONE design correction mid-story:
#             deriving the unit as peak/printed-label made momentum-axis-scale TAUTOLOGICAL, so
#             the unit is now geometric (GCD over heights) and the printed label is the recorded
#             contrast — geometry contradicting itself still raises, the printed axis
#             contradicting geometry is recorded (SM-C1). Like 1.12's possession-regain map there
#             is NO printed row total, and none was manufactured. Contract: MomentumSample
#             {minute,home,away} -> {at: MinuteStamp, home/away integer >= 0} (the bare Minute
#             could not represent stoppage slots at all — every first-half stoppage minute
#             collapsed onto 45), version 2 across all five artifact schemas, types regenerated
#             for contract/ and app/, all seven fixtures re-stamped, m001 (101 samples) and m074
#             (138) regenerated from real corpus data, m002 LEFT null by design and now pinned by
#             a test + a fixtures-README note. Also did the ledgered _check extraction (momentum
#             would have been the fifth copy) -> pipeline.extract.check_entry. Batch --force:
#             104 extracted / 0 failed / exactly 2 self-validation failures (M19, M58 — 1.12's
#             ledgered pair) with the ruled non-zero exit; momentum-axis-scale and
#             momentum-coverage pass 104/104, 0 momentum warnings, 10,954 samples total (96-145
#             per report). Re-run: 104/104 skipped-unchanged, records byte-identical. Gate PASS
#             0 deviations with both new ids in checks_run; two gate runs byte-identical apart
#             from run_timestamp. Suite 1040 passed / 1 skipped (incl. in-flight 1-10 and 1-13);
#             app 240 passed, lint + check:types + build green. Four forced test repairs, all
#             expected: checks_run list, anchor count 30/47 -> 31/48, version.json == 1, and the
#             app's assert-schema-version test (now reads the generated constant). Coordinated
#             with in-flight 1-13 in the same tree: all shared-file edits append-only.
project: wc-stats
# 2026-07-22: story 1-1 completed and moved to review.
# 2026-07-22: story 1-1 code review complete — 5 decisions resolved, 35 patches applied,
#             3 items deferred to deferred-work.md. Status review -> done.
# 2026-07-22: story 1-2 context created and moved to ready-for-dev.
# 2026-07-22: story 1-2 implemented and moved to review — 104/104 reports extracted,
#             0 failures, all 47 anchors resolved on every report (1.4 sampled 16).
# 2026-07-23: story 2-1 context created and moved to ready-for-dev; epic-2 in-progress.
# 2026-07-23: story 1-2 code review complete — 3 decisions resolved, 31 patches applied,
#             6 items deferred to deferred-work.md, ~20 dismissed. All 7 ACs satisfied.
#             Decisions: requirements.txt folded into code_version; orphan records now
#             fail the run (exit 1); match number bounded 1..104 per the contract.
#             Re-verified after patching: 104/104 extracted, re-run 104/104 skipped (42x),
#             --force byte-identical, 1.4 gate clean, 440 tests pass. Status review -> done.
# 2026-07-23: story 1-3 context created and moved to ready-for-dev.
# 2026-07-23: story 2-1 implemented and moved to review — app/ scaffold (Next 16.2.11
#             static export), full DESIGN.md tokens, typed es/en i18n + Intl helpers,
#             generated contract types, AR-13 build chain green, 24 tests, 4 negative
#             gate proofs, zero-external-request audit clean.
# 2026-07-23: story 1-6 context created and moved to ready-for-dev (Domain A: metadata
#             normalization + lineup-page parse; establishes pipeline/extract/; tabular
#             only — coordinates around in-flight 1-3 (markers/) and 2-1 (app/)).
# 2026-07-23: story 1-3 implemented and moved to review — pipeline/markers/ filter chain
#             + shots parser, AD-6 frame verified on ground truth (16 markers, 2/2/8/3/1),
#             Self-Validation in records + manifest, shots-parse/shots-count-match gate
#             checks. Full run 104/104 PASS, all 104 Self-Validations pass, gate PASS.
#             Discovery: 37 reports' attempts tables span two pages; parser sums table
#             pages, count check unchanged. Suite green at verification point (496+1);
#             later reds in test_ingest_*/test_extract_domain_a stem from 1-6's in-flight
#             pipeline/extract/ fixture needs (venue table / lineup content), not 1-3.
# 2026-07-23: story 1-6 implemented and moved to review — pipeline/extract/ established
#             (Domain A: stage/date/kickoff normalization + 16-venue offset table +
#             two-column lineup parse with glyph-classified goal/own-goal/sub/card
#             minutes), 6 self-validation checks appended beside 1-3's, 2 FR-15 gate
#             checks. Full run 104/104 PASS (all match_metadata populated, all checks
#             pass), gate PASS 0 deviations, re-runs byte-identical, suite 588+1 green
#             (the 1-3-noted reds resolved: make_report lineups page + real venue,
#             shots-check filters, kickoff purity exemption). Discovery: red-football
#             glyph = own goal (score = goals + opponent own-goals verified on all 104);
#             second-yellow unrecoverable from card RGBs — AD-14 note filed.
# 2026-07-23: story 1-3 code review complete — 1 decision resolved (off-shape staged
#             self_validation disqualifies the skip: re-extract, per the match_id trust
#             precedent), 11 patches applied (stroked pitch-frame constraint restored,
#             shape-aware failure summary, e2e ground-truth self-validation assertion,
#             gate parse memo, 5 new tests, doc fixes), 3 deferred to deferred-work.md,
#             6 dismissed on full-corpus evidence. Re-verified after patching: suite
#             593+1 green, batch 104/104 PASS, gate PASS 0 deviations. Status review -> done.
# 2026-07-23: story 2-1 code review complete — 4 decisions resolved, 16 patches applied,
#             3 items deferred to deferred-work.md, 3 dismissed. Highlights: i18n lint
#             gate hardened (bypasses closed, component copy-props gated, 13 lint-fixture
#             regression tests), format helpers fail loudly (Z-kickoff/invalid date/time/
#             non-finite), storage no longer resurrects deleted keys, vendored components
#             reconciled to tokens, shadcn package dep dropped, --ink-on-cyan light
#             ratified in DESIGN.md. Chain + 41 tests green. Status review -> done.
# 2026-07-23: story 2-2 context created and moved to ready-for-dev (site chrome: header,
#             ES|EN + theme toggles with pre-paint bootstrap, attribution footer, static
#             404; resolves the deferred t() boundary policy; touches app/ only —
#             coordinates around in-flight 1-5/1-6 pipeline work).
# 2026-07-23: story 1-5 context created and moved to ready-for-dev (marker–event linking;
#             scoping probe on spike/mex_rsa.pdf settled the digit-glyph semantics: on-marker
#             ordinal labels 1..N joining table row position; outcome cross-check via the
#             contract's x-maps-to-outcome; xG stays null per the contract's corpus-verified
#             decision. Touches pipeline/markers/ only — coordinates around in-review 1-6).
# 2026-07-23: story 2-2 implemented and moved to review — site chrome shell (pre-paint
#             locale/theme bootstrap script generated from tested pure functions, Theme/
#             Locale Contexts with wcstats.* persistence + polite live-region announce,
#             sticky header with ES|EN radiogroup + theme toggles, skip link, attribution
#             footer + /about stub, static out/404.html). Deferred t() boundary items
#             resolved: production fallback-to-es policy + no-restricted-imports client
#             seam (5 lint fixtures). Chain green, tests 41 -> 67, browser-verified
#             (no hydration warnings; persistence across reload; en swap on 404/about).
# 2026-07-23: story 1-6 code review complete — 2 decisions resolved (unknown minute
#             glyph -> unknown-rgb like shots; formation-sum stays a recorded check per
#             Task 7.2), 24 patches applied (5 new fail-loud parser guards, typed
#             MalformedFieldError + calendar/clock validation, full §6 completeness walk,
#             gate memo + PipelineError single-attribution, strict self-validation
#             aggregator moved to the package seam, exact-path kickoff purity exemption,
#             AD-14 own-goal ledger note vs stale contract $comment, 14 new tests, 1.6
#             tests + story finally committed), 2 deferred, 6 dismissed. Re-verified:
#             105 tests in 1.6 files green; Domain A sweep over all 104 PDFs clean (the
#             full batch/gate is currently blocked by in-flight 1-5 label mapping, not
#             1-6). Status review -> done.
# 2026-07-23: story 1-7 context created and moved to ready-for-dev (Domains B & C:
#             Key Statistics 19-field block + contested possession, Phases of Play 8+9,
#             line-height/team-length pages with the 3-per-panel metre investigation;
#             AC 2's "block sum ~100%" corrected on corpus evidence — blocks are
#             independent rates (contract $comment + mex_rsa 43/49); possession-sum +
#             metre-bounds are the recorded checks. Tabular extract/ only — coordinates
#             around in-flight 1-5 (markers/); batch/gate acceptance caveat noted).
# 2026-07-23: story 1-5 implemented and moved to review — pipeline/markers/attempts.py
#             (column-segmented row extraction, frozen label->enum maps) + linking.py
#             (digit-glyph collection, two-pass bijective assignment with outcome-
#             compatibility constraint, merged-word readings, link-rate checks); events
#             enriched with linked/ordinal/time_raw/shirt/player/labels/xG-null;
#             Self-Validation completed (count AND 100% link rate); marker-event-link-rate
#             gate check. Heuristic iterated over 5 full-corpus runs to 2571/2571 markers
#             linked; batch 104/104 PASS, gate PASS 0 deviations, re-runs byte-identical
#             (unblocks the batch/gate caveat noted by 1-6's review). Discovery: 3 AD-14
#             contract gaps — bare 'Incomplete'/'On Target' outcome labels missing from
#             the closed enum; deflected-on-target-defensive-event drawn in BOTH marker
#             colours (10 incomplete / 1 on-target of 11 rows); stoppage-minute finding
#             (time_raw prints cumulative; period only from row order) deferred to 1.16.
# 2026-07-23: story 2-3 context created and moved to ready-for-dev (AD-14 gate: per-surface
#             contract sign-off. Verification story — walks the 1.1 pre-computed checklist
#             against /contract v1 + fixtures; 10 pre-analyzed findings F1-F10 to adjudicate
#             incl. per-shot xG conflict, own-goal stale $comment, and 1.5's ShotOutcomeDetail
#             enum gaps routed to this sign-off; change requests batch into ONE change-set,
#             no schemaVersion bump landing mid-flight under 1-5/1-7).
# 2026-07-23: story 2-3 implemented and moved to review — AD-14 gate walked: 24 checklist
#             rows (15 PASS, 8 PASS-with-note, shot-maps carries the 2 change requests;
#             counts as re-tallied by the 2026-07-23 review — original line said 15+7=24),
#             F1-F10 all adjudicated with evidence citations. Outcome SIGNED-OFF-WITH-
#             CHANGE-REQUESTS: CR-1 (ShotOutcomeDetail + bare incomplete/on-target) and
#             CR-2 (deflected-on-target-defensive-event one-to-many map) batched into
#             change-set CS-1 (one atomic bump commit, must land before 1.16 emission;
#             F2 stale own-goal $comment correction rides it); FR-1 fixture-coverage
#             request routed to 1.18 (no bump); FD-1 rendering decision binds 2.7/2.11
#             (uniform markers, popover omits null xG); F3/F8 NO-CHANGE. Epic 1 extraction
#             1.7-1.15 UNBLOCKED; 1.16 blocked-pending CS-1. Durable record appended to
#             contract/README.md; ledger entries struck/added. No contract file changed;
#             baseline + post-edit guards green (192+1 pytest, check:types, schema-version).
# 2026-07-23: story 1-5 adversarial code review (3 layers over d60827c..e6cfa7e): 19 unique
#             findings — 3 decisions (2 pre-adjudicated by 2.3's CR-1/CR-2; cross-check-as-
#             assignment-constraint ratified by user), 11 patches applied (commit-scope
#             disclosure in the story record — 19816fc carried 1.6's Domain A repairs that
#             fixed a 4-commit ImportError on main; tautological row-count assert removed;
#             exclude_legend_rows back in the production path; _word_readings capped;
#             format_summary dispatch by check id; 7 new tests incl. merged-word rescue
#             assignment-level coverage), 1 deferred (_domain_a_memo, ledgered), 5 dismissed.
#             Re-verified: suite 663+1, batch 104/104 PASS, gate PASS 0 deviations.
#             Status review -> done. Spatial-join stories (1-11..1-14, 2-7+) unblocked.
project_key: NOKEY
# 2026-07-23: story 1-11 context created and moved to ready-for-dev (crosses map parser;
#             reuses the 1.3 filter chain via MarkerSpec per-type tuning; crosses anchor
#             already registered; contract pins CrossEvent completed:boolean + 6-value
#             CrossDeliveryType with NO cross RGB legend anywhere — mandatory probe task
#             first, AD-14 dev-note candidates pre-filed. Self-validation counts against
#             the crosses page's OWN table rows, never key_statistics (1.7's Domain B).
#             Touches pipeline/markers/ + the two extract seams + gate checks only —
#             coordinates around in-dev 1-7; shared-file edits append-only).
# 2026-07-23: story 2-3 adversarial code review (3 layers over e6cfa7e..063544e): 18 unique
#             findings — 3 decisions resolved (CR-2 array entry stands, enum-split recorded
#             as rejected alternative; CS-1 tracked via 1-16 BLOCKED annotation + Task 6.2
#             coordination rule restored + 2.7/2.13/2.18 bound to post-CS-1 enum; fixtures-
#             only no-bump carve-out ratified in the AD-14 section), 15 patches applied
#             (headline: CS-1 recipe now names its pipeline consumers — test_markers_attempts
#             frozen-map asserts, BOTH test_fixtures outcome/detail asserts, attempts.py
#             constants — and proves by the FULL pipeline suite; "nothing else consumes the
#             changed values" was false. Also: project_key restored after accidental delete;
#             1.5-review ledger co-commit disclosed in the Debug Log; verdict tally corrected
#             15+7->15+8 with row 13 reclassified PASS-with-note; stale own-goal README table
#             row corrected; FR-1 guard-test scope added; FD-1 annotated at epic 2.7's AC),
#             0 deferred, 3 dismissed. Status review -> done. 1-16 stays blocked-pending CS-1.
# 2026-07-23: story 1-7 implemented and moved to review — pipeline/extract/domain_b.py
#             (Key Statistics 19-field block + contested possession, label-relative span
#             classification, left=home side verification) + domain_c.py (Phases 8+9,
#             defensive-block projection, line-height bracket classification). Task 4.3
#             investigation resolved the 3-per-panel semantics from measurement-bracket
#             geometry: line height (vertical, touches goal line) / team length (vertical,
#             spans block) / team WIDTH (horizontal) — verified on all 3,744 corpus values.
#             4 recorded checks + 4 FR-15 gate checks; AC 2's corrected checks verified
#             corpus-wide (possession-sum ±0.2, metre bounds; blocks confirmed non-summing).
#             Batch 104/104 PASS, gate PASS 0 deviations, gate re-runs identical apart from
#             run_timestamp, suite 663+1 -> 726+1 (63 new tests). Two forced test repairs
#             (checks_run list; kebab panel keys vs snake-case walk — documented). AD-14
#             line-height shape note + 1.8 momentum location note filed in deferred-work.md.
# 2026-07-23: story 2-4 context created and moved to ready-for-dev (match route + Hero:
#             first dynamic route /matches/[slug] via generateStaticParams over the
#             tournament.json manifest; AR-11 split — Hero pre-rendered from metadata+
#             storyStats via useT() client components, below-Hero fetches only its own
#             bundle; 2.3 sign-off gate verified — all Hero rows PASS (title/OG row
#             PASS-with-note F3), CR-1/CR-2/FD-1 are shot-maps-only, so no Hero-relevant
#             contract change is unresolved; ET decidedBy branch built type-first (FR-1
#             fixture gap, unit-tested); validation pass applied 9 corrections (substitutes
#             field name, tile digit pinning, knockoutScore always-present). Touches app/ +
#             locales only — coordinates around in-review 1-7 and in-dev 1-11 pipeline work).
# 2026-07-23: story 1-11 implemented and moved to review — pipeline/markers/crosses.py on
#             the shared filter chain (7.4pt 2-color markers -> completed:bool; 9pt legend
#             excluded by size window; additive MarkerSpec.pitch_margin_pt=1.0 admits 9
#             corpus touchline crosses, coords clamp to [0,100]; orange+blue bit-identical
#             two-tone double-draws collapse to one completed event, 17/16 pages — real
#             same-spot pairs never deduped). Probe overturned the spec's shots-shaped
#             assumptions: section is ONE page/team (208/208), table is per-player delivery
#             AGGREGATES (no per-event rows, no ordinal glyphs) -> Task 4.2 no-linking
#             branch; SV counts markers vs SUM of Total Attempted column (== printed
#             Attempted panel 208/208). crosses-parse/crosses-count-match gate checks.
#             Suite 764+1 green, batch 104/104 PASS (208/208 crosses checks pass, re-run
#             byte-identical), gate PASS 0 deviations (sample includes the anomaly reports).
#             3 AD-14 notes filed — headline: CrossEvent's required playerId/playerName/at/
#             deliveryType are unfulfillable from this page; 1.16 emission needs an AD-14
#             decision (ride CS-1's successor, not CS-1).
# 2026-07-24: story 1-11 adversarial code review (3 layers: Blind Hunter, Edge Case
#             Hunter, Acceptance Auditor). Acceptance Auditor: 0 AC/constraint violations
#             — all 3 ACs + every Dev Note verified against code, batch (104/104 PASS) and
#             gate (PASS). 1 decision resolved → patch: the unconditional coordinate clamp
#             could silently rewrite a mis-normalized coordinate to a plausible boundary
#             (AD-8 tension, count check blind to it). Fix: COORD_CLAMP_TOLERANCE=0.5 +
#             _clamp_coord helper raising the new typed CrossesCoordinateError beyond the
#             sub-tolerance touchline overshoot (1pt-margin markers overshoot ≤~0.4, so the
#             real case still clamps; existing 100.1 test green). 0 deferred, 13 dismissed
#             (fail-loud, unreachable in the 208/208 corpus, or already ledgered).
#             Re-verified: 170 tests pass (crosses + ingest_record/batch + checks_registry
#             + runner). Status review -> done.
# 2026-07-24: story 1-12 context created and moved to ready-for-dev (defensive-actions map
#             parser; second map family on the shared 1.3 chain, following 1.11's per-type
#             tuning template. Scoping probe (spike + M01-M04, 10 pages) pre-filed in the
#             story: the page carries TWO stroked pitch panels of EXACTLY equal area
#             (61,168 pt2) titled "Forced Turnovers" / "Possession Regain", so
#             detect_pitch_frame's max() silently discards one map — the story's central
#             chain gap (additive multi-frame accessor, shots/crosses byte-identical).
#             All markers share ONE fill (0.18,0.3,1.0), so actionType comes from the
#             panel title, not the colour; the 9.0 pt bullet swatches share that exact
#             blue and sit 0.13 pt above the marker size — size window + panel containment
#             are the only defenses. Left panel count == printed Forced Turnovers on 10/10
#             pages; right panel consistently EXCEEDS printed Possession Regained (and the
#             per-player table sum, which match each other exactly) — unresolved, gating
#             Task 5's SV design and AC 2's documented-absence branch. No linking possible
#             (no ordinal glyphs, no per-event rows). Touches pipeline/markers/ + the two
#             extract seams + gate checks only — coordinates around in-dev 1-10; shared-file
#             edits append-only. Note: 1-12 claims the `defensive-actions-count-match` id
#             that test_checks_registry.py currently uses as its unclaimed placeholder.)
# 2026-07-24: story 1-10 context created and moved to ready-for-dev (Domain G: per-player
#             in/out-of-possession + physical. Tabular only — the third instance of the
#             1.6/1.7 extractor convention; no new anchor spec (1.2 registered all four
#             per-team specs). Story-creation probe + full 104-report sweep pinned every
#             structural question BEFORE dev: all 8 G anchors resolve to exactly ONE page
#             (832/832, no 1.3-style overflow); numeric columns invariant at 14/8/15/9 per
#             family over 3,289 rows each; 0 non-numeric tokens; hsr/sprints print '.0' but
#             are integral 3289/3289. Join integrity resolved in BOTH directions and it is
#             ASYMMETRIC — 3,289 page rows join 1:1 to a lineup player with minutes (0
#             unmatched, 0 shirt disagreements) while 8,412 unused-substitute entries
#             legitimately have NO row; the natural "every lineup player needs a row" check
#             would fire on all 104. Four recorded checks corpus-verified with derived
#             tolerances: zone-sum (worst 0.200 m of 0.35 bound), cross-domain distance
#             reconciliation vs Domain B (worst 0.0499 km), internal consistency (offers
#             movement types sum EXACT; completion worst 0.500), and goals reconciliation —
#             corrected at creation time to include Story 1.6's own-goal term after the
#             naive form was found corpus-FALSE on exactly 14 team-innings (the corpus's 14
#             own goals); with the term, 0 mismatches / 208. Suite baselined 766 passed +
#             1 skipped at f932f6c. Touches pipeline/extract/ + the record/gate seams +
#             conftest only — stays off the marker/parser-chain family for in-dev 1-12.)
# 2026-07-24: story 1-12 implemented and moved to review — pipeline/markers/
#             defensive_actions.py on the shared filter chain. Corpus probe (104 reports /
#             208 pages) confirmed the pre-filed findings and sharpened three: the two
#             panels are NOT exactly equal in area (61,168.1435 vs 61,168.1451), so
#             detect_pitch_frame's max() deterministically discarded the LEFT (Forced
#             Turnovers) map on 208/208 — closed by an additive detect_pitch_frames
#             (proven identical on all 5,448 pages; shots+crosses payloads byte-identical
#             over all 104 reports); each panel also draws 4 stroke-only corner arcs at the
#             marker's exact width (only fill-is-None excludes them) and WHITE filled
#             penalty/centre spots inside the pitch (only marker_min_pt excludes them —
#             admitting either aborts all 104 on an unknown fill). Task 1.2's open question
#             RESOLVED by measurement: the possession-regain map matches NO printed number
#             (best single field 4/208; exhaustive ±1 linear combos never reach 150/208;
#             dedup at 7 thresholds reconciles ≤16/208 and destroys the left panel's own
#             206/208; markers form one contiguous draw block) and R−PR ranges −3..+36,
#             negative on 2 pages — so it takes AC 2's documented-absence branch (table:
#             null, no check, one per-report warning) while forced-turnover checks against
#             the page's printed headline. Panel→type is text-anchored via the frozen
#             label map; no linking possible (0 digit glyphs in-panel), contest_type null.
#             Suite 766 -> 811. HEADS-UP: batch is 104/104 extracted, 0 failed, but 2
#             reports self-validate FAIL — PMSR-M19-ARG-V-ALG (39 markers drawn vs 40
#             printed) and PMSR-M58-TUN-V-NED (33 vs 34), verified by rendering and hand-
#             counting as genuine source self-contradictions, not parse defects. The check
#             was NOT loosened (SM-C1/AD-8), so the full-corpus run now exits 1; filed as
#             NEEDS ADJUDICATION at the top of deferred-work.md with three options. FR-15
#             gate PASS 0 deviations (neither report is in the 16-report sample; the
#             count-mismatch path was proven directly on the anomalous pair). Re-run
#             104/104 skipped-unchanged, records byte-identical.
# 2026-07-26: story 1-12 code review complete and moved to done — 4 decisions ruled,
#             15 patches applied, 2 items deferred to deferred-work.md. Production
#             changes: per-panel DIRECTION-label orientation assertion, both printed
#             headline totals staged as printed_total, panel-count check before title
#             lookup, x-restricted player_x, two-line-name row floor, unambiguous
#             headline column match. Suite green at 895 passed / 1 skipped (that
#             figure includes in-flight 1-10). RULED: the full-corpus batch exits 1
#             by design from 1-12 onward; the clean-run baseline is 104 extracted /
#             0 failed / exactly 2 self-validation failures (M19, M58) — later
#             stories must assert that baseline, never a zero exit code.
# 2026-07-24: story 2-5 context created and moved to ready-for-dev (Tactical Layer shell +
#             Key Statistics + the shared empty-state pattern 2.6-2.10 depend on. Builds
#             ONTO 2.4's route without touching the AR-11 split: the layer mounts inside
#             MatchBundleRegion's loaded branch, so it is client-only — no Tactical markup
#             exists in out/, static-output tests cannot cover it (turned into an assertion
#             instead), and deep links must be re-resolved on mount because the browser
#             gives up on #momentum before the target exists. 11 section shells in the
#             normative order from a pure registry; disclosure precedence ruled: empty
#             sections never collapse (an absence you must tap to find is still silent),
#             key-stats + momentum never collapse (both mockups render them expanded at
#             390px; UJ-1 reads them), the other nine collapse <lg via heading-wrapping
#             <button aria-expanded> with lazy-mounted content (deliberately NOT 2.4's
#             hidden-in-DOM pattern). Key Statistics = the full 19-field Domain B block,
#             paired tiles, higher-value-leads + ▲ + sr-only «líder»; at <md the six
#             mockup-led rows show with the rest behind "Ver todas las estadísticas" —
#             the ruled resolution of AC 2's two clauses (full block vs. one scroll still
#             reaching momentum). contestedPossession is match-level, rendered as a caption.
#             null vs [] is load-bearing: only null triggers the empty state. Registry keys
#             enums.metric by TeamKeyStatistics field name, which is string-identical to
#             MetricCode for 18 of 19 — 2.13 inherits the labels. Closes 2.4 deferred
#             decision D4 (bundle now consumed, so matchId/schemaVersion are validated).
#             Route container widens max-w-2xl -> max-w-6xl for the ≥md two-column grid.
#             Touches app/ + locales only — coordinates around in-dev 1-10 and in-review
#             1-12 pipeline work; never `git add -A`.)
# 2026-07-24: story 2-5 implemented and moved to review — Tactical Layer shell mounted inside
#             MatchBundleRegion's loaded branch (client-only, AR-11 untouched): 11 section
#             shells from a pure registry (tactical-sections.ts) in the normative order, each
#             with its stable anchor. Disclosure resolved against the mockups: at ≥lg the nine
#             collapsible sections render in the NON-collapsible presentation (plain <h2>, no
#             trigger, no summary — the desktop mockup's only aria-expanded is the Hero's), at
#             <lg they are heading-wrapping <button aria-expanded> shells with one-line summary
#             and LAZY-MOUNTED content (deliberately not 2.4's hidden-in-DOM pattern); key-stats
#             and momentum never collapse, and an empty section never collapses at any width.
#             Focus-on-expand is nonce-driven so a ≥lg render or a resize never steals focus.
#             Key Statistics: 19 paired tiles (higher value leads + ▲ + sr-only líder), six
#             compact rows + "Ver todas las estadísticas" at <md, contestedPossession as a
#             caption. Shared EmptyStatePanel + a distinct PendingSectionPanel (a ready section
#             with no view yet must never claim the report omits it). Anchors re-resolved on
#             mount + hashchange because the browser gives up on #momentum before the layer
#             exists; scroll-padding-top added for the sticky header. Closes 2.4 deferred
#             decision D4: the bundle is now held and validated (matchId + SCHEMA_VERSION)
#             before "loaded". Route container max-w-2xl -> max-w-6xl (Hero keeps its measure).
#             Locale: tactical.* namespace + enums.metric (19, keyed by field name so 2.13
#             inherits 18) + enums.unit.km. Suite 103 -> 131, build chain green. Browser-verified
#             at ≥lg and <lg (fold, keyboard, deep links + hashchange, m002 empty state, ES/EN,
#             light theme). One item filed: the page reflows cleanly at 320 CSS px but not at
#             195 (header + Hero + key-stats together) — routed to 2.19.
# 2026-07-26: story 2-7 context created and moved to ready-for-dev (pitch-panel infrastructure
#             + shot & cross maps — the reusable base 2.8/2.9 build on). Creates app/src/viz/
#             (the architecture's own structural seed, first use) and extends the ESLint
#             client-import seam to cover it — closing, for the new directory, the residual gap
#             the 2.2/2.4 reviews filed. ONE new runtime dep: d3-delaunay@6.0.4, exactly the
#             Voronoi module inside the pinned d3 7.9.0, deliberately not the monolith; the
#             affine projections are plain arithmetic and React owns every SVG node (d3 never
#             touches the DOM). Testability drove the split: the harness has no jsdom, so
#             projection/ordering/clustering/Voronoi/encoding/log-rows all live in pure
#             src/viz/ modules with real tests and the components stay thin — the same shape
#             that made buildSectionPlans the tested heart of 2.5. Twelve ruled decisions;
#             the load-bearing ones: the cross map is a SECOND PANEL inside #shot-maps (the
#             registry stays at eleven sections); `shot-maps` is empty only when shots AND
#             crosses are both null, with a single missing table naming its own absence in
#             its panel slot — this CLOSES 2.5 review decision D7, deferred here with the
#             reason "2.7 owns shot/cross maps and should rule", and it BREAKS 2.5's existing
#             `shots = null -> empty` unit assertion, which the story names as a required
#             update; cross markers carry the TEAM ACCENT with completion dual-encoded by
#             fill, not the shot-outcome hues (borrowing two would put one hex on two meanings
#             inside one section); half pitch by default but full pitch the moment any event
#             has x < 50 (never clamp — AR-6); layout arithmetic in MEASURED CSS px, because a
#             scaling viewBox turns the 44 px hit floor into 31 px on a 288 px phone; and the
#             root <svg> is NOT aria-hidden (AC 3's literal wording describes an axe
#             aria-hidden-focus violation — decorative subtrees are hidden, the marker group
#             is exposed). FD-1 applied throughout: uniform marker size, no xG row, no xG
#             column. Story-creation probe pinned the traps BEFORE dev: m074 Germany has
#             keyStatistics.goals == 1 and ZERO goal markers (GOMEZ's own goal benefits
#             Germany but is excluded from the map per AD-6), so figure summaries count
#             rendered marks and never read keyStatistics.goals; CrossEvent's playerId/
#             playerName/at/deliveryType are handcrafted fixture values (Story 1.11 proved
#             them unfulfillable from the source page — 1.16 emission is blocked pending an
#             AD-14 decision), so the cross popover/table must be null-defensive today. SM-3
#             has real ground truth: spike/mex_rsa.pdf IS the m001 report and Story 1.3 pinned
#             Mexico at 16 markers, 2/2/8/3/1 — the verify task compares the rendered map
#             against it at <md, where the vertical attacking-goal-up orientation is the
#             source's own. CS-1-proof by construction: this story maps ShotOutcome (stable
#             five values) only, never ShotOutcomeDetail. Touches app/ + locales only —
#             coordinates around in-review 1-10; never `git add -A`. Baseline measured at
#             f8ca7ee: 138 passed / 10 files, build green.
# 2026-07-26: story 1-8 context created and moved to ready-for-dev (momentum-series
#             extraction + the project's FIRST contract bump). OQ-5 was RESOLVED at story
#             creation by a full 104-report probe rather than deferred to dev: the series is
#             the page-1 (lineups page) chart titled "Distribution in the Final Third" —
#             present exactly once per report on 104/104, and the report's ONLY per-minute
#             two-team series (no page anywhere contains the text "Momentum", confirming
#             1.7's Task 9.3 note). Verified 104/104: baseline y=429.13 constant, home fill
#             (1.0,0.239,0.0) grows UP / away fill (0.702,0.533,1.0) grows DOWN, colour->team
#             proven by matching the legend name to each record's metadata, bar width =
#             0.70 x slot pitch exactly, peak bar ALWAYS 50.38pt (the chart auto-scales), and
#             the printed y-axis top label == the peak value in units — the one printed
#             cross-check the page offers. Values are exact non-negative INTEGERS (0
#             non-integer heights corpus-wide); pitch and unit are PER-REPORT (1.95-2.95pt /
#             2.40-5.60pt), so only the baseline, peak height and width ratio are constants —
#             a fixed-width filter silently caught only 75/104 on the first probe pass. 9
#             extra-time reports carry a 120 tick (span 131-144 vs 94-113). Like 1.12's
#             possession-regain map there is NO printed row total: the bar sum matches no
#             Domain B field over all 208 team-innings (receptions_in_final_third is
#             consistently smaller), so the axis-scale check is the genuine validation and
#             must not be manufactured into a fake reconciliation. Contract finding that
#             shapes Task 6: the provisional MomentumSample.minute $refs Minute (0-120,
#             "capped at the end of the period", stoppage carried separately), so it CANNOT
#             represent the 94-144 stoppage-inclusive slots — every first-half stoppage slot
#             collides on minute 45. Fix follows existing precedent: minute -> at:
#             MinuteStamp (the {minute, stoppageMinute} composite already used by every
#             other `at:` field), plus number -> non-negative integer. Bump is NOT strictly
#             additive and the story says so. Safe anyway: an app/src sweep found NO
#             field-level consumer — 2.5 (done) only tests `bundle.momentum !== null`, 2.6 is
#             backlog. Fixture trap flagged: m002's `momentum: null` is a DELIBERATE synthetic
#             edge case (AD-14 requires one) and must NOT be regenerated — all 104 real
#             reports have a band, so the null branch never fires on real data. Anchor
#             "Distribution in the Final Third" is NOT in the registry; story adds it. One
#             question left to dev: slot->minute mapping (first half is stable at slot=minute-1;
#             everything after HT shifts by the per-report stoppage allotment, and tick labels
#             merge as '45HT'/'90FT' on 13 reports). Touches pipeline/extract/ + the record/
#             gate seams + /contract + fixtures — 1-10 (review) and 2-7 (in-dev) touch neither
#             /contract nor extract/momentum; app sessions MUST regenerate types after the
#             bump lands. Suite baseline 895 passed / 1 skipped at f8ca7ee.
# 2026-07-26: story 1-13 context created and moved to ready-for-dev — and the story-creation
#             probe OVERTURNED the epic's premise, corpus-wide over all 104 reports / 416
#             receiving pages: NEITHER `Offering to Receive` nor `Movement to Receive` is a
#             marker-scatter pitch map. They are dashboards. The offers page's two titled
#             panels each hold exactly 11 filled 8.229 pt dots whose positions are identical
#             between the two panels on 208/208 AND identical across every team-page sampled —
#             a static formation template carrying zero data; the movement page's single pitch
#             panel holds ZERO markers (a three-thirds bar chart), and its donut slice values
#             are inside RASTER IMAGES (one text word per donut, the centre total). There is no
#             coordinate, no per-event row and no ordinal glyph anywhere in the family, so
#             `ReceivingEvent` (playerId/playerName/type/movementType/at/x/y per row) is
#             UNFULFILLABLE — strictly harder than 1.11's CrossEvent blocker, which at least
#             had real coordinates. Scope ruled by Juan: re-scope inside the story rather than
#             correct-course. The story extracts what the pages prove (offers: 5 KPIs, the two
#             in-panel shape badges, Most-Offers block, per-player made/received/% table;
#             movement: 4 donut centre totals, the 15-cell third x type grid, Top Ranked
#             Players) and NINE reconciliations were measured exact corpus-wide to key
#             Self-Validation on — incl. three cross-domain ones against Domain G's per-player
#             offers (total made/received, and the grid's per-type totals == G's FIVE-type sum,
#             never its six-type sum, which is what IDENTIFIES what the movement page counts),
#             plus the table percentage == round(100*received/made, 1) exact on 3,208/3,208
#             rows (81 rows print made == 0 — the family's one crash branch). Two documented
#             absences take AC 2's absence branch: raster-only slice values, and the per-phase
#             totals which are NOT a partition (phase sum minus total ranges -48..+314, equal
#             on only 3/208) — never sum them. The chain is still composed (frame detect +
#             an 11-dot/one-fill census tripwire) so a future template that draws real markers
#             aborts loud instead of publishing silence; no chain edit. Gate ids ruled
#             `receiving-parse`/`receiving-count-match` deliberately — `offers-count-match` is
#             test_checks_registry.py's unclaimed placeholder (1.12 moved it there) and
#             claiming it breaks that test. Task 7 owes the AD-14 filings: the 1.16 emission
#             blocker (`events.receiving` can only be null), the re-scope notice for Story 2.9,
#             and the notice that 2.3's offers/movement sign-off row (PASS-with-note on an
#             unfixtured `movementType: null`) was filed without knowing the source cannot fill
#             the shape at all. Touches pipeline/markers/ + the two extract seams + gate checks
#             only — stays off pipeline/extract/ for in-review 1-10; deferred-work.md's
#             filter-chain advisory reaches its LAST named story here.
# 2026-07-26: story 1-13 implemented and moved to review — pipeline/markers/receiving.py.
#             The story-creation probe's premise overturn was RE-DERIVED from scratch over
#             all 104 reports / 416 receiving pages by an INDEPENDENT prototype extractor
#             written before the module existed, then compared cell-for-cell with the
#             shipped parser: 208/208 sides agree, 0 disagreements. Every pre-filed figure
#             reproduced exactly — offers 4 qualifying rects / 2 distinct panels 208/208,
#             11 dots per panel on 416/416 panels at 8.229 pt in ONE fill with a
#             BYTE-IDENTICAL 11-offset signature on all 208 pages, exactly 1 word inside
#             each panel (the badge), row counts 16/15/17/14/13 = 154/26/15/11/2, movement
#             exactly 1 stroked panel with ZERO markers and 48 in-panel digits (15 grid +
#             33 axis ticks), 5 deduped image rects with exactly 1 digit inside each titled
#             donut, 5 Top-Ranked rows 208/208. All NINE reconciliations exact: r1-r5
#             208/208, r6-r8 208/208 (incl. #8, the grid == Domain G's FIVE-type sum that
#             IDENTIFIES what the page counts), r9 3,208/3,208 rows worst deviation 0.0 pp.
#             The crash branch measured: 81 rows print offers_made == 0 and all 81 print
#             "0%" — skipped by count, never divided, never coerced. Both documented
#             absences confirmed: donut slices raster-only, phase delta -48..+314 zero on
#             3/208 — recorded as null counterparts + one warning each, NO check (the
#             binary aggregator would fail every record). Seven check ids / 26 checks per
#             report, both operands always present; the two cross-domain families computed
#             at the extract seam and emitting NOTHING when Domain G is unavailable. The
#             chain is composed but stages nothing: the 11-dot census is a template-revision
#             tripwire so a future page that draws real markers aborts loud instead of
#             publishing silence — NO chain edit. Gate ids receiving-parse /
#             receiving-count-match (offers-* deliberately unclaimed — it is
#             test_checks_registry's placeholder). DISCOVERY not in the pre-filed probe:
#             four corpus pages print a THREE-LINE player name straddling its numeric row,
#             which the first full sweep failed on; closed with the crosses/defensive-actions
#             name x-band. ONE forced repair, and it was the named trap: counts keyed
#             grid_by_type by kebab codes (the 1.12 slip), caught by
#             test_record_keys_are_snake_case. Suite 906+2F -> 1040 passed / 1 skipped /
#             0 failed (+66 tests from this story). Batch --force 104 extracted / 0 failed /
#             exactly 2 SV failures (M19, M58 — 1.12's ledgered pair, no third), re-run
#             104/104 skipped-unchanged, records byte-identical 104/104. FR-15 gate PASS
#             0 deviations with both new ids in checks_run; deviation path proven live on a
#             forced-mismatch directory (one deviation per failing team AND check id, both
#             operands in specifics). Task 7 filed six AD-14 notes — headline: ReceivingEvent
#             is unfulfillable in EVERY required field, so 1.16 can only emit
#             events.receiving: null, Story 2.9 must be re-scoped against the aggregates,
#             and 2.3's offers/movement sign-off row is superseded. deferred-work.md's
#             filter-chain advisory reached its LAST named story and is CLOSED (facets a/b
#             never engage — one fill; facet c held on two independent geometry defenses).
# 2026-07-27: story 1-13 code review — 3 decisions ruled, 14 patches applied, status done.
#             Three adversarial layers over the 1.13-scoped diff; all three ACs audited as
#             satisfied and every Task 2.x/3.x/4.x constraint plus the scope boundaries
#             verified independently. The substantive theme was TEST EVIDENCE, not parser
#             correctness: every player row carries the same DEFAULT_OFFERS, so total_made
#             is always 21 x N — divisible by 3 for any lineup — and the fixture's
#             total//3 and total//2 splits printed 84/84/84 thirds and 126/126 shape
#             badges. test_panel_typing_is_text_anchored_not_positional was therefore
#             asserting 126 == 126 and passed whether panel typing read the printed titles
#             or merely the x order; the movement grid was identical across all three
#             thirds, so any permutation was invisible. Fixtures de-degenerated to
#             asymmetric splits. RULED: the grid now asserts 15 DISTINCT (pitch third,
#             movement type) pairs — the 5-per-third guard alone was satisfied by a
#             cross-band swap, and no reconciliation is third-sensitive (grid-total and
#             domain-g both sum across thirds); phase_sum REMOVED from counts (Task 7.4
#             tells downstream never to sum the phases, and staging a pre-computed sum is
#             what invites it); receiving-offers-table-pct now records both operands. The
#             gate's receiving anchor loop was hand-rolled and skipped _domain_anchor_pages'
#             registry-drift LookupError, so an authoring bug would have surfaced as
#             ReceivingPageLayoutError(pages=None) on all 104 reports — routed through the
#             shared helper. Also: top_ranked_players asserts five DISTINCT types (a future
#             sixth "No Movement" row was silently skipped while the 5-row count still
#             passed), _count_check spreads **extra first so a caller keyword cannot
#             overwrite the verdict, _rotated_lines uses the direction tolerance instead of
#             bit-exact equality, _labelled_value splits its two diagnoses, and two
#             cross-domain FAILING-case tests were added (Task 5.2 required pass and fail
#             for each of the seven ids; those two had only the green path). Three doc
#             figures corrected: 78 -> 81 zero-made rows, 66 -> 67 tests, "six AD-14 notes"
#             -> five (Tasks 7.1-7.5) plus two ordinary deferrals. 7 findings deferred to
#             the ledger, 5 dismissed. Suite 1041 passed / 1 skipped (+2 from this review);
#             the lone test_code_version_is_stable_across_calls failure is the documented
#             in-flight-edit artifact (Story 1.9 was editing pipeline/** during the run)
#             and passes clean in isolation.
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-1: in-progress
  # De-risking gate first (per sprint-planning directive): venue × matchday template check
  1-4-template-consistency-verification-across-the-venue-matchday-sample: done
  # Contract bootstrap (AD-14) — unblocks Epic 2 via fixtures
  1-1-contract-v1-schemas-fixtures-type-generation-spike: done
  1-2-batch-ingestion-run-manifest-text-anchored-page-discovery: done
  1-3-shots-pitch-map-parser-with-marker-count-self-validation: done
  # Gates all spatial-join stories (1-11..1-14 and Epic 2 spatial surfaces)
  1-5-marker-event-linking-via-digit-glyph-proximity: done
  1-6-domain-a-extraction-metadata-lineups-formations: done
  1-7-domains-b-c-extraction-key-statistics-tactical-identity: done
  1-8-momentum-series-extraction-oq-5-resolution: done
  1-9-domains-e-f-extraction-goalkeeping-set-plays: done
  1-10-domain-g-extraction-per-player-performance-physical-data: done
  1-11-crosses-map-parser: done
  1-12-defensive-actions-map-parser: done
  1-13-offers-movement-to-receive-parsers: done
  1-14-pass-network-extraction-nodes-edges: done
  1-15-cross-match-identity-resolution-normalized-spine: done
  # BLOCKED-pending CS-1 (Story 2.3 sign-off change-set: ShotOutcomeDetail enum + map +
  # pipeline constant/test updates — see deferred-work.md "Filed by Story 2.3 sign-off").
  # Do NOT start 1-16 before CS-1 lands as its atomic AD-14 bump commit.
  1-16-match-bundle-emission-canonical-serialization-version-stamp-budget-gate: backlog
  1-17-tournament-index-results-standings-leaderboards: backlog
  1-18-team-player-profile-artifacts: backlog
  1-19-full-batch-run-batch-report-104-104-acceptance: backlog
  epic-1-retrospective: optional
  epic-2: in-progress
  # i18n scaffolding first within Epic 2 (per sprint-planning directive); requires 1-1 fixtures
  2-1-static-app-scaffold-with-design-tokens-i18n-structure-build-gates: done
  2-2-site-chrome-header-language-theme-toggles-footer-404: done
  # AD-14 gate: sign-off before Epic 1 proceeds past the sample set
  2-3-contract-v1-per-surface-sign-off: done
  2-4-match-route-hero-layer: done
  2-5-tactical-layer-shell-key-statistics-empty-state-pattern: done
  2-6-momentum-timeline: done
  # Spatial surfaces from here need 1-5 (marker–event linking) semantics in fixtures/data
  2-7-pitch-panel-infrastructure-with-shot-cross-maps: done
  2-8-pass-network-visualization: done
  2-9-receiving-defensive-action-maps-heatmap-decision: done
  2-10-phases-pressing-blocks-set-plays-goalkeeping-sections: done
  # Epic story 2.11 is SPLIT three ways (ruled by Juan 2026-08-04 at story creation) — see the
  # 2026-08-04 note above. 2.11b depends on 2.11a; 2.11c depends on both. epics.md is NOT edited.
  2-11a-sortable-data-table-contract: done
  2-11b-expert-layer-per-player-tables: in-progress
  2-11c-expert-layer-event-logs: backlog
  2-12-tournament-hub-results-standings: backlog
  2-13-tournament-leaderboards: backlog
  2-14-header-search: backlog
  2-15-player-profile: backlog
  2-16-team-profile: backlog
  2-17-comparison-mode: backlog
  2-18-glossary-about-terminology-completion: done
  # Requires committed real /data from 1-19
  2-19-performance-accessibility-hardening-real-data-swap-launch: backlog
  epic-2-retrospective: optional

# 2026-08-04: story 2-11a code review — 3 layers, 30 raw findings triaged to 4 decisions (all ruled
#             by Juan), 8 patches (all applied), 9 deferrals filed, 5 dismissed. Status -> done.
#             Verified after patching: tsc 0, eslint 0, suite 661 passed / 24 files.
#             NOTE: 2.11a's code reached main inside 54f7093 and 0c407b3 (concurrent sessions'
#             sweeping stage captured it), not under its own commit. The review patches on top are
#             uncommitted as of this line.
