import { AboutContent } from "@/components/AboutContent";

/*
 * Minimal /about stub (Story 2.2 Task 7): exists only so the attribution
 * footer's link never 404s. Attribution statement + independence framing
 * only — Story 2.18 replaces this with the full about page (methodology,
 * glossary, credits).
 */
export default function AboutPage() {
  return <AboutContent />;
}
