import { AboutContent } from "@/components/AboutContent";

/*
 * /about. Created as a minimal stub by Story 2.2 Task 7 so the attribution
 * footer's link never 404s; FILLED by Story 2.18 (AC 3) with the methodology
 * note, the credits and the project framing. The stub docblock's "Story 2.18
 * replaces this" is discharged.
 *
 * NO metadata export, deliberately: the <title>/OG-stays-Spanish decision is
 * open and needs a human ruling, and /about has shipped without one since 2.2.
 * Adding one here would quietly take that decision. Filed, not resolved.
 */
export default function AboutPage() {
  return <AboutContent />;
}
