# Pipeline stack pins (AR-15). Installed with pip into pipeline/venv — never `uv`.
# Exact patch versions as installed and verified on 2026-07-22, Python 3.14.4 (Windows).
pymupdf==1.28.0
pdfplumber==0.11.10
pytest==8.4.2
# Contract validation (Story 1.1). The [format] extra is what makes `format: "date-time"`
# and `"date"` actually checked -- format assertion is off by default and must be opted
# into with an explicit format_checker. `referencing` replaces the deprecated RefResolver
# for cross-file $ref resolution.
jsonschema[format]==4.26.0
referencing==0.37.0
# Pinned EXPLICITLY although jsonschema[format] already pulls it in. jsonschema registers the
# `date-time` checker inside a suppressed ImportError, so if this package is ever absent the
# check does not fail -- it silently stops existing, and `kickoff: "not a time"` validates
# clean. A transitive dependency that a validation guarantee rests on is a direct dependency.
rfc3339-validator==0.1.4
